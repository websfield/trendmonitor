# Progress ledger — respin-m1 (append-only)

- 2026-08-14 — plan gate: Codex 5.5 FAIL→fixed (8 P1); respin-billing-reviewer r1 NEEDS CHANGES (6+5) → fixed, r2 NEEDS CHANGES (5+4) → fixed; respin-tenancy-reviewer r1 BLOCK (1+4+3) → fixed, r2 NEEDS CHANGES (3+3) → fixed; plan-reviewer NOT READY → ordered 4-fix list + 7 NOTEs applied verbatim → Ready-conditions met (`docs/progress/respin-m1-plan-review.md`)
- 2026-08-14 — phase 1 started
- 2026-08-14 — phase 1 built: billing-schema.ts (subscriptions/credit_ledger/stripe_events/config_versions/pause_periods per plan incl. all CHECKs + partial uniques), users.email dropped + auth FK (restrict), bootstrap/seed/layout email-param fallout, migration 0001_futuristic_stingray applied to fresh PGlite (tests) AND live Docker dev DB (12 tables; one pre-existing orphan dev row repaired first — the old seed predated the FK), config v1 seeded once + idempotent, R-20 (D-M1-1…8) appended + read back, Docker concurrency harness (guarded respin_test create/reset incl. drizzle journal schema) + STRICT 8/8 parallel-bootstrap race test green twice, CI gained the Postgres service container + no-silent-skip check, PGlite SHORTCUT marker retired.
- 2026-08-14 — entry-gate PASS: typecheck/lint (0 errors 0 warnings)/test 70 passed + 1 loud-skip (docker suite; proven separately 8/8 with env, twice)/db:check clean/keyless build exit 0. Evidence: docs/progress/respin-m1/phase-1-entry-gate.txt.
- 2026-08-14 — least-confident: (a) stripe_events carries receivedAt/processedAt instead of the house createdAt/updatedAt pair — justified by D-M1-1 (rows never update post-commit) but a deviation the plan's exception list didn't name explicitly; (b) pause_periods has no CHECK that ended_at > started_at — the open-pause partial unique doesn't prevent a nonsense interval; Phase 2's pause ops must enforce it.
- 2026-08-14 — reviewer respin-tenancy-reviewer (code round 1): NEEDS CHANGES — 3 CHANGE (CI pipefail can green a failing race suite; pause interval CHECK missing; expiry-idempotency index bypassable via NULL ref_id) + 3 NOTE. reviewer respin-billing-reviewer (code round 1): NEEDS CHANGES — same two structural CHECKs (both probe-confirmed live bypasses) + lot expires_at CHECK + 5 NOTE. All fixed same session: migration 0002_volatile_fenris adds 4 CHECKs (expiry_ref, lot_expiry, pause interval, stripe_events outcome vocabulary), applied to PGlite + Docker; AC-4 suite extended to 75 tests incl. the exact probe cases (inverted interval, NULL-ref_id expiry, NULL-expiry grant, remaining sign kinds, outcome vocab); seed test now deep-equals CONFIG_V1_SEED; CI step gains shell:bash (pipefail); sole-writer comment softened to forward reference; stripe_events timestamp deviation recorded in R-20 D-M1-1 + the plan's exception list. Suite 75/75 + concurrency 8/8 green; db:check clean; keyless build exit 0.
- 2026-08-14 — reviewer respin-tenancy-reviewer (code round 2): PASS (Ready, A) — all six round-1 fixes verified by execution (incl. its own re-probes); 1 informational NOTE (expiry-lot unique is global by design — fails closed on cross-workspace collision), now pinned as a comment on the index. reviewer respin-billing-reviewer (code round 2): NEEDS CHANGES with exactly one one-line finding — the stripe_event_id-unique test had gone vacuous (probe-proven: the duplicate row tripped lot_expiry before the unique) — fixed same session (`expiresAt: FUTURE` on the duplicate insert) + the ref_id-is-always-the-lot-uuid assumption pinned; suite re-run 75/75, db:check clean, keyless build exit 0. All other round-1 fixes verified real by execution.
- 2026-08-14 — phase 1 complete. Evidence: migrations 0001+0002 applied to fresh PGlite (suite) and live Docker (12 tables + 4 round-1 CHECKs); 75/75 PGlite tests incl. AC-4 1:1 constraint enumeration with probe-derived cases; strict 8/8 Docker race test green (×3 runs incl. reviewer re-runs); db:check clean; keyless build green; R-20 (D-M1-1…8 + timestamp deviation) in decisions.md; CI service container + pipefail-guarded no-silent-skip step; all four M1-entry obligations discharged (concurrency test live, email guard retired by column removal, auth FK restrict, dual-truth resolved).
- 2026-08-14 — phase 2 started
- 2026-08-15 — phase 2 built: @respin/config (RespinConfigV1 strict Zod, fail-closed getActiveConfig, append-only appendConfigVersion; parity test drives from the real CONFIG_V1_SEED), @respin/credits (pure D-M1-7/D-M1-8 fold in fold.ts; deriveBalance sole authority w/ lazy materialization + D-M1-7 lock composition; grant/pack/adjust/refund/debit ops all TxLike-composable with typed refusals; clock guard on allocating writes; state machine w/ read-time tier + lazy grace; pause record-keepers), VerifiedWorkspaceId brand + trustWorkspaceId allowlist cage (named-files lint + 5-direction fixtures + grep assertion). Suites: config 5, balance/fold 11 (every edge bullet named incl. the REQ-G03 pack-vs-grant case, compounding pauses, refund-after-materialization invariant), ledger ops 8, state 5, isolation 8 (1:1 enumeration assertion over the exported surface incl. B's-open-pause case), boundary +5 = 117 PGlite tests; Docker races 4/4 (20-debits-exactly-10, same-event dedupe, one-expiry-row ×8 readers, MIXED debit-tx/bare-db no-hang). CI concurrency step now runs all package suites (pnpm -r).
- 2026-08-15 — entry-gate PASS: typecheck/lint clean, 117 passed + 5 loud-skips (docker suites, proven separately with env), db:check clean, keyless build exit 0, AC-6 allowance-literal grep clean.
- 2026-08-15 — least-confident: the fold computes each lot's effective expiry from the FULL pause history rather than the pause state as of each historical event — argued time-consistent (a pause can only shift an expiry it precedes), and the compounding-pause test pins the intended semantics, but if that argument has a hole anywhere, historical debit allocations could differ from what the runtime guard allowed at write time.
- 2026-08-15 — reviewers (code round 1): respin-tenancy-reviewer BLOCK (AC-5 flaky-red via ms-exact clock guard vs lock serialization; pre-creation pause inflation in effectiveExpiry — both probe-reproduced; + vacuous git-grep on untracked tree; deep-subpath lint bypass; 3 NOTEs) and respin-billing-reviewer BLOCK (same fold root cause, probe: post-pause grant got +pause-duration life; refund double-shift — same root cause; test gaps; 5 NOTEs). ALL fixed same session: effectiveExpiry now clamps to the lot's own lifetime (pause ended before birth shifts nothing; straddling pause counts post-birth portion only — 3 new table cases + refund-no-double-shift case); clock guard symmetric 60s skew (stale-clock intent, not ms ordering — pause-end interval case updated, beyond-skew case added); git grep --untracked; exports map on @respin/db kills deep subpaths (no live deep importers, verified); dangling app-server exports removed from credits/config manifests; frozen-source refund gets its own message; dual-pause-truth warning pinned in pause.ts; AC-3 completions (expiring positive adjust order, negative-adjust-paused, paused-refusal-no-row, invariant in remaining cases). Full gate re-run: 123 PGlite + 5/5 Docker races green, lint/typecheck/db:check/keyless build clean.
- 2026-08-15 — reviewers (code round 2): respin-tenancy-reviewer PASS (Ready, A) — all 5 fixes verified by execution (AC-5 stable across 4 runs; deep-subpath probes ERR_PACKAGE_PATH_NOT_EXPORTED; grep assertion sees 7 files non-vacuously; exports map strictly narrowing; clock softening has no tenancy dimension); 2 cosmetic NOTEs. respin-billing-reviewer PASS (Ready, A) — both blockers verified by re-running its round-1 probes (pre-pause grant now Mar-1/balance-0; refund effective EQUALS source); fix-tests discriminate against the round-1 code; 1 trivial NOTE (overlapping-pause double-count bounded to the 60s window) — hardened same session with interval merge + named test (124th test).
- 2026-08-15 — phase 2 complete. Evidence: 124/124 PGlite tests (config parity/fail-closed, fold table incl. 6 pause-lifetime cases + REQ-G03 order + invariant, ledger ops + typed refusals, state machine, 8-case isolation suite w/ enumeration assertion) + 5/5 Docker races (20-debit exactly-10, same-event dedupe, 8-reader one-expiry-row, mixed-lock no-hang, bootstrap 8/8); lint/typecheck/db:check/keyless build clean; trustWorkspaceId cage proven in 5 directions + resolver-level deep-import refusal; both Critical-Path gates PASS (Ready, A) at round 2.
- 2026-08-15 — phase 3 started
- 2026-08-15 — phase 3 built: stripe SDK v22.5.0 (API 2026-07-29.dahlia; shapes verified against installed types — billing_reason values, items-level current_period_*, pause_collection, webhooks.generateTestHeaderString), lazy adapter (keyless build green), customer↔workspace mapping at checkout creation (D-M1-6 sole authority), single-tx webhook dispatch (D-M1-1: handler+event-row+processed-mark one tx; PK race → DuplicateStripeEvent → 200), handlers (cycle-only grants w/ period_end+1mo expiry + config version; grace idempotent-never-extends + lazy downgrade + recovery-clears; pack checkout; pause_collection mirror sync incl. deleted-while-paused; auto-topup PI), six owner-gated actions (AlreadySubscribedError F1, config pauseMonths bounds, portal, setAutoTopup validation), maybeAutoTopup (one-pack-per-trigger F4, cents-based cap, idempotency key, paused refusal), checked-in idempotent setup script w/ typed keyless remedy, app-server facades for credits+config + ADMIN-ONLY config write entrypoint, eslint builder-pattern allowlist (base + admin override from ONE function), webhook route (constructEvent edge, 400-no-row on bad signature).
- 2026-08-15 — entry-gate PASS: 157 PGlite tests (18 webhook integration incl. all 6 accept-when cases + F2 proration-ignored + unmapped-price self-heal + D-M1-1 discriminator; 7 actions/auto-topup; 19 boundary fixtures incl. facade + admin-only directions; 13 routes incl. signature-layer proof via generateTestHeaderString) + 6/6 Docker races (new: same event id ×8 → exactly 1 grant + 7 duplicates); typecheck/lint/db:check/keyless build clean; vitest hookTimeout raised to 30s (PGlite cold-start flake class, same rationale as the M0 testTimeout fix).
- 2026-08-15 — least-confident: the hand-constructed Stripe.Event fixtures — every field I read is verified against the installed SDK's types, but a REAL payload nesting a field differently (invoice.customer expansion, checkout session.subscription id-vs-object) would pass these tests and fail live; the Phase 4 owner test-mode run (stripe listen) is the honest check. Secondary: pause actions write Stripe-then-local with webhook reconciliation as the local-failure recovery — the reconciliation branch is tested, the failure interleaving itself is not.
- 2026-08-15 — phase 3 reviewer gate round 1 ABORTED (not a verdict): both respin-billing-reviewer and respin-tenancy-reviewer terminated mid-run on an API usage-credit exhaustion, after confirming only partial checks (schema clean for B1; 157 PGlite tests pass). NO gate verdict was rendered; re-run required before phase 3 can be called complete.
- 2026-08-15 — phase 3 reviewer gate round 2: BOTH gates BLOCK — respin-billing-reviewer and respin-tenancy-reviewer, four distinct blockers, two found independently by both. (1) grant expiry read `invoice.period_end` instead of the line item's service period → no rollover for any new subscriber (REQ-G02); (2) pack credits minted at `checkout.session.completed` before payment settled; (3) `isUniqueViolation` matched EVERY 23505 → any collision became DuplicateStripeEvent → 200 with no row, no ledger write, no log (reachable via pause_periods_open_uq / subscriptions_subscription_uq / credit_ledger_expiry_lot_uq); (4) `customerIdOf` read `data.object.customer` on `customer.*` events where the object IS the customer → resolvable creator PII (email/name/address) left with workspace_id NULL, outside the REQ-A04 deletion cascade. Common root cause named by both reviewers: hand-built Stripe fixtures encoding the same wrong assumptions as the code — `stripe.test.ts` fabricated a `customer` field onto a Customer object while `routes.test.ts` in the SAME diff used the real shape.
- 2026-08-15 — phase 3 blocker-fix round. HONEST STARTING STATE: the four source fixes had been applied but the suite was never re-run and was RED (2 failures — the pack fixture carried no `payment_status`; the unmapped-price test's nonsense `current_period_end: 2` mirror). Fixed this session: (a) ROOT CAUSE — every Stripe fixture is now checked against the installed SDK via `satisfies DeepPartial<T>` (a misnamed or mis-typed field is a red typecheck; it immediately caught an untyped `billing_reason`), the invoice fixture carries real `lines[].period` with top-level `period_end` pinned at creation time exactly as Stripe emits it, and `customerObject()` asserts `"customer" in obj === false`; (b) three named regression pins under `code-review blockers` — customer.* attributed + proven to cascade on workspace delete, a non-idempotency 23505 (subscriptions_subscription_uq) propagating rather than becoming DuplicateStripeEvent, unpaid pack minting nothing until `async_payment_succeeded`; (c) grant-expiry test now asserts the line period AND that the top-level field was NOT used (>20d discriminator); (d) C5 — `handleStripeEvent` moved off `@respin/credits/app-server` (importable by ALL of app/**) to `@respin/credits/webhook-server`, allowlisted to `app/api/stripe/**` through the SAME single eslint builder as admin-server, with deny fixtures at 3 paths + an allow fixture + a base-denies-still-hold fixture; (e) C6 — pause/resume convergence unified into ONE shared form (`ensurePauseStarted`/`ensurePauseEnded` in pause.ts) used by BOTH the owner action and the reconciling webhook, so they cannot drift apart again; (f) C4 — the isolation enumeration now reads all 7 public entrypoints (index, both facades, the 4 stripe modules) instead of only src/index, plus a NON-VACUITY test asserting the Phase-3 names are present there and absent from src/index, plus 7 new two-workspace cases (workspaceForCustomer, getOrCreateCustomer, handleStripeEvent writing only B's rows, the auto-topup cents cap, createPortalUrl, setAutoTopup, ensurePause*); (g) C7 — plan AC-4 and the edge bullet amended to the D-M1-6 wording (metadata is a cross-check, absent metadata processes for the mapped workspace) with a named test in the surviving direction; (h) NOTEs — the route now reads the webhook secret through the adapter's `getWebhookSecret` (one reader, was dead code), the lost-mapping-race orphan Stripe customer is now named in a warn line so it is operable, the test-only unscoped UPDATE is workspace-keyed; (i) the Docker Phase-3 race fixture gained real invoice lines (it would have hit the new service-period refusal) and a new pause-interleaving race case.
- 2026-08-15 — entry-gate PASS after the fix round: typecheck clean, lint clean (0 errors 0 warnings), 171 PGlite tests passed + 7 loud-skips (docker suites), db:check clean, keyless build exit 0; Docker races with TEST_DATABASE_URL live: db 1/1, credits 6/6 (up from 5 — the new owner-Pause-vs-webhook interleaving case green).
- 2026-08-15 — phase 3 reviewer gate round 3: respin-tenancy-reviewer NEEDS CHANGES (Almost, B) — it re-ran every probe and verified ALL 12 round-2 fixes real by execution (incl. a tsc type-probe proving the fabricated `customer` key is now TS2353, and a live `pg_constraint` query proving the idempotency allowlist names real constraints); 4 CHANGE + 4 NOTE remained. respin-billing-reviewer BLOCK (Not yet, D) — round-2 blockers 3 and 4 confirmed genuinely fixed, but 1 and 2 only PARTIALLY, and it reproduced three money defects with probes, TWO OF THEM INSIDE THIS ROUND'S OWN FIXES: (a) `firstSubscriptionLine` returned `lines.data[0]`, but the installed SDK documents that `lines` sorts pending invoice items INCLUDING PRORATIONS first — so after any portal plan switch both the allowance and the expiry came from the wrong line (probe: Pro subscriber charged for Pro received the Creator allowance, expiring a month early). Same defect class as the blocker it replaced, surviving because every fixture had exactly one line; (b) `checkout.session.completed` and `.async_payment_succeeded` shared one minting branch keyed only on `stripe_event_id`, and the two events carry DIFFERENT ids → probe minted 2 pack rows for one $10 session; (c) pre-existing: `customer.subscription.deleted` never cleared `graceExpiresAt`, so a late/final `invoice.paid` hit the grace-clearing branch and wrote `status: "active"` — probe showed a canceled workspace holding a paid tier permanently with no event that would ever correct it, directly contradicting the comment two lines above it. Plus 3 CHANGE (interval-blind allowances; the checkout subscription-mode mirror write had no order guard and no test; the pause convergence had silently dropped the `resumesAt` update on a re-pause — another regression from this round) and 7 NOTE.
- 2026-08-15 — phase 3 round-3 fix round. VERIFIED THE CENTRAL CLAIMS FIRST rather than taking them on trust: the `lines` ordering docstring read verbatim out of the installed `Invoices.d.ts`, and `parent.type === "subscription_item_details"` confirmed as the discriminator in `InvoiceLineItems.d.ts`. Fixed: (a) `subscriptionLineOf()` selects by DISCRIMINATOR, never by position, and a grant-bearing invoice with no subscription line now fails closed instead of pricing off the mirror — pinned by BLOCKER 5 (proration first → Pro allowance + subscription-line expiry, asserting the creator allowance was NOT used) and 5b; (b) per-SESSION pack idempotency — an in-tx pre-check plus a new partial unique `credit_ledger_checkout_session_uq` (migration 0004_chubby_nemesis, applied to PGlite and to live Docker, index verified by `\di`), pinned by BLOCKER 7 (both settlement events, different event ids → 1 pack) and 7b (the index itself refuses, asserted on `err.cause.constraint`, i.e. the guarantee is structural not just a pre-check); (c) invoice.paid never revives a TERMINAL subscription (canceled/incomplete_expired/unpaid) and `deleted` clears the stale grace deadline — pinned by BLOCKER 6 plus a companion test proving past_due recovery STILL works, so the fix is not just "stop writing status"; (d) non-monthly intervals refuse loudly rather than under-grant; (e) the checkout subscription-mode branch is order-guarded and stamped, with BLOCKER 8 covering both the id-string and expanded-object shapes and 8b proving a stale event cannot repoint the mirror at a dead subscription; (f) `ensurePauseStarted` reconciles `resumesAt` on an already-open pause; (g) T-C1 — a dynamic-`import()` source scan (eslint's no-restricted-imports has no ImportExpression handler, so `await import("@respin/db")` was invisible to the whole cage), itself proven non-vacuous against a planted violation, and the FOUR comments claiming the connection is "unreachable" corrected to name both mechanisms; (h) T-C2 — the webhook lint grant narrowed from `app/api/stripe/**` to `app/api/stripe/webhook/**`, with deny fixtures at sibling checkout/portal routes; (i) T-C4 — the enumeration is now DERIVED from `package.json` exports plus a walk of `src/**`, so a new public subpath (exactly how webhook-server escaped the round-2 guard) fails the suite; (j) NOTEs — `winner` null-check, `payment_status` refusals now say which status, the grep offender filter tightened to `packages/*/tests/`, the DeepPartial guard's three known limits written into its own comment (it is a SHAPE check and could NOT have caught the wrong-line defect), and the production event list documented in README + plan REQ-G06 including the `async_payment_succeeded` subscription without which the settlement fix is inert in production.
- 2026-08-15 — entry-gate PASS after round 3: typecheck clean, lint clean, 184 PGlite tests passed + 7 loud-skips, db:check clean, keyless build exit 0; Docker races db 1/1 + credits 6/6 with TEST_DATABASE_URL live. NOTE: the Docker Phase-3 race initially went RED on the subscription-line discriminator (its fixture predated it) — caught only because the race suite was actually run with the env set; it would otherwise have been a CI-only failure.
- 2026-08-15 — least-confident (round 3): (a) BLOCKER 7's double-mint depends on Stripe actually emitting BOTH `completed(paid)` and `async_payment_succeeded` for one session — the reviewer rated that medium-high, and I did not verify it against live Stripe; the fix is justified by Stripe's documented "fulfilment must be idempotent per session" guidance regardless of emission, so it is defence-in-depth, not a proven live bug; (b) the non-monthly-interval refusal reads `price.recurring` off the line item, a shape no fixture or live payload in this repo has yet exercised — if that path is wrong the guard silently never fires (it fails OPEN to the monthly assumption), and no current config maps a non-monthly price; (c) unchanged and now three rounds old: no fixture is a captured live test-mode payload, and the Phase 4 owner `stripe listen` run remains the only honest end-to-end check.
- 2026-08-15 — least-confident (fix round): the blocker-2 pin uses `subscriptions_subscription_uq` because it is deterministic in a single-threaded PGlite suite; `pause_periods_open_uq` — the constraint the reviewer actually reached via the pause interleaving — is only exercised in the Docker race, where the losing writer may legitimately win the check-then-insert and produce no violation at all, so that test asserts the INVARIANT (one open pause, a loser never silently 200s) rather than forcing the 23505 every run. Secondary, unchanged from the build entry: no fixture is a captured live test-mode payload; the Phase 4 owner `stripe listen` run remains the honest end-to-end check.
- 2026-08-15 — CORRECTION to the round-3 entry above: the claim that "a live `pg_constraint` query proved the idempotency allowlist names real constraints" is inaccurate for one of the two names. `credit_ledger_stripe_event_uq` is a unique INDEX, not a constraint, so it does not appear in `pg_constraint` at all (round-4 review confirmed this against the live Docker DB). The allowlist still works — Postgres reports index names in the same `constraint` field of the error, which `stripe.test.ts` "BLOCKER 7b" demonstrates by asserting `err.cause.constraint` for the sibling index — but the evidence recorded did not show what it said it showed. Also recorded: the Docker credential is `respin_local_dev` (docker-compose.yml), not `respin`; a round-4 reviewer hit an authentication failure from a URL I supplied.
- 2026-08-15 — phase 3 reviewer gate round 4: respin-tenancy-reviewer NEEDS CHANGES (Almost, C+) — 0 BLOCK, and it verified ALL SEVEN round-3 fixes real BY EXECUTION (eslint-API probes across 10 paths x 16 import shapes; a Node simulation of the derived enumeration against the real package.json with four mutations, confirming a new public subpath fails the suite; live pg_constraint/pg_indexes for all six constraint names). 4 CHANGE + 5 NOTE. respin-billing-reviewer BLOCK (Not yet, D) — 4 BLOCK + 4 CHANGE + 4 NOTE, THREE of the four living inside or beside round-3's own fixes: (a) `subscriptionLineOf` selected the first `parent.type === "subscription_item_details"` line, but a subscription PRORATION is exactly that shape — the installed SDK gives `Parent.SubscriptionItemDetails` its own `proration` boolean and `Invoices.d.ts` names that field as the recommended way to find prorations — so the renewal after any plan switch STILL priced off the proration; the BLOCKER 5 pin could not catch it because the fixture built the proration as `invoice_item_details` and then ASSERTED that shape; (b) `invoice.payment_failed` was the one status writer of four with no terminal and no order guard, so `deleted` then `payment_failed` then `paid` walked a canceled workspace to `past_due` and then to permanently `active` — round 3 fixed the one-event case and left the two-event case, which is the ordinary end of Stripe dunning; (c) the non-monthly refusal was provably DEAD CODE: `Stripe.InvoiceLineItem` has no `price` member at all, a cast hid that from the compiler, and the reviewer proved it with a `tsc` probe (TS2339) — it failed OPEN to the monthly assumption, exactly the risk my own round-3 least-confident line named; (d) NEW regression from round-3 fix (e): the checkout branch stamped the SHARED `mirrorEventAt` watermark from a PARTIAL write (subscription id only), so a checkout event with a later `created` second suppressed the real `customer.subscription.created` — a paying workspace derives to `free` and the F1 double-billing guard silently switches off.
- 2026-08-15 — phase 3 round-4 fix round. Verified both SDK claims in the installed types before touching anything (`SubscriptionItemDetails.proration` exists; `InvoiceLineItem` has no `price`, only `pricing.price_details.price: string | Price`). Fixed: (a) `subscriptionLinesOf()` filters prorations by `parent.subscription_item_details.proration !== true` and returns ALL matches so the caller fails closed on 0 (only prorations) and on more than 1 (multi-item, no rule for which line is the allowance) — pinned by BLOCKER 5 (which now asserts the TRAP shape, both proration kinds ahead of the real line), 5b and new 5c; (b) the interval guard now measures the SERVICE PERIOD (20-45 days), not the price's `recurring` interval, because the interval is NOT in a webhook payload — `pricing.price_details.price` is a bare id string unless expanded — so the previous fix would have stayed dead code even with the path corrected; pinned by BLOCKER 3 (annual refused, monthly still passes); (c) one shared `invoiceMayWriteStatus()` (terminal + order) now guards EVERY invoice-driven status write, and `invoice.payment_failed` additionally requires a subscription-generated invoice — pinned by BLOCKER 6b (the two-event sequence) and a one-off-invoice case; (d) the checkout branch guards WITHOUT stamping, since the watermark means "full subscription snapshot" — pinned by BLOCKER 8c asserting the full mirror survives and the tier is real, with BLOCKER 8's old assertion (which pinned the regression) inverted; (e) `customer.subscription.deleted` order-guarded; (f) symmetric per-INVOICE idempotency: in-tx pre-check + partial unique `credit_ledger_invoice_grant_uq` (migration 0005_salty_marvex, applied to PGlite and live Docker, verified by psql), pinned by BLOCKER 9 and 9b (structural, asserting `err.cause.constraint`); (g) T-C1 — THE SPECIFIER-SHAPE HOLE: every cage rule was anchored to `@respin/` names, so `@/packages/db/src/client` and `../../packages/db/src/with-workspace` reached the raw connection, the non-session mint, raw tables, the dispatcher and the admin config write with zero lint errors; closed with a blanket path-deny group, a WIDENED scan regex, and 25 deny fixtures across 5 app-side locations plus an allow fixture proving the deny is shape-blind and not a blanket ban; (h) T-N2 — guard and probe now share ONE `scanForDynamicPackageImports()` (their git invocations had already drifted), with three planted shapes proving non-vacuity; (i) T-C3 — the pack pre-check is workspace-scoped and both indexes' deliberate globality is pinned on the schema; (j) T-C4 — the A-vs-B live-subscription case that three STRIPE_BOUND exclusions cited BY NAME now exists (it never did), plus a two-workspace pack-checkout case; (k) C3 — `maybeAutoTopup`'s SUCCESS path finally has tests: the PI params, the idempotency key at n=0 and n=2 (which the plan claimed the Docker race asserts — it did not), and the cap's ALLOW direction at exactly-at-cap; (l) claims corrected wherever they outran the code — the two surviving "unreachable" assertions, the stale `app/api/stripe/**` allowlist comment, the drifted db.test.ts enumeration (both new indexes now have a 1:1 case including the cross-workspace direction), config literals replaced by CONFIG_V1_SEED, and `async_payment_succeeded` added to the "EVERY handled event type" double-delivery list; (m) the Docker pause race now uses DISTINCT event ids so its honesty clause is discriminating rather than trivially satisfied.
- 2026-08-15 — entry-gate PASS after round 4: typecheck clean, lint clean (0 errors 0 warnings), 205 PGlite tests passed + 7 loud-skips (up from 184), db:check clean, keyless build exit 0; Docker races with TEST_DATABASE_URL live: db 1/1, credits 6/6. NOTE (second round running): the Docker race again went RED first — the new per-invoice global unique caught a FIXTURE that reused one invoice id across two different workspaces, a shape Stripe cannot produce. Caught only because the race suite was actually run with the env set.
- 2026-08-15 — least-confident (round 4): (a) the monthly guard is a 20-45 day BAND on the service period, not a reading of the price interval. It is honest about what the payload contains and it actually fires, but it is a proxy: a hypothetical 5-week billing interval would pass, and a legitimately short first period (mid-cycle start) would be refused. No config maps a non-monthly price today, and both directions are pinned. (b) `credit_ledger_invoice_grant_uq` and `credit_ledger_checkout_session_uq` are GLOBAL while their pre-checks are workspace-scoped, so a genuine cross-workspace id collision fails closed into Stripe's retry loop rather than converging — argued correct because Stripe ids are globally unique, but it is a deliberate asymmetry, and the fixture failure above is what it feels like when the premise is violated. (c) unchanged and now four rounds old: no fixture is a captured live test-mode payload, and the Phase 4 owner `stripe listen` run remains the only honest end-to-end check.
- 2026-08-16 — phase 3 round-5 fix round (external billing review, 8 findings + 4 evidence gaps). VALIDATED EACH FINDING AGAINST THE CODE FIRST — two were wrong as stated and both were reported as such rather than "fixed" silently: (i) finding 1's mechanism (`invoice.payment_failed` lost when "the customer mapping exists but the subscription mirror has not been created yet") is IMPOSSIBLE — the mapping row IS the mirror row (`workspaceForCustomer` resolves through `subscriptions.stripe_customer_id`, the mirror reads the same table by `workspace_id`, both unique), so `!mirror` is unreachable whenever `workspaceId` resolved; (ii) finding 5's mechanism is INVERTED — the comparison is strict (`>`), so an equal `created` second is APPLIED, not treated as stale. Both nonetheless sat next to real defects, found by chasing them: an order-dependent GRACE DEADLINE (dunning `subscription.updated` arriving before the older `payment_failed` left `past_due` with a null deadline, which `state.ts` derives to **free** — the customer loses the whole 7-day window to a delivery-order coin flip, the opposite direction from the reported harm), and an unguarded RESURRECTION on the subscription mirror (`deleted` + a trailing `active` `updated` share a second routinely; whichever arrived second won, so a canceled workspace could hold a paid tier permanently — the TERMINAL rule the three invoice writers already had). Fixed: (a) `addMonthsUtc` extracted to `packages/credits/src/months.ts` and CLAMPED (31 Jan +1 → 28/29 Feb, not 3 Mar) — the four month-end call sites are invoice-grant expiry (REQ-G02 rollover), pack expiry, auto-top-up expiry and pause `resumesAt`, the last of which could outrun the `pauseMonths.max` its own guard validates; (b) per-PAYMENT-INTENT auto-top-up idempotency — in-tx pre-check + partial unique `credit_ledger_auto_topup_uq` (migration 0006_wealthy_the_leader), the third and last mint path, which had been left on the event-id unique alone while checkout sessions and invoices each got a business-object unique; (c) `createTierCheckoutUrl` passes a Stripe idempotency key built from durable state (`workspace:price:subscription-generation:urls-hash`) so concurrent callers — and the far likelier plain double-click inside the seconds-to-unbounded window where the F1 mirror guard is blind because the webhook has not landed — collapse to ONE Checkout Session, hence at most one subscription; the dead subscription id in the key is what keeps subscribe → cancel → re-subscribe from replaying the completed session; (d) grace opened by the dunning subscription event too, so both delivery orders converge on ONE deadline (`payment_failed`'s never-EXTEND rule keeps it single); (e) TERMINAL guard on the mirror writer, keyed on subscription id so a genuine re-subscribe still mirrors; (f) multi-item subscriptions fail closed, symmetric with `invoice.paid`'s existing ambiguity refusal (the mirror was taking `items.data[0]` on trust while the invoice path refused to guess between two lines — and tier is derived from the mirrored price at read time, so the workspace could be charged for one plan and entitled to another); (g) `maybeAutoTopup(db, workspaceId, shortfall, at)` restored to the pinned handoff signature and validated (deliberately NOT scaled on — F4 decided one pack per trigger); (h) the invoice service period is REQUIRED at both ends — the two fail-open branches removed were a missing `end` silently pricing expiry off `mirror.currentPeriodEnd` (a possibly DIFFERENT cycle) and a missing `start` skipping the monthly-interval guard entirely, i.e. failing open to the monthly assumption that BLOCKER 3 exists to prevent; (i) `stripe/setup.ts` builds its client through `getStripe()`, and AC-9 is now a SOURCE-SCAN TEST rather than a hand-grep — the rule had been recorded satisfied for four rounds while that second construction site existed, and the new scan caught it immediately (it first failed on this fix's own comment quoting the pattern); (j) the phase-3 Handoff Contracts block corrected — three of its six action signatures had drifted from the code Phase 4 will call (absent `email` params, a phantom `at` on resume, `tierOrPriceId` for what is a tier), the same defect class as finding 7.
- 2026-08-16 — round-5 evidence. 16 new tests, each written to FAIL against the pre-round code and several stating the discriminator in-test: `months.test.ts` (13 cases incl. an explicit side-by-side proving the naive `setUTCMonth` form disagrees, so the suite cannot pass against both implementations); 12 webhook/action pins (grace in BOTH delivery orders; equal-second resurrection AND the re-subscribe direction that must still work; multi-item fail-closed asserting the tx rolled back with no `stripe_events` row; one-PI-one-pack plus its structural index assertion on `err.cause.constraint`; 31-Jan service period → 28-Feb expiry; both period-refusal shapes; the two checkout-key properties); 1 db-schema case for the third partial unique incl. the cross-workspace direction; 1 new Docker race — one PaymentIntent under EIGHT DISTINCT event ids (distinct deliberately: reusing one id would let the event-id unique answer and prove nothing about the new one) — carrying a live `pg_indexes` assertion so it cannot pass on a Postgres where migration 0006 never ran. Entry gate: typecheck clean, lint clean, **233 PGlite tests passed** + 7 loud-skips (up from 205), db:check clean, keyless build exit 0 (`env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET`), Docker races 8/8 on real Postgres.
- 2026-08-16 — least-confident (round 5): (a) the checkout idempotency key is a 24h-TTL guarantee, and Stripe expires both keys and Checkout Sessions at ~24h — near that boundary a replay can hand back a just-expired session URL, so the user sees Stripe's own "session expired" page and the next attempt creates a fresh one. That is a self-correcting annoyance chosen over a durable pending-checkout lock (a column, a release path and a stale-lock sweeper — three new failure modes), but it IS a behaviour a live `stripe listen` run should confirm, and it is untested against real Stripe; (b) finding 2's double-mint has no confirmed second-event path in Stripe today — it is defence in depth on the same defect CLASS as the two reported siblings, not a reproduced live bug, and it is recorded that way rather than as a fix for a proven leak; (c) the multi-item and missing-period refusals both throw, which means a genuinely multi-item or shapeless payload retry-loops in Stripe forever — deliberate (fail closed, and M1 sells single-item subscriptions), but it is an outage shape if either assumption is ever wrong; (d) UNCHANGED and now four rounds old: no fixture is a captured live test-mode payload, and the Phase 4 owner's `stripe listen` run remains the only honest end-to-end check — the round-4 entry said the same thing and it is still the largest gap in this phase's evidence.
- 2026-08-16 — phase 3 reviewer gate round 5: respin-billing-reviewer **BLOCK** (Not yet, D) — 1 BLOCK + 4 CHANGE + 4 NOTE, the BLOCK living INSIDE round 5's own terminal-guard fix, which is the FOURTH consecutive round with that pattern. It verified both of my "the finding is wrong as stated" calls as correct (the `!mirror` path is unreachable; the comparison is strict so equal seconds are applied) and proved the round's 16 tests non-vacuous by MUTATION — seven source-level reverts, each killing exactly its own pin and nothing else. respin-tenancy-reviewer **NEEDS CHANGES** (Almost, B) — 0 BLOCK, all eight tenancy checks hold, every round-5 tenancy claim verified by execution (incl. re-running the module-enumeration guard with the registration removed to prove it still bites, and a `createDockerTestDb` source check proving `harness.pool` cannot see another test database); 1 CHANGE + 4 NOTE.
- 2026-08-16 — phase 3 round-5b fix round (fixing the gate's findings on the gate's own round). THE BLOCK, verified by reading the two writers before touching them: `checkout.session.completed` repoints `stripeSubscriptionId` WITHOUT writing status, and the new terminal guard identified the dead subscription BY that id — so on a re-subscribe where the checkout event beat `customer.subscription.created`, the mirror became {canceled, sub_NEW} and sub_NEW's own first snapshot looked exactly like a resurrection and was refused forever. The workspace pays, is granted its credits by `invoice.paid`, derives to **free** with no event that can ever correct it, and `isLive()` reading `canceled` switches the F1 double-billing guard off so the next checkout makes a THIRD subscription. My own FINDING-5 re-subscribe test missed it because it omitted the checkout event — the test proved the guard's happy path, not its interaction. Fixed by restoring the guard's premise rather than patching the symptom: while the mirrored subscription is irreversibly dead, the checkout branch may not repoint it (nothing is lost — the snapshot that always follows binds id AND status together), and the mirror image was closed at the same time (a late `deleted` for an OLD subscription no longer cancels the current one, which that branch's unconditional `stripeSubscriptionId: sub.id` write allowed). Also fixed: (a) `unpaid` removed from the MIRROR's irreversible set — the installed SDK documents it as payable-out-of, and treating it as one-way made such a workspace unrecoverable by any event, a worse failure than the resurrection the guard exists to stop (it stays in TERMINAL_STATUSES for the invoice writers, where BLOCKER 6/6b pin it); (b) an ACTIVE snapshot now CLEARS `graceExpiresAt`, scoping "never EXTEND" to one dunning episode — a recovery seen only as `subscription.updated → active` left the old deadline behind, and a second failure more than `graceDays` later then derived `free` with zero grace; (c) the checkout idempotency key is now `checkout:{workspace}:{subscription-generation}` — durable state ONLY. Dropping the tier closed the cross-tier race the gate reproduced (two racers on different tiers made two sessions, two subscriptions, on a mirror that is one row per workspace); dropping the URL hash removed a guarantee that depended on a caller-supplied argument, and a parameter mismatch is now a typed `CheckoutInFlightError` explaining itself instead of a raw Stripe 400. Stated consequence, accepted: an owner switching tiers mid-checkout is refused until the open session lapses — the same refusal F1 makes once a subscription exists, moved earlier to the only window where double billing was still reachable; (d) both new fail-closed throws now carry the operator's way forward (CLAUDE.md lesson 2026-07-30), naming the dashboard action and, for the multi-item case, warning that every mirror update for that workspace is blocked meanwhile; (e) the two expected-expiry ORACLES the tenancy gate caught still computing the overflowed answer — green today, red on 7 calendar days a year — now use `addMonthsUtc`, and a source scan forbids the raw month setter outside `months.ts` across src/ AND tests/, which turns months.ts's "only month arithmetic in the package" comment into an assertion; (f) the isolation enumeration gained a REVERSE assertion — an exclusion or coverage claim naming something the surface does not export now fails — which immediately caught the two stale keys the gate flagged (`getStripe`, `setupStripeProducts`; the real export is `stripeSetup`); (g) dev database migrated (it was one behind, so a `stripe listen` run against it would have exercised the auto-top-up mint with only the pre-check) — `pg_indexes` now shows all three business-object uniques.
- 2026-08-16 — round-5b evidence. 5 further pins, each written against the reproduced defect: the re-subscribe-with-checkout sequence asserting the workspace derives ACTIVE (not free); `unpaid` recovering while `canceled` still refuses, in one test, so the narrowing cannot be mistaken for a weakening; a second dunning episode getting its OWN deadline with the mirror asserted null in between; a late `deleted` for an old subscription leaving the live one alone; the cross-tier checkout race collapsing to one key; and `CheckoutInFlightError` raised on a Stripe idempotency mismatch while an UNRELATED Stripe error still propagates unchanged. Entry gate: typecheck clean, lint clean, **240 PGlite tests** + 8 loud-skips (205 → 233 → 240 across this session), db:check clean, keyless build exit 0, Docker races 8/8 on real Postgres. NOT re-verified after 5b: the two reviewer gates — this round's fixes have not themselves been through a gate, which is exactly the condition that produced four consecutive rounds of fix-induced blockers, and it is the honest state to hand over.
- 2026-08-16 — CORRECTION to the round-5 evidence entry above: it said "233 PGlite tests passed + 7 loud-skips"; the run reported 8 skipped across 2 skipped files (the credits Docker suite had gained the PI race). Recorded because the tenancy gate caught the miscount, and a count nobody checks is how "present-and-unrun" gets recorded as "present".
- 2026-08-16 — least-confident (round 5b): (a) the checkout tier-switch refusal is a DELIBERATE UX regression traded for closing the cross-tier double-subscription race — if pilot creators hit it while browsing tiers, the fix is a stored session id plus a Stripe `sessions.retrieve`/`expire` on the open one, which I judged too much new state for the window it closes but did not build; (b) the `CheckoutInFlightError` path is keyed on `err.type === "StripeIdempotencyError"` from the installed SDK's Error.d.ts and is exercised only against a MOCK carrying that shape — no live Stripe call has produced it here; (c) unchanged and now five rounds old: no fixture is a captured live test-mode payload, and the Phase-4 owner's `stripe listen` run remains the only honest end-to-end check — with the dev database now migrated, that run would at last exercise all three mint paths with their structural guarantees in place.
- 2026-08-16 — phase 3 reviewer gate round 6: respin-billing-reviewer **BLOCK** (Not yet, D) — all five round-5b fixes verified RESOLVED (incl. the volunteered `deleted` mirror-image guard, and the `StripeIdempotencyError` detection checked against `stripe@22.5.0/cjs/Error.js:204-208`), and the new month-setter scan proven non-vacuous by planting a violation — but 1 new BLOCK + 3 CHANGE, all PRE-EXISTING rather than round-5b regressions. respin-tenancy-reviewer **NEEDS CHANGES** (Almost, B-) — all five round-5 findings closed and proven by execution (module-enumeration guard re-probed with the registration removed; the stale-key reverse assertion probed against the real 40-name surface; live `__drizzle_migrations` = 7 rows). Both gates independently reproduced the SAME BLOCK.
- 2026-08-16 — phase 3 round-6 fix round. THE BLOCK (found by both gates, pre-existing since phase 3 was written): `cancel_at_period_end` is written by the created/updated branch and reset by NOBODY — the `deleted` branch never cleared it and no further events exist for a dead subscription — while `isLive()` counted it as liveness. So after the ordinary REQ-G01 self-serve cancel-at-period-end the mirror read {canceled, cancelAtPeriodEnd: true}: FREE in `state.ts`, "already subscribed" in `actions.ts`, refusing the owner a re-subscribe FOREVER and pointing them at a Customer Portal with nothing left to manage. Its sibling, from the same allowlist: `unpaid` was omitted, so the F1 guard waved a SECOND Checkout through for a subscription still alive in Stripe. Both are one root cause — two readers of one mirror row with two different definitions of "live" — so the fix is one definition: `hasLiveStripeSubscription()` in state.ts, derived from IRREVERSIBLE_STATUSES (canceled, incomplete_expired) rather than from a hand-maintained allowlist, now used by both readers; the `deleted` branch also clears the flag rather than leaving a permanently-wrong field for the next reader to inherit. Also fixed: (a) round-5b's clear-grace-on-ACTIVE was itself a hazard the gate reproduced — `invoice.payment_failed` deliberately does not stamp `mirrorEventAt`, so the watermark says nothing about a deadline's age and an OLDER `active` snapshot wiped a LIVE deadline, serving the paid tier to a non-payer. Replaced with the rule that made the clear unnecessary: never extend a **LIVE** deadline (`hasLiveGrace`), so a LAPSED one from an earlier episode is simply not a window and the next episode opens a fresh one with nobody clearing anything and nothing to race; (b) `CheckoutInFlightError` was thrown by `createTierCheckoutUrl` but not re-exported from the app-facing facade — `app/**` may import only that entrypoint, so the "typed refusal explaining itself" was not `instanceof`-reachable by its only permitted caller; (c) THE EVIDENCE DEFECT, reproduced independently by both gates: both `*.docker.test.ts` suites shared one fixed database and each starts by dropping schema public, while vitest runs files in parallel and CI sets TEST_DATABASE_URL at job scope — so the ledger race suite was being reset mid-run by its neighbour and reported "11 of 20 concurrent debits of 10 succeeded against a 100 balance", indistinguishable from a real B1/B3 over-consumption, intermittently, in the ONE suite whose whole job is proving that invariant on real Postgres. `createDockerTestDb` now takes a per-suite database name.
- 2026-08-16 — round-6 evidence. 4 new pins: the cancel-at-period-end journey end-to-end (the flag reset AND liveness), `unpaid` still counting as live, a lapsed deadline not denying the next episode its window, and an older `active` snapshot failing to wipe a live one. Entry gate: typecheck clean, lint clean, **251 tests passing with the Docker suites INCLUDED in the same run** — the exact CI shape both gates reproduced as intermittently red — stable across 3 consecutive paired runs; db:check clean; keyless build exit 0. NOT DONE, and the honest state to hand over: the round-6 fixes have NOT themselves been through a reviewer gate. Five of the six rounds so far have found a defect inside the previous round's fix; assuming this round broke that streak would be exactly the assumption this ledger keeps recording as wrong.
- 2026-08-16 — least-confident (round 6): (a) `hasLiveStripeSubscription` now treats `incomplete` as LIVE, which is right for double-billing (the subscription exists) but means a checkout abandoned at the payment step blocks a retry until Stripe expires it to `incomplete_expired` (~23 hours) — plausible and untested against live Stripe; (b) the four billing NOTEs are recorded and NOT fixed: `debitCredits` writes no `configVersion` (the B5 seam lands with M3's call site), `setup.ts`'s `AMOUNTS_CENTS` is a second price authority beside `pack.priceUsd`, the 20/45-day monthly band cites no decision, and a late `invoice.paid` on a canceled subscription still grants credits to a workspace that derives free (defensible — the money arrived — but pinned by no test); (c) `CheckoutInFlightError`'s message says "on different terms", but Stripe also returns `idempotency_error` for a concurrent in-flight request with IDENTICAL terms, so a double-click may see wording that does not fit — the safety property (≤1 session) holds either way; (d) unchanged and now six rounds old: no fixture is a captured live test-mode payload, and the Phase-4 owner's `stripe listen` run remains the only honest end-to-end check.
- 2026-08-16 — phase 3 round-7 fix round (billing NEEDS CHANGES 0/7/5, tenancy NEEDS CHANGES 0/2/3). EVERY fix carries a test written to FAIL against the pre-round code, and each was proven discriminating by MUTATION (revert the source change → exactly its own pin goes red, nothing else). Fixed: (a) CHANGE 1 — `maybeAutoTopup` had no liveness guard and `customer.subscription.deleted` never disarmed the opt-in, so an owner who enabled auto-top-up at a $50 cap and then self-serve cancelled left {canceled, autoTopupEnabled: true} for M3's debit site to charge off-session; both halves fixed through ONE definition (`hasLiveStripeSubscription` plus a shared `DEAD_SUBSCRIPTION_FIELDS` used by the `deleted` branch AND by any snapshot landing an irreversible status), with the fixtures corrected — every auto-top-up fixture in the suite had modelled a workspace that never subscribed, which is why they went red when the guard landed; (b) CHANGE 2 — app-server's comment claimed "the isolation suite now asserts this set covers the thrown surface"; no such assertion existed and the claim was false. Written as `tests/facade-errors.test.ts`: a TS-AST call-graph walk from each facade method through the package's relative imports, collecting every `new <ErrorSubclass>` it can reach. It reported `LedgerIntegrityError` and `ClockSkewError` missing from BOTH facades on its first run (that was the failing test), both are now re-exported, and deleting a re-export turns it red (proven); it also asserts the NEGATIVE direction — the debit-path errors are NOT demanded, because no facade method reaches them; (c) CHANGE 3 — the 20/45-day monthly band moved into `RespinConfigV1.monthlyPeriodDays` + seed, read at the call site, with a refusal naming the config key, the active version and both remedies, and stating that the invoice SELF-HEALS on redelivery; jsonb content, so no migration — the live dev DB got an APPENDED v2 (D-M1-2), never an edit to v1; (d) CHANGE 4 — `stripe:setup` takes a db and creates the pack price at the active config's `pack.priceUsd`, and REFUSES on divergence (a Stripe price amount is immutable — verified: `PriceUpdateParams` has no `unit_amount` — so the remedy is a new price plus a stripePriceMap update, and it says so); re-running the script IS the operator's check; (e) CHANGE 5 — the Free-tier gap is now NAMED: `allowances.free` has exactly one consumer (`invoice.paid`) and Free has no invoice, so a Free workspace's balance is 0 forever; recorded as R-21 plus a Deferral Ledger row with M3 as the receiver and a Stop-Condition tripwire, NOT patched with an unplanned minting mechanism; (f) CHANGE 6 — `createDockerTestDb`'s round-6 guard was TAUTOLOGICAL (it compared `current_database()` with the caller-supplied name the connection URL is built from), so `dbName: "respin"` would have dropped schema public on the live dev database; the guard is now a NAME whitelist checked BEFORE connecting, `dbName` is required (the default that let a third suite silently rejoin the shared DB is gone — asserted at type level), and the post-connect check is kept but described honestly as a landed-where-we-said assertion; (g) CHANGE 7 — `AlreadySubscribedError` branches on state: `incomplete` still blocks a second Checkout but now names what that state permits (pay the open invoice, or wait ~23h for `incomplete_expired`), verified against the installed SDK's own status prose rather than from memory. NOTEs fixed: a trailing canceled `updated` can no longer re-arm `cancelAtPeriodEnd` (the SDK documents `cancel_at_period_end` as surviving on a canceled object — verified, so the payload really does carry it); the grace rule became "never extend the deadline of the episode you are IN" (`inheritsGrace` keys on the mirror status), which closes the recovery-seen-only-as-`updated→active` residue without reintroducing the clear round 6 removed; a snapshot older than an owner-opened pause can no longer close it (bounded by the package's existing CLOCK_SKEW_MS, because Stripe's `created` is second-granularity); a grant-bearing invoice with no id is refused rather than granted against the event id; the isolation suite's INTERNAL_MODULES reasons are now STRUCTURED and CHECKED (`viaIndex` must be in index.ts, `internalOnly` must not) — which immediately caught the stale `state.ts` claim the gate flagged and a second nobody had noticed (`clock.ts`'s `latestEventAt`); plan/master-plan/decisions wording corrected for the REQ-G08 refType drift (the code is right — a different refType would evade `credit_ledger_invoice_grant_uq`), the retention sweep ("age- AND deletion-aware", with the deletion mechanism written out), and D-M1-6's log rule ("ids, never payload fields").
- 2026-08-16 — round-7 UNPLANNED FINDING, and the largest one: running the Docker suites as this round required reproduced round 6's "11 of 20 concurrent debits of 10 succeeded against a 100 balance" — STANDALONE, in 2 of 5 runs, with the per-suite database round 6 introduced and no neighbour able to reset anything. Round 6's diagnosis (suite interference) was therefore at best partial. The real cause is in the balance authority: `getDbNow` ran `SELECT now()`, which in PostgreSQL is `transaction_timestamp()` — the calling transaction's START — and `foldLedger` drops every row whose `created_at` is later than the `asOf` it is given. A debit committed by a transaction that BEGAN after mine but COMMITTED before I took the workspace advisory lock carries exactly such a `created_at`, so its spend was invisible to my balance check and B3's refuse-before-writing passed on money already gone. Pinned DETERMINISTICALLY rather than by a flaky race: one transaction pins its timestamp, a second connection spends the whole balance and commits, then the first debits — RED every run against `now()`, green every run against `clock_timestamp()`. Fixed in `clock.ts` (one reader, so the grace deadline, pause instants, the materialization key and the write-clock guard were ALL reading a clock that went staler the longer their own transaction ran). Docker race suite: 5/5 consecutive green after the fix, having been 2-of-5 red before it. This is Phase-2 code changed from a Phase-3 fix round — declared, not silent: it is the invariant the phase's own race suite exists to prove, and reporting it without fixing it would have handed round 9 an intermittently red money test with the wrong root cause attached.
- 2026-08-16 — round-7 evidence. 30 new/changed tests. New: 5 harness-guard cases (`respin` refused by name before any connection; 8 non-test name shapes refused; the two real suite names still pass; `dbName` required, asserted at type level; both Docker suites named distinctly, read from their sources); 5 facade/claim assertions in `facade-errors.test.ts` incl. the negative direction and a cross-package check that ConfigUnavailableError reaches app/** through @respin/config/app-server; 4 `stripe:setup` cases (config-priced pack, divergence refusal, quiet agreement, config-read-before-any-Stripe-write); 5 action pins (canceled and never-subscribed refusals, the not-a-blanket-ban direction, the `incomplete` remedy, the unchanged active message); 9 webhook pins (auto-top-up disarmed on cancel; trailing-canceled cannot re-arm; grace across episodes in BOTH arrival orders plus the within-episode direction that must NOT change; stale-pause-close and its non-vacuity twin; the config band self-healing; the id-less invoice refusal); 1 INTERNAL_MODULES claim check; 1 deterministic Docker over-consumption pin. Entry gate: typecheck clean, lint clean (0 errors 0 warnings), **272 PGlite tests passed + 9 loud-skips** keyless, **281 tests with BOTH Docker suites in the SAME parallel run — 3 consecutive green paired runs** (the CI shape), db:check clean, keyless build exit 0 (`env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET`). Live dev DB: config v2 appended (monthlyPeriodDays), v1 untouched; the orphan `respin_test` database left alone.
- 2026-08-16 — least-confident (round 7): (a) THE CLOCK FIX'S BLAST RADIUS — `clock_timestamp()` is right for the balance fold, but `getDbNow` has other readers (grace deadline, pause instants, expiry materialization, `assertWriteClock`), and I reasoned about each rather than testing each against the change; the suites pass, but a semantic that quietly WANTED transaction-start time would look exactly like this. It also means two writes in one transaction can now carry different `now()` values — nothing depends on that today, though the fold's ordering would notice if anything ever did. (b) `inheritsGrace` keys "same episode" on the mirror status being `past_due`; if Stripe ever leaves a dunning subscription in some other status while retrying, a second failure opens a fresh 7-day window instead of continuing the old one — generous rather than dangerous, but it is an assumption about Stripe's state machine no fixture proves. (c) The pause staleness bound reuses CLOCK_SKEW_MS (60s) because Stripe's `created` is second-granularity, so a snapshot 30 seconds older than an owner pause still closes it; bounded loss (the next snapshot re-opens the pause), but a threshold chosen for a clock, not measured against live delivery lag. (d) The facade call-graph walk resolves identifiers, not values — a handler stored in a data structure and called dynamically would be invisible to it; there is none today and the test asserts the graph it reached is non-trivial, but it is a static approximation, not a proof. (e) Unchanged and now SEVEN rounds old: no fixture is a captured live test-mode payload, and the Phase-4 owner `stripe listen` run is still the only honest end-to-end check — and it matters more now, because `stripe:setup` gained a database dependency only that run exercises for real.
- 2026-08-17 — phase 3 round-8 fix round (the orchestration calls this "round 10"; this ledger numbers fix rounds by the gate they answer, and NO gate entry was written for the one that returned billing BLOCK 1/4/3 + tenancy 0/2/4 — recorded here so the numbering gap is visible rather than confusing). THE BLOCK, and it is round 7's own least-confident line coming true verbatim: round 7 moved the READ clock to `clock_timestamp()` and left every `credit_ledger` insert on the column default, which is `now()` = `transaction_timestamp()` — the instant the writing transaction BEGAN. A debit that BEGINs at t0, blocks on `takeWorkspaceLock`, watches an `invoice.paid` grant commit at t1, acquires the lock at t2, folds at t2, SEES the grant and is correctly allowed — then writes its row dated t0, BEFORE the grant that funded it. Every later fold replays debit-then-grant, finds no live lot, and throws `LedgerIntegrityError … over-consumes` PERMANENTLY, because the ledger is append-only and the row cannot be removed: no usage page, no debit, no refund, for that workspace, forever, while webhook grants keep landing and the customer keeps paying. Fixed as a CLASS, not as three inserts: every insert in `packages/credits` now stamps `created_at` explicitly — allocating writes (debit, negative adjust, expiry materialization) from the fold's OWN `asOf`, so the row can never sort before a lot that fold saw; lot writes (grant, pack, positive adjust, refund) from a `clock_timestamp()` read in the same statement sequence, because `effectiveExpiry` compares a lot's `created_at` against `pause_periods.started_at` and a transaction-start stamp could make a lot look born before a pause it did not exist for (the pre-creation inflation class the tenancy round-1 BLOCK closed). The OTHER tables were audited and the decision written into `billing-schema.ts` beside the default itself: `pause_periods.started_at/ended_at` are already caller-supplied from `getDbNow`; `pause_periods.created_at/updated_at`, `stripe_events.received_at` and the `subscriptions` pair may keep the default because NOTHING derives from them (grepped, not assumed) — and for `received_at`, transaction-start is arguably the right semantic, since the D-M1-1 transaction begins when we start handling the event.
- 2026-08-17 — round-8 fixes beyond the BLOCK. B-C1: the "EVERY handled event type" double-delivery list is now DERIVED from the dispatch switch's own `case` labels (it was 7 of 8 — `customer.subscription.updated` missing, while the list also carried `customer.updated`, which dispatch does not handle at all; round 4 had already fixed this same defect once by hand, which is why the fix is on the list-BUILDING and not on the entry). B-C2/T-C1: the "the analysis is not vacuous" test ended in `expect(relative(SRC, resolve(SRC, "balance.ts"))).toBe("balance.ts")` under a comment claiming a planted error class was "proven here" — a `node:path` identity that returns the same string for a file that does not exist and touches neither the walk nor its data, i.e. the defect class that file exists to eliminate, inside the fix for that class. Replaced with a real plant: `balance.ts` is re-`analyse`d from MUTATED SOURCE TEXT carrying an unexported `PlantedFacadeProbeError`, the walk is re-run over that map, and the class must appear in `reachableErrors` and NOT in the facade's exports (with the un-mutated walk asserted not to report it). B-C3: `DebitParams.configVersion` is REQUIRED — a compile error at M3's future call site is the cheapest tripwire, and landing it produced exactly that: 18 red call sites in one typecheck; the master-plan deferral row now says so. B-C4: `setAutoTopup` arms through `hasLiveStripeSubscription` — one definition, three readers — so `{status: canceled, autoTopupEnabled: true}` is unreachable again, and the AC-6 fixture that had been deciding the contract (a mirror with no subscription id) is corrected; DISABLING stays ungated on purpose. T-C2: `stripe:setup`'s CLI evaluated `createDb(process.env.DATABASE_URL)` as an ARGUMENT, so its throw beat `.catch()` and printed a raw Node stack — the exact thing Phase-3 Verification Step 6 forbids, present-and-unrun because every test drove `stripeSetup(db)` directly. Now both env vars are checked together, up front (so a cold operator hears about STRIPE_SECRET_KEY too, which a DB-first construction had made unreachable), and a test SPAWNS the real CLI keyless.
- 2026-08-17 — round-8 NOTEs, all seven fixed. (1) The eight anonymous `throw new Error`s in `actions.ts` and the ninth in `customers.ts` are typed classes re-exported from the app facade, and `facade-errors.test.ts` now ASSERTS the walk's documented blind spot is EMPTY on that facade (zero reachable plain throws) while pinning the webhook facade's deliberate bare throws to `stripe/webhooks.ts` alone — the limit is closed on the side that matters and named on the side where it is intentional. (2) Test titles softened to what a static walk can prove, and limit 3 is now CHECKED: no unresolvable relative import, no class extending an Error SUBCLASS, and the dispatcher is still a `switch` — with non-vacuity counts, so three empty arrays cannot prove nothing. (3) `ensurePauseStarted` gained the SYMMETRIC `knownAt` bound round 8 gave only to `ensurePauseEnded`: a pause-era snapshot arriving after the owner's resume can no longer re-open the pause (credits frozen and `state: "paused"` for a paying customer, plus a permanent spurious period row shifting every lot's expiry, with no further event coming to correct it). (4) BLOCKER 6 now ASSERTS the grant it only declared — a late `invoice.paid` on a canceled subscription still mints, and that choice is pinned rather than described in a comment. (5) `stripe:setup` WARNS when a tier price's Stripe amount disagrees with the R-7 literal and prints each price id's amount beside the map it asks the operator to paste — deliberately a warning, not the pack's refusal, because a tier has ONE charger (Stripe) and no second authority to diverge from. (6) `setup.test.ts` saves and restores `STRIPE_SECRET_KEY`. (7) `DOCKER_TEST_DB_NAME_PATTERN` now requires a suffix, so bare `respin_test` — the shared name behind round 6's phantom B1/B3 symptom, still live on the dev Postgres — is refused before any connection.
- 2026-08-17 — round-8 evidence. EVERY fix carries a pin proven discriminating by MUTATION (revert the source, that pin goes red, the rest of its file stays green): the write clock (a behavioural case that sleeps 60ms inside one transaction and checks all seven writer paths against `transaction_timestamp()` — reverting the debit stamp reproduces the real production failure, `row … over-consumes`, inside the test); a SOURCE SCAN refusing any `.insert(creditLedger)` in `src/**` without `createdAt` (it first went red on the write-clock comment quoting its own pattern — the AC-9 trap again — so comments are blanked, LINE comments first, because `src/**` opens a block comment as far as a regex is concerned); a new Docker race that is the MIRROR IMAGE of the ROUND-7 case (B grants instead of spending, so the debit is correctly ALLOWED and the question becomes what instant its row carries) — RED 3/3 runs against the reverted stamp with both timestamps printed, green 5/5 with it; the derived dispatch list (red, naming `customer.subscription.updated`); the planted facade class (red, and the whole walk collapses if `resolveRelative` is broken); the plain-Error assertion (red, naming `stripe/actions.ts`, when one typed throw is reverted); the pause symmetric bound (red from BOTH the pause.ts guard and the webhooks.ts argument); the auto-top-up liveness (2 red, plus a not-a-blanket-ban direction covering past_due / cancel-at-period-end / unpaid); the CLI keyless run (red, plus a non-vacuity case where only DATABASE_URL is missing); the harness name pattern (red on bare `respin_test`); the tier warning (red, plus a must-not-cry-wolf twin); BLOCKER 6's new ledger assertion (red when the grant is suppressed). Entry gate: typecheck clean, lint clean (0 errors 0 warnings), **286 PGlite tests + 10 loud-skips keyless**, **296 tests with BOTH Docker suites in the SAME parallel run — 3 consecutive green paired runs** (the CI shape), credits race suite standalone **5/5 green**, db:check clean, keyless build exit 0 (`env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET`). No migration: the fix is in the writers, not the schema — the column default is unchanged and now carries the audit saying which tables may keep it. Phase-2 code changed again from a Phase-3 fix round (`ledger.ts`, `balance.ts`, `pause.ts`, `clock.ts`'s callers, `packages/db/src/testing.ts` + `billing-schema.ts` comments) — declared loudly, as round 7 did: the BLOCK is in the balance authority, and there is nowhere else to fix it.
- 2026-08-17 — round-8 NOT FIXED, and why. Nothing was left from either gate's list. Two things were deliberately not done: (a) the bare `respin_test` database on the dev Postgres was NOT dropped — the tightened pattern refuses it, which is the control; deleting a database I did not create is golden rule 3; (b) `setAutoTopup({enabled: false})` was not put behind the liveness guard, because the invariant to protect is that `{dead, armed}` cannot be CREATED, and a disable can only clear it — gating it would leave an owner whose subscription died while armed looking at a switch they cannot turn off, with Phase 4's billing UI as the reader. Both are stated as choices, with their reasons, rather than as omissions.
- 2026-08-17 — least-confident (round 8): (a) THE MUTATION HARNESS LIED ONCE, and I caught it only because the result was surprising. Its first version built the vitest command as a SHELL STRING and cmd.exe mangled `-t "ROUND 10"`, so the run silently selected a DIFFERENT, passing test and reported a real pin as vacuous; its second version used `execFileSync("npx.cmd", …)`, which Node 22 refuses to spawn without a shell — that throw was indistinguishable from a red test run and produced no output at all. The final harness runs whole files and prints the failing test, and the Docker pin was additionally verified by hand 3/3 with the mutation's landing confirmed on disk. But one harness invocation still reported that pin GREEN and I could not reproduce it; the most plausible explanation is vitest's mtime-keyed transform cache serving the pre-mutation module when mutate-and-run happen within one file-timestamp tick, and I did NOT prove that. Treat any single green from a fast mutate-then-run as unproven — including in the next round's own re-probes. (b) The write-clock rule leans on uuidv7 tie-breaking in ONE place: expiry rows materialized by a fold and a debit stamped with that same fold's `asOf` share a `created_at` to the microsecond, and `foldLedger` orders equal timestamps by `id`. The expiry rows are inserted first and therefore carry smaller uuidv7s — true today, in one process; I reasoned about it and the MIXED Docker case exercises it, but no test asserts the tie-break itself, and a second app instance with a skewed clock is the shape that would break it. (c) I argued, and did not test, that non-allocating writers still need no workspace advisory lock: a lot stamped early can only make a debit look better funded than it was validated for, never worse, so it cannot over-consume. That argument is why `grantCredits` still takes no lock, and if it has a hole the symptom would look exactly like this round's BLOCK. (d) The `stripe:setup` tier warning fires against a mocked Stripe client only; no live price has ever produced it. (e) Unchanged and now EIGHT rounds old: no fixture is a captured live test-mode payload, and the Phase-4 owner `stripe listen` run remains the only honest end-to-end check.
- 2026-08-17 — phase 3 reviewer gate round 11: **both PASS — phase 3 is Ready.** respin-billing-reviewer **PASS (Ready, A)** — 0 BLOCK, 0 CHANGE, 6 NOTE; respin-tenancy-reviewer **PASS (Ready, A−)** — 0 BLOCK, 0 CHANGE, 3 NOTE. First round in eleven where neither gate could reproduce a defect. Neither took round 10's pins on trust. Billing built an in-memory mutation harness (a vite `transform` hook, so the repo was never modified — deliberately sidestepping the mtime-keyed cache that made round 10's own harness report a real pin vacuous): mutating `createdAt: view.asOf` out of `ledger.ts` reproduced the production error `LedgerIntegrityError: row … over-consumes`; mutating the expiry stamp out of `balance.ts` failed the same pin naming 2 stale writers; five mutations each killed exactly their own pin and nothing else, and three planted violations (a new mint file, a new dispatch `case`, a bare `new Error` reachable from the facade) were all caught. **No vacuous pin found.** Tenancy re-implemented the facade AST walk itself and ran three plants proving the report fires on REACHABILITY, not mere existence (declared-but-never-thrown → not reported; thrown in `deriveBalanceInTx` → reported; thrown in an unreachable `debitCredits` → not reported), ran the keyless CLI itself (typed remedy naming BOTH missing vars, no stack frame, exit 1), and confirmed live that the orphan `respin_test` database is intact rather than dropped.
- 2026-08-17 — round-11 CORRECTION to the round-8/10 record, found by the billing gate and worth keeping: round 10's disclosure (c) — "an early-stamped lot can only make a debit look BETTER funded, never worse" — is true for over-consumption and **false for the materialization-drift invariant**. An injected probe produced `LedgerIntegrityError: expiry row … expires 30 but lot … has 50 remaining — materialization drifted`, permanent on an append-only table. The conclusion (lot writers need no advisory lock) still stands, but for a reason the comment did not give: the drift requires a granting transaction held open across a lot's expiry instant with a debit and a later fold inside that window, and `handleStripeEvent` performs no network I/O, so the window is milliseconds. Recorded because an argument that survives for the wrong reason is one refactor away from being wrong.
- 2026-08-17 — phase 3 entry gate, verified independently of the fix rounds (main session, not a subagent's self-report): typecheck exit 0; lint exit 0 (0 errors, 0 warnings); db:check clean; keyless build exit 0 with no `STRIPE_*` env; CI shape (`TEST_DATABASE_URL` set, BOTH Docker suites in one parallel run) **296 passed / 20 files**, three consecutive runs; credits race suite standalone **5/5 green at 9 tests** (the suite that twice hid a real defect behind an intermittent symptom — 2-of-5 red before the round-8 clock diagnosis). All six `.insert(creditLedger)` sites read by hand and confirmed caller-stamped.
- 2026-08-17 — phase 3 open NOTEs carried into phase 4 (none blocking, 9 total across both gates). Cheap and Phase-4-relevant, to be folded into the phase-4 build rather than deferred: `fold.ts:145-148` comparator tie-break (order lots before allocating rows at equal `createdAt`, removing a cross-process uuidv7 assumption — three lines, closes a narrow permanent-poisoning class); `pause.ts:104-119` `resumesAt` written before the `knownAt` bound (display-only, Phase-4-cheap); `customers.ts:23-30` `CustomerMappingLostError` embeds workspace UUID + Stripe customer id in a message Phase 4 will RENDER (ids belong in the log line, remedy in the page). Deferred with reasons: `billing-schema.ts:49-50` default stays `now()` (source scan is the control); `ledger.ts:1-2` import-fence claim not enforced for `packages/**` (6 production insert sites, all in `packages/credits/src` — verified repo-wide); `balance.ts:80-83` historical-`asOf` seam for M3; `db.test.ts:257,465` hand-written enumeration (M2 `brain_docs` is the table it would miss). Unchanged and now nine rounds old: no fixture is a captured live payload — the Phase-4 owner `stripe listen` run remains the only honest end-to-end check.
- 2026-08-17 — phase 4 started
- 2026-08-17 — phase 4 built. UI: `/usage` (derived balance from `deriveBalance` — the page never sums deltas itself; ledger table with kind/delta/expiry/ref/date; honest empty states that name their receiver — burn-by-mode → M3, days-to-empty → "not enough data", invoice list → the Customer Portal with an M2+/M6 slot comment; top-up prompt at zero; frozen notice with the resume date, or an explicit "no resume date has been recorded yet" rather than a fabricated one). `/settings/billing` (tier+state from `getWorkspaceBillingState`; **subscribe buttons render only where `hasLiveStripeSubscription` is false** — the UI face of `AlreadySubscribedError`, and the FOURTH reader of that one predicate rather than a fifth notion of "live"; pack purchase priced from config; auto-top-up opt-in + cap in dollars→cents; pause with bounds read from config `pauseMonths`; resume; and the cancel path that offers pause FIRST). `/admin/config` (append-version editor with field-level Zod issues and the operator's draft preserved, version history with author, fail-closed "no active config" + seed remedy). Accessors: `withWorkspace` gained `subscription()` and `ledger(page)` with breach validators in the same change, a clamped page size, and an ARGS map typed against the accessor signatures. Server actions: six thin wrappers (gate → scope → packaged op → redirect); refusals travel back as a short CODE and the page owns the words (`app/(product)/billing-errors.ts`), because several package messages carry ledger uuids and ISO instants and because a message in a URL is text an attacker could put in front of a creator.
- 2026-08-17 — phase 4 PRICE HONESTY decision, stated because it is a visible product choice: the billing page shows each plan's **monthly credit allowance from config** and sends the reader to Stripe for the AMOUNT. Config carries `pack.priceUsd` but no tier prices — those live in Stripe, where the setup script creates them and where a price amount is immutable — so printing R-7's indicative $10/$60/$200 would be a second price authority that can silently disagree with the card charge (non-negotiable 6). The pack button DOES print its price, because config is the pack's authority and `stripe:setup` refuses on divergence.
- 2026-08-17 — phase 4 carried Phase-3 NOTEs closed, all three in Phase-2/3 files — DECLARED LOUDLY, the way rounds 7 and 8 declared theirs: (a) `fold.ts` — equal-`created_at` replay is now ranked lots → expiry → allocating rows before falling back to `id`, removing the cross-process uuidv7 assumption. The tie is REAL, not hypothetical: `deriveBalanceInTx` stamps materialized expiry rows and the debit it is validating with the same `asOf` by construction. Pinned by three PURE fold cases with hand-written ids that make uuidv7 order disagree with causality (a debit id sorting before its funding lot reproduces `over-consumes`; a debit before its own expiry row reproduces `materialization drifted`), plus a case asserting `id` still totally orders within one rank. (b) `pause.ts` — the `knownAt` staleness bound MOVED ABOVE the already-open early return, so a delayed pause-era snapshot can no longer rewrite the CURRENT pause's `resumesAt` with an older pause's date; pinned by a two-pause sequence (June must survive a March-era snapshot) plus a non-vacuity twin proving a contemporaneous snapshot still reconciles. (c) `customers.ts` — `CustomerMappingLostError` takes no arguments now: the workspace UUID and the orphaned Stripe customer id moved to the `console.warn` at the throw site and the message carries only the remedy, because Phase 4 is what RENDERS it. The other thirteen app-facade error classes were audited for the same shape; the two that also embed identifiers (`LedgerIntegrityError` row/lot uuids, `ClockSkewError` two ISO instants) are handled at the render layer instead — a test constructs both with ids in their messages and asserts the rendered copy contains no uuid and no timestamp.
- 2026-08-17 — phase 4 AC-1 mechanism, strengthened past what the walk alone proves: `PROTECTED_PREFIXES` gained `/usage` and `/settings`, and `tests/gate-completeness.test.ts` gained a NAMED fixture of all five protected pages with the specific gate each must carry (`requireUser` vs `requireAdmin`, imported BY NAME from `@respin/auth` and actually CALLED), plus an EXHAUSTIVENESS assertion that the derived on-disk set equals the named set. Probe: an ungated `page.tsx` planted under `app/(product)/settings/` turned BOTH the walk and the exhaustiveness test red; removing it returned 10/10. Also asserted rather than commented: `middleware.ts`'s static matcher literal is parsed and checked to cover every `PROTECTED_PREFIX` (a missing entry is not a security hole — the server-layer gate is the gate — but it silently costs the optimistic redirect).
- 2026-08-17 — phase 4 test evidence. `tests/billing-ui.test.tsx` (NEW, 37 cases): every Edge-Case bullet and every Failure-Mode row of the phase plan has a named test — zero balance → prompt (+ non-vacuity twin); paused → frozen notice with date, and the no-date branch asserting nothing was fabricated; paused settings shows resume; a paused workspace's history still renders; free workspace → subscribe options and NO portal control; already-subscribed → portal, no subscribe button; empty `stripePriceMap` → disabled with `pnpm stripe:setup` + `/admin/config` + `stripePriceMap` named (+ non-vacuity twin proving they are live forms when mapped); non-owner → no live form action anywhere (+ owner twin); admin config with no versions → "no active config" + `pnpm db:seed` and a still-usable textarea; invalid config → field-level issues + "No version was appended" + the draft preserved; inline typed action error; an unrecognised `?e=` degrading to generic copy and NOT echoing the URL text; config unavailable → controls disabled, no allowance number printed; balance derivation throwing → honest message and NO balance element. AC-3 order: four cases, including "the ordinary page carries no data-cancel=final at all" and the DOM-order assertion with all three markers proven present first. Completeness: every Error subclass exported by the two app-facing facades (plus `WorkspaceAccessError`) must have copy, with a stale-claim assertion in the reverse direction. Package suites: +3 fold replay-rank cases, +2 pause staleness cases, +4 config cases (AC-4 count+1 and byte-identical old rows; rejected content appends nothing; field paths; history metadata + clamp), +3 accessor cases (both-directions breach loop, page clamp, paging order).
- 2026-08-17 — phase 4 MUTATION PROBES (a green suite on the first run is exactly when this ledger has been wrong before). (1) Deleting the `data-cancel` marker expression from `billing-view.tsx` → exactly one pin red (the DOM-order assertion), nothing else. (2) A NAIVE mutation that only moved the marker attribute onto a different heading → suite stayed green, which is correct and is recorded because it is what an insufficient mutation looks like. (3) The REAL inversion — physically relocating the cancellation block above the pause offer inside the interstitial → exactly two pins red (the order assertion and its blocked-non-owner twin), nothing else. File restored and verified by md5 after each probe.
- 2026-08-17 — phase 4 entry gate: `tsc --noEmit` exit 0 (root) + all four packages exit 0; `eslint .` exit 0 (0 errors, 0 warnings); `db:check` "Everything's fine"; keyless `next build` exit 0 with `env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET` (and `.env.local` verified to contain NEITHER variable, so the claim is not resting on an unset shell alone) — `/usage`, `/settings/billing`, `/admin/config` all compile as dynamic (f) routes; keyless `vitest run` **343 passed + 10 loud-skips**; CI shape (`TEST_DATABASE_URL` set, BOTH Docker suites in one parallel run) **353 passed / 21 files, three consecutive green runs** (base was 296/20); credits race suite standalone **5/5 green at 9 tests**. Repo-level: the three UGC contract schemas still parse. NOT re-run: the .NET / Python / Frontend entry-gate commands — nothing outside `respin/` changed except `CLAUDE.md`'s Commands block and two `docs/initial/tech-spec.md` sentences, neither of which any of those suites reads; that is a reasoned skip, not a pass.
- 2026-08-17 — phase 4 AC-5 evidence (render snapshot, NOT a screenshot — no dev server was driven, and no screenshot is claimed). Keyless `/usage` (zero balance, no Stripe customer) renders: "0 credits" / "Derived from your credit ledger as of 2026-08-17." / "You have no credits. Buy a credit pack or start a plan on billing settings to keep generating." / "Nothing has been spent yet: generation arrives in a later milestone (M3)..." / "Not enough data. This needs a spending history to measure..." / "No credit activity yet. Rows appear here when a subscription grants your monthly credits..." / a DISABLED "Open the Customer Portal" with "There is no billing account for this workspace yet — one is created the first time you subscribe or buy a credit pack." Keyless `/settings/billing` (free, empty price map) renders the banner "Billing is not configured on this server" with "No Stripe key is set on this server, so no billing action can run. An operator needs to put STRIPE_SECRET_KEY in respin/.env.local (see respin/env.example), run `pnpm stripe:setup`, and paste the printed price ids into /admin/config as `stripePriceMap`." repeated beneath each DISABLED Subscribe button and the DISABLED "Buy 1000 credits for $10", with "250 / 2000 / 8000 credits per month (from config v2). The price is shown on Stripe's checkout page — it lives in Stripe, and this page will not print a number it cannot verify."
- 2026-08-17 — phase 4 ENGINEERING vs EVIDENCE (AC-7). Two separate claims; no evidence row is discharged by a fixture. **ENGINEERING (done)** — build-plan M1 accept-when, integration-test half: subscribe → grant = `stripe.test.ts` "accept-when: subscribe → grant > subscription.created mirrors state; invoice.paid (subscription_create) grants the tier allowance expiring the SERVICE period end + 1 month"; cancel → downgrade = "accept-when: cancel → downgrade & payment-failed → grace → downgrade > subscription.deleted → effective free"; payment-failed → grace → downgrade = same describe, "payment_failed → grace for config graceDays; past deadline → free; a second failure never EXTENDS; recovery invoice clears grace"; pack purchase = "accept-when: pack purchase > checkout.session.completed (payment + pack metadata) → pack row with amountCents and 12-month expiry"; double-delivered webhook (no double grant) = "accept-when: double-delivered webhook > the same event id delivered twice grants once" plus the DERIVED "EVERY handled event type is double-delivery safe"; debit refused at zero balance = `ledger.test.ts` "bullet: debit larger than balance refused, NO row written; debit exactly equal succeeds"; pause freezes / resume shifts expiry = `balance.test.ts` "pause-aware effective expiry (D-M1-3) > bullet: an OPEN pause freezes the clock — the lot cannot expire and is NOT materialized" plus the six pause-lifetime table cases and `ledger.test.ts` "pause ops: start/end write the period + mirror". Plus this phase: usage/settings/admin UI = `tests/billing-ui.test.tsx` (37); gate completeness = `tests/gate-completeness.test.ts` (10); accessors = `with-workspace.test.ts` (10). **EVIDENCE (ALL PENDING, owner-credential-bound)**: (E1) subscribe at the Creator price and see 250 credits with the grant expiring one month past the invoice's SERVICE period end → README "Recording M1 evidence" step 1; (E2) the second-subscribe attempt showing portal-only → step 2; (E3) buy a pack → +1,000 at 12 months, balance 1,250 → step 3; (E4) `stripe events resend` changing nothing → step 4; (E5) pause freezes credits → step 5; (E6) resume shifts expiry, with the before/after dates recorded → step 6; (E7) cancel through the pause-first interstitial → step 7; (E8) payment-failed → grace → downgrade with a live dunning card → step 8; (E9) debit refused at zero balance — **not runnable at M1 at all**, the only debit caller is M3's generation pipeline, and step 9 says so rather than claiming it. Nine rounds of this ledger have named the same gap: **no fixture in this repo is a captured live test-mode payload**, and the owner's `stripe listen` run is the only thing that can close E1–E8.
- 2026-08-17 — phase 4 NOT DONE, and why. (a) The live Stripe run above — owner-credential-bound, and the largest outstanding risk in M1. (b) No reviewer gate has seen this phase: six of eight rounds in this milestone shipped a defect inside their own fix, and the honest hand-over is that this phase's diff has been read adversarially by its author and by nobody else. (c) The Customer Portal can cancel a subscription and our portal link does not force the pause offer through Stripe — REQ-G08's rule is enforced on OUR cancel path (the only in-app control that reaches cancellation sits below the pause offer, asserted); disabling cancellation in the portal is a Stripe dashboard configuration, named here rather than silently assumed. (d) No screenshot was taken and none is claimed; AC-5 is discharged by the render snapshot above. (e) Paging beyond the newest 50 ledger rows is not built — the page says so instead of implying completeness. (f) `stripe_events` is not surfaced anywhere in the UI; the owner reads it in psql during the evidence run.
- 2026-08-17 — least-confident (phase 4): (a) THE TESTS DRIVE THE VIEWS, NOT THE PAGES. `page.tsx` in all three routes — the gate, the scoping, the try/catch that turns a thrown `ConfigUnavailableError` into the degraded render — is executed by NO test in this repo, because it needs a session and a database. I reasoned about those branches and typechecked them; I did not run them. If `getWorkspaceBillingState` throws something my catch does not expect, or `searchParams` arrives in a shape Next 15 changed, the first person to find out is the owner on the runbook. This is the single line the gates should start on. (b) The `useActionState` config editor is rendered in tests only through its PURE half (`ConfigEditorForm`); the client wrapper's hook was proven to render under `renderToStaticMarkup` by a throwaway probe, not by a suite, and no test submits the form. (c) `redirect()` inside a `catch` is load-bearing in all six actions — it works because Next signals navigation by throwing and my catches wrap only the operation, never the redirect. That is a property I read out of the control flow, not one a test asserts. (d) The fold replay-rank change touches the single most defect-prone function in this milestone. Its three new pins are pure and discriminating, but the change alters replay order for EVERY history with an equal-microsecond tie, and the Docker races (5/5 x5, 353 x3) are the only evidence that no existing behaviour moved. (e) The pause `knownAt` reordering means a workspace whose LAST pause ended more than 60s after a snapshot's `knownAt` now refuses that snapshot's pause-open as well as its resumes-at write; I believe that is strictly more correct, and the non-vacuity twin covers the ordinary path, but it is a widening of a refusal on a path the owner's run will exercise for the first time. (f) `BETTER_AUTH_URL` is now load-bearing for checkout return URLs; it is documented in env.example and refuses loudly when unset, but no test spawns an action to see that refusal rendered.
- 2026-08-17 — phase 4 ADVERSARIAL RE-READ of my own diff, before reporting done (build-plan working agreement). Four defects found by reading, all in code written this session, all fixed with the fix asserted where it could be: (1) **PROTOTYPE-KEY OPEN REDIRECT** — `returnPathOf` looked the `from` form field up in a plain object (`RETURN_PATHS[raw] ?? BILLING_PATH`). `from=constructor` or `from=__proto__` answers from `Object.prototype`, so `??` never fires and `redirect()` would have received a FUNCTION. Replaced with a `Set` + `.has()`. Reachable by a crafted POST to any of the six actions. (2) **THE CANCEL MARKER RODE ONLY THE LIVE BRANCH** — `data-cancel="final"` was rendered on the submit form but not on the disabled-with-reason branch, so a cancellation control that happened to be blocked (non-owner, no Stripe key) carried no marker and the "no control on the ordinary page reaches cancellation" assertion would have passed straight over it. The marker now rides both branches. This is the AC-3 guard having a hole in exactly the direction the AC exists to close. (3) **A DEAD ALLOWLIST ENTRY** — `RETURN_PATHS` admitted `/usage`, but no form supplied `from=/usage`, so the branch was present-and-unrun; and had it been supplied, `/usage` did not render `?e=` at all, so a refused portal action would have looked like a button that does nothing. Both halves fixed (hidden field + error block on the usage page) and both pinned by tests. (4) A `useActionState` cast in `config-view.tsx` widened the action type to `string` for no live reason — removed, so the compiler checks the real shape.
- 2026-08-17 — CORRECTION to the phase-4 entry-gate entry above: the counts recorded there (343 keyless / **353** CI-shape) were taken BEFORE the adversarial re-read added its two pins. The final numbers, re-run after every fix, are **345 passed + 10 loud-skips keyless** and **355 passed / 21 files in the CI shape, three consecutive green runs**; credits race suite standalone **5/5 at 9 tests** (re-run after the fixes, not carried over); `tsc --noEmit` exit 0, `eslint .` exit 0, `db:check` clean, keyless `next build` exit 0. Recorded as a correction rather than silently edited, because this ledger is append-only and a number nobody re-checks is how "present-and-unrun" gets written down as proof.
- 2026-08-17 — phase 4 TOOLING DEVIATION, declared because it is a config change the plan did not ask for: the phase plan named `respin/tests/billing-ui.test.ts`, and the file shipped is `.tsx`. Rendering the real components (rather than asserting on hand-built strings) needs JSX in the test, so `vitest.config.ts` gained `esbuild: { jsx: "automatic" }` and its `include` widened from `tests/**/*.test.ts` to `tests/**/*.test.{ts,tsx}`. The package globs are untouched and every pre-existing suite still runs (21 files, up from 20 by exactly the new one). The alternative — `React.createElement` in a `.ts` file — would have made the DOM-order assertions unreadable, which is the one assertion in this phase that most needs to be obvious. No new dependency: `react-dom/server` is already a direct dependency, and esbuild ships inside vitest.
- 2026-08-17 — phase 4 reviewer gate round 1 (the FIRST gate this phase has seen): respin-billing-reviewer **NEEDS CHANGES (Almost, B)** — 0 BLOCK, 6 CHANGE + 6 NOTE; respin-tenancy-reviewer **NEEDS CHANGES (Almost, B)** — 0 BLOCK, 4 CHANGE + 3 NOTE; two findings reported independently by both. What both gates verified as REAL rather than taking on trust: the AC-3 cancel-order guard (billing rendered `BillingView` across all **1,536** prop combinations and found `data-cancel="final"` in exactly the 768 `showCancel` renders, 0 violations), the prototype-key open-redirect fix (class hunted repo-wide, no second instance), and the fold comparator change (both gates checked the regression direction the disclosure did not name).
- 2026-08-17 — phase 4 round-2 fix round. **THE HEADLINE FINDING, and it falsified one of my own disclosures:** phase 4's least-confident (c) said "`redirect()` inside a `catch` is load-bearing … it works because my catches wrap only the operation, never the redirect". False as written, and nothing asserted it. Probed against the INSTALLED `next@15.5.23`: `redirect()` throws a plain `Error` with `digest="NEXT_REDIRECT;replace;/sign-in;307;"` and `notFound()` throws `digest="NEXT_HTTP_ERROR_FALLBACK;404"` — neither is a subclass of anything, so a bare `catch (err)` swallows both. `sessionScope()` called `requireUser()` INSIDE the try in all six billing actions, and `appendConfigAction` called `requireAdmin()` inside its try: an expired session on an open billing form was logged `[billing-action] refused (unknown)` and sent to `/settings/billing?e=unknown` instead of `/sign-in`, and a non-admin POST to the config action was told "The configuration could not be saved and no version was appended" — an internal failure that never happened, on the page whose subject is provenance. No authorization was bypassed (both helpers throw before any operation) but the reported reason was wrong. Fixed as a CLASS, two mechanisms: (1) all seven gates hoisted ABOVE the try; (2) every `catch` in `app/**` and `lib/**` re-throws Next's control flow as its FIRST statement, through `lib/next-control-flow.ts`, which delegates to Next's OWN `unstable_rethrow` rather than a hand-maintained digest allowlist (one definition, and it stays right when Next adds a signal — `forbidden()`/`unauthorized()` already exist). Pinned by `tests/action-gate.test.ts`: the digest premise verified against the installed SDK, the primitive in both directions (a real domain error passes through untouched), all six billing actions plus the config action asserted to PROPAGATE their gate's signal, and a SOURCE SCAN over all 14 catch sites in app/ and lib/ — proven non-vacuous by three planted shapes including `catch {` with no binding.
- 2026-08-17 — round-2 fixes, the rest. **CHANGE 2/3 (AC-1 was one filename short AND default-OPEN):** the walk now includes `actions.ts` (a `"use server"` module is a POST endpoint with a stable action id, invocable without its page ever loading — both of this milestone's action files were invisible to the walk, the exhaustiveness assertion, the named fixture and the default-deny lint at once), both are named in `NAMED_PROTECTED_PAGES` with their gate, and a NEW assertion closes the other direction: every `page.tsx`/`route.ts`/`actions.ts` under `app/(product)/**` or `app/(admin)/**` must map to a URL `isProtectedPath` covers. The tenancy gate's own probe — an ungated `app/(product)/zzbrain/page.tsx` at a URL nobody added to `PROTECTED_PREFIXES`, previously 10/10 GREEN — now turns it red; re-run here against the real tree, red, then removed. **CHANGE 4 (the clamp was false for NaN):** `Math.min(Math.max(1, NaN), 200)` is `NaN`, drizzle drops a NaN LIMIT, and `Number(searchParams.rows)` on absent or garbage input IS NaN — so the accessor served the whole table while its doc-comment said the limit "is CLAMPED rather than trusted". `Number.isFinite` guards on both `limit` and `offset`, in ONE helper; every other `Number(` over untrusted input audited (pause months to `PauseLengthError`, auto-top-up cap to `AutoTopupCapError`, `?saved=` below; the auto-topup SQL aggregates are not URL-derived). **CHANGE 5 (the cage did not deny the Stripe SDK):** `stripe` is a direct dependency of the app package, so `new Stripe(process.env.STRIPE_SECRET_KEY!)` in a server action lint-passed, bypassed the adapter's pinned API version and the keyless refusal, read the secret at the page layer, and was invisible to the AC-9 scan (which walks `packages/credits/src` only). Now denied by name AND by deep path through the SAME allowlist builder, re-admitted ONLY for `app/api/stripe/webhook/**` (the static `constructEvent` check needs no key), with 4 import shapes × 7 app-side locations as deny fixtures, an allow fixture, an internals-still-denied fixture, a `new Stripe(` source scan over app/ and lib/, and a dynamic-`import("stripe")` scan proven against a planted probe. **CHANGE 6 (a billing mutation offered to non-owners):** `/usage` computed `portal.available = Boolean(subscription)` with no role test while the billing page blocked the same action — nothing was bypassed (`createPortalUrl` calls `assertOwner`) but the plan's REQ-A02 row promises BOTH halves asserted. The decision is now the pure `portalAvailability({isOwner, hasStripeCustomer})` in `usage/copy.ts` — extracted deliberately, because a mutation probe proved an inline ternary in `page.tsx` is asserted by NOTHING (no test in this repo executes a page component) — with all four combinations pinned, role before state, plus the owner twin and an end-to-end render.
- 2026-08-17 — CORRECTION to the phase-4 AC-5 entry above (the "render snapshot" entry), found by the billing gate. That entry quoted `(see respin/env.example)` and "as `stripePriceMap`" as part of a keyless `/settings/billing` render. **Those strings existed only in `page.tsx`'s `STRIPE_REMEDY`, which no test executed**; the suite's fixture used a different, shorter remedy, and the gate reproduced the real fixture render: both quoted strings false, while "250 credits per month (from config v2)" was true — "config v2" being the FIXTURE's `CONFIG_OK.version`, not what a seeded install (v1) prints. So the recorded output was page-source copy spliced onto fixture numbers, and the live consequence was that **the operator-facing remedy the keyless page actually shows was asserted by nothing** — `pnpm stripe:setup` could have been deleted from it with the suite green. Corrected by appending, never by editing the original. What the render actually shows is now BOUND: `STRIPE_REMEDY` moved to `app/(product)/settings/billing/copy.ts` (a sibling module, not a `page.tsx` export — Next validates page-segment exports), the suite imports it and asserts all six phrases the ledger quoted, and deleting `pnpm stripe:setup` from it turns 2 tests red (probed). The same was done for `/usage`'s two portal reasons and `/admin/config`'s no-active-config remedy. **The honest restatement of that AC-5 entry: it is a FIXTURE RENDER of the pure view components, not a page render — no dev server was driven, and the config version in it is the fixture's, not a seeded install's.** The keyless PAGE render remains executed by no test and is the owner's runbook step.
- 2026-08-17 — CORRECTION to the phase-4 "NOT DONE" entry above, item (c). It said "the only in-app control that reaches cancellation sits below the pause offer, asserted". **Not accurate.** A live subscriber's ordinary `/settings/billing` renders "Manage plan and payment method in the Customer Portal" ABOVE the pause section, and `/usage` renders a third portal button; the Customer Portal cancels subscriptions. The AC-3 guard keys on the `data-cancel="final"` marker, which none of those carry, so it passes over them by construction — the guard is correct about what it asserts, and the sentence claimed more than it did. The accurate statement: **REQ-G08's pause-first rule is enforced on Respin's own cancel FLOW (the interstitial), and cancellation is also reachable through Stripe's portal by routes that carry no pause offer, including Stripe's own emails.** Resolved by decision rather than by pretending otherwise: **R-22** records that portal cancellation stays ENABLED, with its reasons (disabling it breaks the documented evidence step, leaves a paying subscriber with no self-serve exit, and would not close the hole anyway since Stripe emails link straight to the portal), verified against the installed SDK that `BillingPortal.Configurations` carries `subscription_cancel` and **no `subscription_pause` feature at all** — so the portal cannot be made to offer our pause even in principle. README Stripe setup gains step 4: LOOK at the portal's feature configuration and record what it says, with its success check. A dashboard setting no runbook named was the actual defect.
- 2026-08-17 — round-2 NOTEs, all six addressed. (1) `?saved=` — `Number("") === 0` is an integer, so a bare `?saved=` rendered "Saved as **v0**. Earlier versions are untouched" for a save that never happened and `?saved=999` fabricated a version, on the one page whose subject is append-only provenance (non-negotiable 6). Now `resolveSavedVersion(raw, activeVersion)`: string, integer, `> 0`, and **equal to the ACTIVE version** — a save appends, and the row it appends becomes active, so an older-but-real number is still not evidence of THIS save. Pure and pinned in both directions. (2) `withWorkspace` sat outside every try, so the M2 multi-workspace refusal reached Next's default error page and `billing-errors.ts`'s `workspace_access` copy — written for exactly this — was **unreachable on the page path**. Both product pages now catch it and render `AccessRefusal`, pinned by a render test asserting the copy appears and the package message's identifiers do not. (3) **the pause staleness bound: FIXED, not deferred** — its own entry below. (4) `resumeSubscription` converged the pause PERIOD but not the MIRROR, and `state.ts` reads the mirror: with `pausedAt` set and no open period, `ensurePauseEnded` returned false and the action redirected with no error — the Resume button appeared to do nothing while the page still said "Paused", with no event coming to correct it. `clearPauseMirror` (in `pause.ts`, the only module allowed to write `subscriptions.pausedAt`, and deliberately NOT called from `ensurePauseEnded`, whose `false` also means "this snapshot is too stale to be believed") converges it; pinned at unit level AND at the action level with the Stripe un-pause call asserted, plus the direction that must not change (an OPEN pause is never cleared). (5) `pauseMonths` gained a `min <= max` Zod refinement — an inverted pair made `Array.from({length: max-min+1})` produce `[]`, so the pause `<select>` rendered zero options with a `defaultValue` no option carried; `monthlyPeriodDays` got the same refinement as the same class, and equal ends stay legal (non-vacuity). (6) the three new pages were reachable only by typed URL: the product header gained Studio/Usage/Billing links and `/admin` a "Runtime configuration" link (plain anchors, existing inline style, no framework), and the runbook wording now says how to get there.
- 2026-08-17 — round-2 NOTE 3 worked through properly, and FIXED, with a migration this phase's plan did not anticipate — declared loudly, as rounds 7 and 8 declared theirs. The bound in `ensurePauseStarted` compared a caller's `knownAt` (a webhook's `event.created`) against `pause_periods.ended_at`, which is our **processing** time. Two different clocks. So: Stripe resumes at T0, the resume event is processed at T0+5min after backoff, `ended_at = T0+5min`; the owner re-pauses from the portal — or an operator pauses from the Stripe DASHBOARD, which needs no portal setting at all — at T0+2min, and the bound sees 3 minutes of "staleness" and refuses. **No `pause_periods` row is written, expiry clocks keep running through a pause Stripe has applied, and no further event is coming.** Permanent, on the money path, which is why it was not left as a deferral. I looked for a no-schema fix and did not find one: the knowledge time of the close is simply not derivable from what we store, and every alternative (stamping `mirrorEventAt` from a partial write; writing a retroactive `ended_at` past `assertWriteClock`) reintroduces a defect an earlier round closed. So `pause_periods` gains a nullable `ended_known_at` (migration **0007_mighty_prima**, applied to fresh PGlite by the suite and to the live Docker dev DB — `\d pause_periods` shows the column, `__drizzle_migrations` = 8 rows, `db:check` clean), `recordPauseEnd`/`ensurePauseEnded` record `knownAt ?? at`, and the bound reads `ended_known_at ?? ended_at` (the fallback keeps the old conservative behaviour for pre-migration rows). Knowledge time is compared with knowledge time now. Three pins: the late-resume case OPENS the pause (red against the reverted read — probed), the owner-resume case still REFUSES a genuinely stale snapshot, and `ensurePauseEnded` is asserted to store a `knownAt` that differs from its own write instant. The fold does not read the new column — effective expiry is `started_at`/`ended_at` only, stated rather than assumed.
- 2026-08-17 — round-2 evidence. `tsc --noEmit` exit 0 (root + all four packages); `eslint .` exit 0 (0 errors, 0 warnings); `db:check` "Everything's fine"; keyless `next build` exit 0 under `env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET` (all three M1 pages still dynamic routes); keyless `vitest run` **387 passed + 10 loud-skips**; CI shape (`TEST_DATABASE_URL` set, BOTH Docker suites in one parallel run) **397 passed / 22 files, three consecutive green runs** — up from the 355/21 base; credits race suite standalone **5/5 green at 9 tests**. New/changed suites: `tests/action-gate.test.ts` (7, NEW file — the gate class), gate-completeness 10 to 15, billing-ui 39 to 50, import-boundary 31 to 42, with-workspace 10 to 11, config 9 to 10, ledger 14 to 19, actions 24 to 25. **Every fix carries a pin proven discriminating by MUTATION** (revert the source, that pin goes red, the rest of its file stays green): un-hoisting one gate and dropping its rethrow gave 2 red (the behavioural pin naming the action, and the source scan); reverting the NaN guard gave "limit NaN must not serve the whole table: expected 10 to be less than 10"; deleting the `stripe` deny from the eslint builder gave 7 red; deleting `pnpm stripe:setup` from the page's remedy gave 2 red; removing the role test from `portalAvailability` gave 2 red; reverting `ended_known_at ?? ended_at` gave the late-resume pin red; planting `app/(product)/zzbrain/page.tsx` in the REAL tree turned the default-deny assertion red naming the file; planting an ungated `app/(product)/settings/zzprofile/actions.ts` gave 2 red (walk + exhaustiveness). Every probe file deleted and verified gone by enumerating `app/`, `lib/`, `tests/` and `packages/*/tests/`.
- 2026-08-17 — round-2 NOT FIXED, and why. Nothing was left from either gate's list — all 10 CHANGE and all 9 NOTE are addressed (the two duplicated findings once each). Deliberately NOT done: (a) **the live Stripe test-mode evidence run** — owner-credential-bound, unchanged, and still the largest outstanding risk in M1; (b) **no in-app cancellation API** — R-22 records the choice and its consequence rather than building one inside a fix round; (c) `AccessRefusal`'s WIRING (the `return <AccessRefusal …/>` inside each page's catch) is not executed by any test, for the same reason the whole page-component layer is not — the copy and the component are pinned, the two lines that call them are not; (d) the `stripe` cage's deny fixtures use ESLint's API against synthetic file paths, exactly like every existing cage fixture, so they prove the RULE, not that a real file at that path would be linted by the same config resolution (the CLAUDE.md 2026-08-02 lesson): `eslint .` over the real tree is the second mechanism and it is green.
- 2026-08-17 — least-confident (round 2): (a) **the page components are still executed by NO test**, and this round moved three more decisions out of them (`portalAvailability`, `resolveSavedVersion`, the copy constants) precisely because a mutation probe showed an inline ternary in `page.tsx` is asserted by nothing. What remains inside them — the gate call, the scope call, the try/catch wiring, `searchParams` shape — is proven only by typecheck and by reading. It is a smaller surface than last round and it is not zero. (b) The `catch`-must-rethrow source scan is a REGEX over comment-blanked source, not an AST walk: a `catch` inside a template literal or a regex literal could confuse it in either direction. It is proven non-vacuous against three planted shapes and it currently sees all 14 real sites, but an AST walk is what it should become when app/ grows. (c) `unstable_rethrow` is an `unstable_`-prefixed API. I chose it over a hand-maintained digest allowlist because Next's own predicate cannot drift from Next's own signals, and the premise test pins the two digests I verified — but the name says the contract may move, and a Next upgrade should re-run `tests/action-gate.test.ts` deliberately. (d) `ended_known_at` is written by exactly two call sites and read by one; I reasoned that the fold cannot see it (effective expiry reads `started_at`/`ended_at`) and grepped rather than assumed, but this is the pause/clock area where five of this milestone's rounds shipped a defect inside their own fix, and it is a schema change made in a fix round. (e) The `?saved=` cross-check suppresses a TRUE banner if a second admin appends a version between the redirect and the render — harmless (the save happened; the history shows it) but it is a deliberate false-negative. (f) Unchanged and now TEN rounds old: no fixture in this repo is a captured live test-mode payload, and the owner's `stripe listen` run remains the only honest end-to-end check.
- 2026-08-17 — phase 4 round-3 fix round. **THE META-FINDING, and both gates found it independently wearing different hats: the guards ENUMERATED NAMES where they had to DERIVE PROPERTIES.** Four instances, all live at once: `ENTRYPOINT_FILES = ["page.tsx","route.ts","actions.ts"]` (installed `next@15.5.23` defaults `pageExtensions` to `['tsx','ts','jsx','js']` — `node_modules/next/dist/server/config-shared.js:53-58`, read, not remembered — so three planted `page.ts` files were live dynamic routes with the suite 15/15 GREEN, one of them `/admin/zztools` with no `requireAdmin` in it at all); the same list missed a `"use server"` module renamed `mutations.ts`; `CODE_FILE = /\.tsx?$/` in the catch scan left `.js`/`.jsx` under `app/` unscanned; and the two suites' `SCAN_ROOTS` had drifted (`["app","lib"]` vs `["app","lib","middleware.ts"]`) so a swallowing catch in `middleware.ts` was scanned by NEITHER. Fixed as ONE class in a new shared `tests/support/app-surface.ts`: an entrypoint is a file Next serves **at a URL of its own** (basename `page`/`route`, or a metadata convention — `sitemap`/`robots`/`opengraph-image`/…, default-deny for the first one M2 adds) with an extension **derived from the installed Next's `pageExtensions`** (project override first, `defaultConfig` second), OR **any file whose FIRST directive is `"use server"`, by any name**. `SCAN_ROOTS` and the comment-blanking now have one definition and both readers. `layout`/`template`/`default`/`error`/`not-found`/`loading` are deliberately NOT entrypoints and the module says why — they have no URL of their own, they render only beneath a page that already passed its own gate, and `app/(product)/layout.tsx`'s URL is the group root `/`, so demanding a PROTECTED_PREFIX for it would be nonsense. A pin asserts the derived extension set against `["tsx","ts","jsx","js"]` so a Next upgrade gives deliberate notice (the walk widens automatically; the pin exists so someone re-reads the fixtures) and so a derivation that silently returned `undefined` cannot pass for "no entrypoints found".
- 2026-08-17 — round-3 CHANGE 1 (the real bug, migration **0008_uneven_oracle**): **the CLOSE side of the pause bound was still on the wrong clock.** 0007 fixed the OPEN side; `ensurePauseEnded` still compared the caller's knowledge time (`event.created`) against `pause_periods.started_at`, which is our PROCESSING time. Reproduced exactly as the gate did: a pause Stripe applied at T0 but processed at T0+5min, resumed in Stripe at T0+2min then returns `false`, the row stays OPEN, `effectiveExpiry` freezes every lot's clock indefinitely, `state.ts` says `paused`, M3's debit would refuse — while Stripe bills normally and **no further event is coming**. Fixed symmetrically: nullable `started_known_at` (same shape as 0007, same `?? started_at` pre-migration fallback), written by `recordPauseStart(…, knownAt)`, read by the bound. Migration applied to fresh PGlite by the suite AND to the live Docker dev DB (`\d pause_periods` shows both knowledge columns; `drizzle.__drizzle_migrations` = 9 rows; `db:check` clean). **The new column cannot reach the money path, and here is how that was checked rather than asserted:** (a) a repo-wide grep for `startedKnownAt|endedKnownAt|started_known_at|ended_known_at` across `packages/*/src` returns 4 code sites, all in `pause.ts`, plus the schema — `fold.ts`, `balance.ts`, `ledger.ts` and `state.ts` contain ZERO occurrences; (b) `effectiveExpiry` was read and takes only `startedAt`/`endedAt`; (c) the pure-fold fixtures in `balance.test.ts` now set BOTH knowledge columns to an absurd sentinel a millennium away, and a mutation making `effectiveExpiry` fold `startedKnownAt` turns **5** pause cases red — so the "the fold never reads them" claim is now a test, not a sentence. Migration 0008 also adds `pause_periods_ended_known` (`ended_known_at IS NULL OR ended_at IS NOT NULL`); `billing-schema.ts` records why the OBVIOUS check — ordering the two knowledge columns — would be **wrong**: `ensurePauseEnded` tolerates CLOCK_SKEW_MS, so a legitimate close can carry `ended_known_at` up to 60s BEFORE `started_known_at`, and that CHECK would refuse real resumes on the money path.
- 2026-08-17 — round-3 CHANGE 2 and 3, and the NOTEs. **CHANGE 2:** `action-gate.test.ts`'s "planted shapes" test re-spelled the regex inline instead of calling `findSwallowingCatches`, so the in-memory mutation that made it return `[]` unconditionally left 7/7 green. It now writes seven shapes into an `mkdtempSync` tree (bare catch, binding-less catch, comment-first catch, **`.js` and `.jsx`**, the compliant shape, and a catch that exists only inside a comment) and runs the REAL function over it, asserting the exact offender list. **CHANGE 3:** the AC-3 marker guard passed when neutered because both disabled-branch renders keyed on `data-testid="cancel-final"`; three new cases (non-owner, keyless server, no Stripe customer) assert the marker on the DISABLED branch and first prove they ARE the disabled branch (a `<div … data-testid="cancel-final">` wrapping a `<button type="button" disabled`), so the case cannot silently drift onto the live form. **NOTEs:** `lastPauseCloseKnownAt` now orders AND projects on one `coalesce(ended_known_at, ended_at)` expression, removing the "safe only because periods cannot overlap" reasoning step; the eslint Stripe/dispatcher grant narrowed from the `app/api/stripe/webhook/**` SUBTREE to `app/api/stripe/webhook/route.ts` EXACTLY, with deny fixtures at `helper.ts` and `lib/dispatch.ts`; `app/api/**` gained an explicit `SESSION_FREE_API_ROUTES` allowlist (Better Auth's catch-all, the signature-checked Stripe webhook, each with the mechanism that authenticates it instead) plus default-deny for everything else, and a staleness check that each allowlisted file exists and still carries no gate; `with-workspace.ts`'s header now names the THIRD mechanism (`@typescript-eslint/no-require-imports`, which was incidental, unnamed and asserted by nothing) with a fixture covering four `require()` shapes at four app-side paths. **R-23** appended correcting R-22's third reason (with `subscription_cancel` disabled the emailed portal shows no cancel control, so that route WOULD close — R-22 stands on reasons (a) and (b), and the honest statement is "we could close this and are choosing not to"); **R-24** appended giving `CLOCK_SKEW_MS` provenance, stating plainly that 60s is a guess doing three jobs and not a measurement, with the owner's `stripe listen` run as the tripwire that would split it.
- 2026-08-17 — round-3 evidence: **EVERY guard written or repaired this round was mutated and watched fail.** 9 mutations, each red on exactly its own pin(s): (1) `open.startedKnownAt ?? open.startedAt` reverted to `open.startedAt` gives 1 red, the late-pause pin; (2) `recordPauseStart` stops storing `startedKnownAt` gives 1 red; (3) `lastPauseCloseKnownAt` back to order-by-`ended_at`/return-coalesce gives 1 red; (4) migration 0008's CHECK deleted gives 1 red; (5) `effectiveExpiry` folds the knowledge column gives 5 red (the sentinel); (6) `findSwallowingCatches` returns `[]` unconditionally — **the exact mutation that used to be silent** — gives 1 red; (7) the eslint webhook grant widened back to the subtree gives 2 red; (8) `no-require-imports` disabled for `app/**` gives 1 red; (9) `data-cancel` removed from `ActionButton`'s disabled branch — **the mutation that used to leave 50/50 green** — gives 3 red. Plus the gates' own five probes planted in the REAL tree (`app/(product)/zzbrain2/page.ts`, `(product)/settings/zzsub/page.ts`, `(admin)/admin/zztools/page.ts`, `app/api/zzbrain/route.ts`, `(product)/settings/billing/mutations.ts`): 4 assertions red naming every file, then all five deleted and the tree re-walked clean. Two further mutations on the new page-wiring suite: dropping the role test at the page call site gives 1 red; replacing the `AccessRefusal` return with a re-throw gives 1 red.
- 2026-08-17 — round-3: **the page components are executed by a test now — for `/usage`.** Three rounds disclosed "the tests drive the VIEWS, not the PAGES" and each answered by moving ANOTHER decision out of `page.tsx`, which shrinks the untested surface without testing it. A Next async server component is an async function returning JSX, so `tests/page-wiring.test.tsx` awaits `UsagePage({searchParams})` and hands the result to `renderToStaticMarkup`, mocking the four package surfaces through `importOriginal` so every ERROR CLASS in the tree is the real one. 8 cases: the gate runs ABOVE everything and its `NEXT_REDIRECT` propagates with nothing scoped or read; the happy path scopes by the SESSION user id and clamps the ledger read at `limit: 51`; a real `WorkspaceAccessError` renders `AccessRefusal` with no uuid on the page; a failing balance derivation is CONTAINED (honest copy, no balance element, history still rendered, no uuid); a failing billing-state read does not take the balance down; a paused workspace gets its frozen notice; REQ-A02's role test is asserted AT THE CALL SITE (round 2 could only assert the pure helper); and `searchParams` is driven with a known code, a hostile string (not echoed) and an ARRAY shape. **NOT DONE and stated plainly: `/settings/billing` and `/admin/config` page components are still executed by no test.** The harness now exists and is ~40 lines per page, so this is a budget call in a fix round, not an impossibility — the honest report is that one of three pages is covered.
- 2026-08-17 — round-3 entry gate: `tsc --noEmit` exit 0 (root + all four packages); `eslint .` exit 0 (0 errors, 0 warnings); `db:check` "Everything's fine"; keyless `next build` exit 0 under `env -u STRIPE_SECRET_KEY -u STRIPE_WEBHOOK_SECRET` (all three M1 pages still dynamic (f) routes, no probe routes); keyless `vitest run` **414 passed + 10 loud-skips** (up from 387); CI shape (`TEST_DATABASE_URL` set, BOTH Docker suites in one parallel run) **424 passed / 23 files, three consecutive green runs** — base was **397/22**; credits race suite standalone **5/5 green at 9 tests**. New/changed suites: `tests/support/app-surface.ts` (NEW, not a test file — the shared derivation), `tests/page-wiring.test.tsx` (NEW, 8), gate-completeness 15 to 22, action-gate 7 to 8, import-boundary 42 to 44, billing-ui 50 to 53, ledger 19 to 24, db 34 to 35. Every probe file deleted and verified gone by walking `app/`, `lib/`, `tests/`, `packages/*/tests/` and the respin root; a repo-wide scan for `MUTATION:`/`MUTATED` in sources returns nothing.
- 2026-08-17 — **CORRECTION to the round-2 NOTE-3 entry above.** That entry ended "Knowledge time is compared with knowledge time now (round-2 NOTE 3)." **That was true of ONE SIDE only.** Migration 0007 put the OPEN-side bound (`ensurePauseStarted` vs the last close) on knowledge time; the CLOSE-side bound (`ensurePauseEnded` vs the open pause) was left comparing the caller's knowledge against `started_at`, our processing time — the same defect, mirrored, with a worse consequence (a pause that never closes rather than one that never opens). Round 3's billing gate reproduced it in PGlite through the real migrations. It is true of both sides as of migration **0008**. Recorded as a correction rather than an edit because the sentence read like a closed invariant and was used as one.
- 2026-08-17 — round-3 NOT FIXED, and why. (a) `/settings/billing` and `/admin/config` page components remain unexecuted by any test — see above; the `/usage` harness is the template and the cost is known. (b) The `catch`-must-rethrow scan is still a REGEX over comment-blanked source, not an AST walk — unchanged from round 2's disclosure, now proven non-vacuous against seven planted shapes in five extensions rather than three re-spelled strings. (c) The **live Stripe test-mode evidence run** — owner-credential-bound, eleven rounds old, and still the largest outstanding risk in M1. R-24 now hangs a measurement on it (observed `event.created` to processing lag). (d) The `stripe` cage's deny fixtures still use ESLint's API against synthetic paths rather than real files (CLAUDE.md 2026-08-02); `eslint .` over the real tree is the second mechanism and is green.
- 2026-08-17 — least-confident (round 3): (a) **`METADATA_ROUTE_BASENAMES` is default-deny for a class this tree does not exercise.** No `sitemap.ts`/`opengraph-image.tsx` exists, so the only proof that entry is live is a classification test over a planted file — if Next's metadata conventions include a filename I did not list, an M2 URL-addressable file would be invisible to the walk exactly as `page.ts` was. The list came from reading Next's documented conventions, not from the installed package's own source, which is a weaker derivation than `pageExtensions` got. (b) **Excluding `layout` from the entrypoint set is a judgement**, not the reviewer's literal wording. The argument (a layout has no URL of its own and renders only beneath a page that already passed its own gate) is written into the module and I believe it — but it is an argument, and if Next ever serves a layout independently it is wrong. (c) The `"use server"` detector is a regex over comment-blanked source; it correctly rejects this tree's two prose mentions, and a directive built at runtime or inside an odd literal would fool it. (d) `started_known_at` is written by two call sites and read by one; the fold-can't-see-it claim is now grep + a sentinel-backed mutation rather than reasoning, but this is the pause/clock area where six of this milestone's rounds shipped a defect inside their own fix, and it is a second schema change made in a fix round. (e) The `/usage` page harness mocks four module boundaries; it proves the WIRING, and it cannot prove that the real `respinDb.withWorkspace` returns the shape the page destructures — that is still typecheck plus the accessor suite, and the owner's run.
- 2026-08-17 — phase 4 round-4 fix round (main session, not a subagent). Round 3's gates returned billing NEEDS CHANGES (Almost, **B+**, 1 CHANGE / 6 NOTE — "the code is Ready and I could not reproduce a defect in it, including inside this round's own fixes") and tenancy NEEDS CHANGES (Almost, **B**, 2 CHANGE / 3 NOTE), with both gates explicitly separating must-fix-before-Ready from worth-doing-in-M2. All three must-fix items closed here.
  (1) **THE LOCATION HOLE** (tenancy CHANGE 1): the three guard walks each answered a question about a KNOWN PLACE — a gated route group, a protected URL prefix, `api/` — so an entrypoint at a new top-level URL outside every group was seen by none of them. The gate planted `app/zzbrain5/page.tsx`: suite 22/22 green, keyless `next build` served `ƒ /zzbrain5`. That is the M2 filename that matters (`app/brain/page.tsx` would be ungated, unscoped, and would not even inherit `(product)/layout.tsx`'s `requireUser`). Fixed by making the LOCATION derived the way the DEFINITION already was: `findUnclassifiedEntrypoints` walks ALL of `app/` and requires every entrypoint to be protected-and-gated, an allowlisted session-free API route, or on a new explicit `PUBLIC_ENTRYPOINTS` list (marketing landing, sign-in, sign-up — each with its reason and a staleness check that fails if the file stops being an entrypoint or becomes protected). Adding a page is now a deliberate act in one of three lists, never a silent default-open.
  (2) **THE METADATA VARIANT HOLE** (tenancy CHANGE 2): `METADATA_ROUTE_BASENAMES` was seven exact basenames — the SAME defect as the filename list `app-surface.ts` exists to kill, one level down. Next builds every image/sitemap matcher with `variantsMatcher = '\d?'`, so `opengraph-image2`/`icon2`/`apple-icon3` are metadata routes too; the gate planted `app/(product)/usage/opengraph-image2.tsx` and it classified as `"module"` while a keyless build served `/usage/opengraph-image2-1m8hwu` — a URL-addressable file under a PROTECTED prefix carrying no gate. Fixed by asking the installed Next itself via its exported `isMetadataRouteFile`, exactly as `PAGE_EXTENSIONS` already asks it for the extension set. The literal list survives only as the non-vacuity pin for that derivation. This was the round-3 fix round's own least-confident (a) — "the metadata list came from Next's *documented* conventions, not its installed source, a weaker derivation than `pageExtensions` got" — demonstrated rather than argued, which is exactly what that disclosure was for.
  (3) **R-24's TRIPWIRE HAD NO RUNBOOK STEP** (billing CHANGE): R-24 accepts `CLOCK_SKEW_MS = 60_000` only *conditionally* — record the observed `event.created` → processing lag during the owner's run, and split the constant if any legitimate event exceeds ~30s. That condition lived in the decision and two code comments; `grep "lag\|R-24\|CLOCK_SKEW" README.md` returned nothing. The one measurement that would correct a guessed tolerance now doing three jobs on the money path was in no step the owner would follow while `stripe listen` scrolled past — the same class as R-22, which *did* get its README step. Added as evidence step 4b with its success check ("a number is written down, not 'looked fine'").
  Also fixed, all three the stale-claim class: the eslint builder docstring and `STRIPE_SDK_DENY` still described the grant as the `app/api/stripe/**` subtree after round 3 narrowed it to `route.ts` exactly; and `usage/page.tsx` still claimed "no test in this repo executes a page component" after `tests/page-wiring.test.tsx` began executing that very page.
- 2026-08-17 — round-4 evidence, proven by the gates' OWN method rather than by reading. Both probes re-planted in the REAL tree: `app/zzbrain5/page.tsx` → the new ALL-of-app assertion goes **red naming the file**; `app/(product)/usage/opengraph-image2.tsx` → red **twice**, as an ungated protected entrypoint (AC-10) and by the exhaustiveness assertion. Both probes then deleted and the tree re-walked clean (`find app lib tests packages -name "zz*" -o -name "opengraph*"` → empty). Entry gate after the fixes: typecheck exit 0, lint exit 0, **428 passed / 23 files** in CI shape (base 424/23 — four new pins), keyless build exit 0 with exactly the 10 expected routes and no probe routes. NOT re-verified after round 4: the two reviewer gates have not seen these three fixes — they are small, surgical, and mutation-proven here, but that is this ledger's most-repeated caveat and it applies unchanged.
- 2026-08-17 — phase 4 open NOTEs, carried to M2 with both gates' agreement that they are not Ready-blockers: `CODE_EXTENSIONS` admits `.mts`/`.cts` (which Next does not resolve) and omits `.mjs`/`.cjs` (which it does) — an asymmetry, low likelihood in a TS project; `isUseServerModule` sees module-level directives only, so an inline `"use server"` as the first statement of a function body inside a non-entrypoint module is invisible (zero instances today); the REQ-A02 owner matrix is a hand-written list of six actions rather than derived from the facade's exports; `(product)/layout.tsx` calls its gate and performs a write (`ensureUserWorkspace`) but nothing asserts that it calls the gate; `scope.accessors.ledger()`/`subscription()` on the usage page sit outside the try while balance and billing-state are contained; and `/settings/billing` + `/admin/config` page components are still executed by no test (`/usage` has the harness — a declared budget call, ~40 lines per page). Unchanged and now twelve rounds old: no fixture is a captured live payload; the owner's `stripe listen` run remains the only honest end-to-end check, and it now also carries R-24's lag measurement.
- 2026-08-17 — phase 4 scoped CONFIRMATION gates on the round-4 fixes (not full re-reviews; both gates had already covered this phase three times). **respin-billing-reviewer: CHANGE RESOLVED, zero code findings, phase 4 B+, "M1's engineering half is complete and Ready."** It verified the R-24 provenance chain now closes (decisions.md:175 → clock.ts:11-28 → README.md:207), that `stripe_events.received_at` exists, is `defaultNow().notNull()`, and is written for EVERY authenticated event regardless of outcome, and that all four new pins are non-vacuous in both directions. It hunted specifically for a regression from the round-4 edits — a `packages/` import creeping into the guard suites, a fixture writing inside the repo, a pin passing by construction, a comment describing a narrower grant than the code — and found none. **respin-tenancy-reviewer: both round-3 CHANGEs RESOLVED**, with the location fix confirmed as a genuine class fix (it starts from ALL of `app/` and SUBTRACTS three named sets, so anything unnamed fails by default) and the metadata fix confirmed sound including the basename-only question, checked by running the installed matcher over 16 names across both path shapes.
- 2026-08-17 — TWO defects found in the round-4 fixes themselves — i.e. in MY OWN edits, which is this milestone's most-repeated shape and it did not stop for the main session either.
  (1) **The runbook step I wrote offered a source that measures the wrong interval** (billing CHANGE): step 4b named `stripe listen`'s leading per-line timestamp as a source for the event's `created`. It is the CLI's LOCAL RECEIPT time, so subtracting it measures the CLI→app hop in milliseconds and yields a number that **can never exceed the 30s threshold** — a success check that is performed but cannot fail, which is the same defect one layer along from the one step 4b was written to fix. It also offered "the app log line for that event", which does not exist for a successful event (`webhooks.ts:264` logs only when `outcome !== "processed"`). Fixed: the step now names exactly two sources that work — `stripe events retrieve <id>` for `created`, and `stripe_events.received_at` for the processing side, with the `docker compose exec … psql` one-liner to read it (executed verbatim here to confirm it runs: returns 0 rows today, correct — no live events yet) — and warns explicitly against the `stripe listen` route, noting `--print-json` as the CLI form that does carry `created`. R-24's second, operational trip condition (a pause or resume refused during the run) is now cross-referenced too.
  (2) **The `api/` branch my walk delegates to accepted a non-enforcing helper** (tenancy CHANGE): `GATE_IMPORT` includes `getSessionUser`, which `packages/auth/src/server.ts:27` defines as returning `null` — it refuses nothing — and unlike the page branch, `api/` had no `NAMED_PROTECTED_PAGES`-style backstop demanding a specific helper be imported AND called. The gate planted `app/api/zzghost/route.ts` that imported and CALLED `getSessionUser`, ignored the null and answered anyway: **26/26 green**, lint clean, `next build` serving `ƒ /api/zzghost`. Because `withWorkspace` takes `authUserId: string`, that shape can pass a caller-supplied id and get a **correctly filtered read of another tenant's workspace** — a correctly-filtered bypass is still a bypass. Fixed with `hasEnforcingGate`: `requireUser|requireAdmin` only (`getSessionUser` dropped from the api predicate), imported AND called, both matched over `blankComments(src)` so a comment cannot satisfy it. Pinned with three shapes — the `zzghost` probe verbatim, an import-but-never-call route, and a comment-only route. Proven by re-planting `zzghost` in the REAL tree: **red, naming `api/zzghost/route.ts`**, where it had passed 26/26 an hour earlier. Probe removed, tree verified clean.
  Also closed, both stale-claim class in the file written to kill that class: `isMetadataRoute`'s `appRoot` parameter had **no caller anywhere** (a two-mode helper with one live mode is a claim nothing exercises) — removed, with the basename-only reasoning written into the docstring; and the comment claiming "every image **and sitemap** matcher" takes the `\d?` variant suffix was **wrong about sitemap** (only the four `STATIC_METADATA_IMAGES` take it), now corrected AND asserted by a new `sitemap2.tsx → "module"` case so the corrected claim is a property rather than prose.
- 2026-08-17 — phase 4 entry gate, final: typecheck exit 0, lint exit 0 (0/0), db:check clean, **429 passed / 23 files** in CI shape (both Docker suites in one parallel run), keyless build exit 0 with exactly the 10 expected routes, credits race suite 5/5 standalone at 9 tests. Test count across this session: 243 → 251 → 272 → 281 → 296 (phase 3 close) → 355 → 397 → 424 → 428 → **429**.
- 2026-08-17 — phase 4 gate round 5 (final, scoped): **respin-tenancy-reviewer PASS — Ready, A−.** The round-4 `getSessionUser` finding is closed at class level, verified adversarially rather than by reading: it planted NINE shapes at once and the suite went red naming exactly the four that matter (`zzghost`, a `"use server"` module under `api/`, a method call on a non-imported object, and an aliased real gate). It also confirmed the page/actions branches did not regress and was precise about WHICH assertion catches a `getSessionUser`-only protected page — not `findUnguarded` (whose `GATE_IMPORT` accepts it by design) but the exhaustive named fixture, i.e. the backstop the fix relies on, proven live rather than assumed.
- 2026-08-17 — round-5 residual closed (the reviewer's stated reason for A− rather than A): `import { getSessionUser as requireUser }` still slipped, because both halves of the predicate matched the LOCAL name — the round-4 defect wearing the gate's name. The same weakness produced a false positive in the other direction: `import { requireUser as ru }` is genuinely gated and was reported as ungated. Fixed by **resolving the import binding instead of matching the name**: `enforcingGateBindings` parses the `@respin/auth` import clause, skips `import type` (which binds nothing at runtime and cannot refuse anyone), takes the IMPORTED name to decide whether it is enforcing, and returns the LOCAL name it was bound to; `hasEnforcingGate` then requires a bare call of that local name via `(?<![.\w$])`. Both directions now correct. Proven by re-planting the round-5 escape in the REAL tree: **red, naming `api/zzalias2/route.ts`**. Pinned with four fixtures (aliased non-gate → caught; aliased real gate → not caught; type-only import → caught; a method call on a local stub beside a real import → caught). NOTE: my first expectation for that last fixture was WRONG — I expected the method-call case to pass and the entry gate caught it. The predicate is stricter than I assumed, in the safe direction, and the fixture now records the correct answer. Recorded because an expectation written to match an assumed implementation is how a vacuous pin gets born.
- 2026-08-17 — round-5 remaining residuals, recorded as **M2-forward deferrals with the tenancy gate's agreement, not Ready-blockers**: three shapes still slip the regex — `requireUser()` called only inside a branch that never runs; a real import used in type position beside a stub object call; both patterns inside a template literal (`blankComments` blanks comments, not string literals). All three need scope/binding analysis (an ESLint rule, or a runtime `AsyncLocalStorage` stamp that `withWorkspace` refuses without) — genuinely beyond what a regex can assert. All require code that LOOKS gated while not being gated, i.e. deliberate disguise or dead code, not the forgetting-mode this gate exists to catch. M1's `api/` surface is two files, both allowlisted, and holds no creator-data route. **Receiver: M2, when `api/` grows — the alias case was first in line and is now closed; the remaining three go to the M2 plan's dependency check.** Also carried: `findUnguarded` alone still accepts `getSessionUser` for pages (a two-link chain — exhaustiveness assertion → named-gate assertion — both links present and both proven red); consider `hasEnforcingGate` there too at M2, since no page legitimately uses `getSessionUser` today.
- 2026-08-17 — housekeeping the round-5 gate disclosed rather than left implicit: its probe build had left `respin/.next/server/app/api/zz*` and `.next/types/**` entries behind (gitignored, but `next start` would have served them, and the stale generated types broke `tsc --noEmit` on the next edit — the post-edit hook caught it immediately). Cleared by rebuilding; `.next` now holds exactly the 10 real routes and no `zz*` types. Worth keeping as a lesson-shaped note: a probe deleted from `app/` is not deleted from the build output.
- 2026-08-17 — **M1 phase 4 FINAL entry gate**: typecheck exit 0, lint exit 0 (0 errors, 0 warnings), db:check clean, **430 passed / 23 files** in CI shape (both Docker suites in one parallel run), keyless build exit 0 with exactly the 10 expected routes, credits race suite 5/5 standalone. Session arc: 243 → 296 (phase 3 close) → 355 → 397 → 424 → 428 → 429 → **430**.
