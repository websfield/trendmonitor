# Audit-remediation progress ledger — `docs/plans/respin-audit-remediation-2026-08-17.md`

Append-only. One line per lifecycle event, so an interrupted session resumes from the true state.
Source audit: `docs/progress/audit/2026-08-17.md` (30 findings, readiness **Not yet / C+**).

---

- 2026-08-17 — **started.** Checkpoint snapshot taken (`git stash` entry `claude-jig checkpoint: respin-audit-remediation R0-R4`; tree re-applied intact). Baseline entry gate before any edit: `pnpm -C respin typecheck` exit 0, suite **431 passed / 10 skipped / 23 files** (2 Docker suites loud-skipping, no `TEST_DATABASE_URL`).

- 2026-08-17 — **R0 COMPLETE (decisions + design + citations).**
  - `docs/initial/decisions.md` **R-25** appended (append-only, as the file requires) settling all four policy findings: **D-AUDIT-1** `invoice.paid`-while-paused (refuse during-pause, grant the pre-pause delivery race, timestamp-discriminated on `pause_periods.started_known_at` ± `CLOCK_SKEW_MS`; the more expensive "never grant while paused" branch is recorded as *rejected with its reason*, not silently skipped); **D-AUDIT-2** `stripe_events.payload` 90-day retention + the binding "no new reader before redaction" constraint; **D-AUDIT-3** fold metrics `respin.credits.fold.row_count` / `.duration_ms` with the 10,000-row / 250 ms-p95 revisit triggers; **D-AUDIT-4** proprietary / all-rights-reserved posture, explicitly not an OSI license by default.
  - The tolerance DIRECTION in D-AUDIT-1 was made explicit after drafting (it widens the grant window, matching `pause.ts`'s two existing bounds) because the first wording could be read either way and the code had to match the doc.
  - `docs/plans/respin-m2-profile-cage-design.md` written — audit #23. `VerifiedProfileId` brand with **no** `trustProfileId` counterpart, composite workspace+profile scoping at *every* accessor (not just the mint), a non-enumerating refusal, and six entry-gate tests P1–P6 that must fail against un-caged code before any M2 route is written.
  - `NORTH_STAR.md:10` citation fixed (#30): "PRD §5.2" → "PRD §5, success metric 2" — verified against `docs/initial/PRD.md:161`, which is a flat unnumbered list with no §5.2.
  - `docs/progress/respin-m1/ledger.md:125` migration numbering corrected in place with a note (#adjacent oddity): "0010_acoustic_warhawk" → **0009**, which is what has always been on disk.

- 2026-08-17 — **R1 money-path: 11 of 12 items landed, 1 deferred.** Entry gate after: typecheck exit 0, `eslint .` exit 0, **439 passed / 10 skipped / 21 files passed** (+8 vs baseline, no suite lost), `db:check` "Everything's fine".
  - **#1 paused pack charge (HIGH) — FIXED.** `createPackCheckoutUrl` now refuses with a typed `SubscriptionPausedError` **before** the config read and before `getOrCreateCustomer`, because that helper can CREATE a Stripe customer as a side effect — a refusal that has already written to Stripe is a weaker refusal. Guard is in the package, not only the action, per the same reasoning `assertOwner` lives there.
  - **#2 `invoice.paid` while paused (HIGH) — FIXED per D-AUDIT-1.** New `openPauseStartedKnownAt` in `pause.ts` (knowledge clock, not processing clock — mixing those has been a live money defect in both directions, migrations 0007/0008) + `prePauseGrantDecision` in `webhooks.ts`. Structured log naming event type, invoice id, event id, pause decision, measured lag, and reason.
  - **#3 unlocked mirror-writer TOCTOU (HIGH, the audit's most cross-confirmed finding) — FIXED.** `takeWorkspaceLock` taken ONCE in `handleStripeEvent` after workspace resolution, ahead of `dispatch` — guarding where the path is BUILT so a sixth writer inherits it, per CLAUDE.md's 2026-07-30 lesson, rather than five call sites that must each remember.
  - **#4 invoice→subscription identity (HIGH) — FIXED** in both invoice handlers. `invoiceSubscriptionId` reads `parent.subscription_details.subscription`, verified against the installed SDK (`string | Subscription`, both shapes handled). The legitimate first-invoice-before-snapshot case is allowed explicitly — the evidence run OBSERVED that order live (`invoice.paid` 23:36:48.738 before `subscription.created` 23:36:49.106).
  - **#5 dead-subscription drift (HIGH) — FIXED, both halves.** `stripePriceId` added to `DEAD_SUBSCRIPTION_FIELDS`; `pausedAt`/`resumesAt` deliberately **not** added there — they are `pause.ts`'s columns by the dual-truth rule, so the death writers call `ensurePauseEnded` then `clearPauseMirror` instead. Read side: `state.ts`'s paused branch is now liveness-gated. 3 new pins incl. a non-vacuity case (a paused `past_due` subscription is still paused).
  - **#6 `unpaid` keeps charging authority armed (MEDIUM) — FIXED.** `TERMINAL_STATUSES` moved to `state.ts` beside `IRREVERSIBLE_STATUSES` (it was defined in `webhooks.ts` while its sibling lived in `state.ts` — two half-definitions of "dead" in two files). New `mayChargeOffSession` documents that liveness ≠ chargeability. Refused at BOTH `setAutoTopup` (arming) and `maybeAutoTopup` (charging).
  - **#7 two pack-price authorities (MEDIUM) — FIXED.** New `stripe/pack-price.ts`: one resolver, Stripe's `Price` is the charge authority, validates active / fixed `unit_amount` / currency against the installed SDK's real fields, refuses on config divergence rather than picking a side.
  - **#25 resume liveness (LOW) — FIXED.** Typed refusal instead of a raw Stripe error reaching the page as "Something went wrong".
  - **#27 ignore/refusal log omits `event.type` (LOW) — FIXED.**
  - **#28 business-object unique race → false 500 (LOW) — CLOSED BY #3's LOCK**, and the alternative fix was deliberately NOT taken: adding those constraints to `IDEMPOTENCY_CONSTRAINTS` turns a 23505 into a silent 200, which is exactly the "collapsing them all into a 200 silently discards events" hazard the round-2 BLOCK closed. Reasoning recorded in the code.
  - **#29 zero-line-item snapshot (LOW) — FIXED**, fails closed instead of writing a live mirror with a null price.
  - Five new typed errors reach `app/**` through the facade with rendered `?e=` copy, as the facade-errors and billing-ui completeness suites demand.
  - **DEFERRED, named: #24** (`PackParams.configVersion` optional → required). Mechanical, touches every pack-mint caller and their fixtures; not started.

- 2026-08-17 — **R1 evidence note: three existing tests asserted the OLD behaviour and were corrected, not deleted.** (a) `state.test.ts`'s paused fixture carried no `stripeSubscriptionId`, making it accidentally the #5 drift shape rather than the ordinary paused shape — given a subscription id, with the genuine drift pinned separately. (b) `isolation.test.ts`'s invoice fixture set the LINE's parent but not the INVOICE's `parent.subscription_details` — an unrealistic payload that passed only while nothing checked invoice identity (the sibling fixture in `stripe.test.ts:167` has always set both). (c) `actions.test.ts`'s "dunning or cancel-at-period-end can still arm it" loop included `unpaid`, i.e. it actively pinned the violation audit #6 reports; `unpaid` moved out, with three new cases (cannot arm / can still disarm / charge site refuses). The charge-site test **failed red first** and caught that half of #6 was still missing from `auto-topup.ts` — the fix followed the test.

- 2026-08-17 — **NOT DONE, and none of it is claimed.** R2 (audit #8 incomplete-subscription UI state, #15 auth-form labels, #16 admin config error focus/association, #17 billing disabled-control `aria-describedby`, #26 auto-top-up M3 disclosure) — **not started**. R3 (#9 backups + restore drill, #10 honest rollback, #11 RUNBOOK quick-start, #18 CI `pnpm audit`, #19 LICENSE, #20 Better Auth rate limiting, #21 retention receiver, #22 fold metrics) — **not started**; #9/#10 are externally gated by the unprovisioned Lightsail deploy anyway, and the plan marks R3's backup acceptance a production blocker. R4 (#12–#14 runbook consistency, audit-closure record) — **not started**. Also NOT done for R1: the **differential-payload Docker concurrency test** the plan requires as #3's proof (`concurrency.docker.test.ts` only exercises identical payloads today), the UI half of #1 (disabling the Buy-pack control when paused), and the full CI-shape run with `TEST_DATABASE_URL` set — so the lock fix is proven by construction and by the unit suite, **not** by a race that fails before it and passes after.

- 2026-08-17 — **Critical-Path gates: NOT RUN.** This diff touches the Respin billing & credits path (and `state.ts`/`pause.ts`, so arguably brain-tenancy too). Neither `respin-billing-reviewer` nor `respin-tenancy-reviewer` has seen it: the session is configured not to spawn agents, so the gate could not run in the form CLAUDE.md specifies. Six of eight rounds in M1 shipped a defect inside their own fix, so "the suite is green and the reasoning is written down" is exactly the argument that has been wrong here before. **The diff is not Done by the CLAUDE.md Definition of Done until those gates run.**

- 2026-08-17 — **R1 LEFTOVERS CLOSED, and the one that was mis-claimed.** (a) **#24** `PackParams.configVersion` is required; both production callers already passed it, two fixtures did not, and the post-edit hook named them. (b) **#1's UI half** landed with R2 (below). (c) **The differential-payload Docker race** — #3's required proof — now exists, and writing it exposed that #3 was only HALF fixed: `invoice.paid`'s grace-clear was still unconditional while its own status-lift half was order-guarded, so a STALE paid invoice ended a LIVE dunning window and `past_due` + a null deadline derives to `free` — a paying customer dropped out of the window they were promised, permanently, because nothing re-opens a deadline. The lock could never have fixed this (serializing two writers does not give a stale writer a newer opinion), so the previous ledger entry's "#28 CLOSED BY #3's LOCK" was right but "#3 — FIXED" was premature. Split `invoiceIsStale()` out of `invoiceMayWriteStatus` — ORDER only, deliberately not TERMINAL, because on a terminal mirror clearing the deadline is still right and only the order reason must veto. 4 PGlite pins + 2 Docker races.
- 2026-08-17 — **the differential race was VACUOUS on first write, and that is worth recording.** Both new Docker cases passed with `takeWorkspaceLock` DISABLED (3 runs). Cause: `pg.Pool.connect()` opens a real TCP connection (~11ms measured) for a racer that finds no idle client, and `handleStripeEvent`'s whole transaction is FASTER than that setup — so racer B's transaction began after A had committed, and it converged for the honest reason rather than because of the lock. Added `prewarmPool()`; then the snapshot race went red 3/5 (a real race is a coin flip), so it now runs **6 rounds** on 6 workspaces — red **5/5** on the mutant, green **5/5** fixed. The stale-invoice case was vacuous a second way: raced, the invoice reads the mirror before the dunning snapshot commits and skips the clear innocently — it passed 3/3 with the staleness guard removed. Restructured to commit the dunning window first and race the late invoice against its own redelivery: red 3/3 on the mutant, green 3/3 fixed. **Two guards that proved nothing until they were mutation-tested** — the 2026-08-10 present-and-unrun lesson, in race form.
- 2026-08-17 — **R2 COMPLETE.** **#8** `incomplete` is its own `BillingState` (entitlement `tier: "free"`, separate `pendingTier` — collapsing them would hand a paid entitlement to a subscription that never collected a cent) with an owner-only hosted-invoice remedy, `createInvoiceRecoveryUrl`, that fetches the URL from Stripe AT CLICK TIME and never mirrors a payable link (a stored payable URL is a new surface for something D-AUDIT-2's posture already refuses, and `hosted_invoice_url` goes stale under us). Narrowed to `incomplete` only: `past_due`/`unpaid` are dunning and the Portal IS their remedy. **#15** the product's ONLY entry point had no test rendering it at all — visible labels, no placeholders, one constant feeding both the shown password rule and the enforced `minLength`; new 10-case suite. **#16** issue-list id + `aria-describedby` (only when a list exists — a dangling reference is worse than none), `aria-invalid`, focusable alert, focus effect keyed on the state OBJECT so a second failed save re-focuses where a boolean would fire once. **#17** reason ids derived from a now-REQUIRED `testId`, plus a paired assertion that every `aria-describedby` resolves AND every disabled button has one. **#26** the M3 disclosure `/usage` already gave, asserted identical across both pages. **#1 UI half.** +18 UI tests.
- 2026-08-17 — **a hand-maintained gate list had already leaked.** `tests/action-gate.test.ts` listed six actions with `expect(named).toHaveLength(6)` beside them — so the assertion guarded the LIST's length, not the module's surface, and #8's new `recoverInvoiceAction` was added without the gate check seeing it. Now DERIVED from the module's exports: a `"use server"` module may export only async functions, so every export is a POST endpoint and must carry the gate. Same class as the isolation suite's enumeration, and the same class as the six-times-recurring lesson of 2026-07-30 — fix the class, not the instance.
- 2026-08-17 — **R3 COMPLETE except the deployment-gated half of #9.** **#22** `metrics.ts` emits the two metric names D-AUDIT-3 committed to, from the ONE balance authority; a case asserts the payload has exactly three keys so PII cannot be smuggled in later, and another proves a throwing sink cannot take a balance read down. The 7-day p95 trigger is deliberately NOT computed from one sample — that would be the measurement dishonesty this audit exists to catch. **#20** verified against the INSTALLED better-auth 1.6.28 types before coding: the default is production-only and in MEMORY, so the shipped limiter was both off in every environment a test could reach and non-durable where it was on. Now explicit, `storage: "database"` — which needed a `rate_limit` table and migration 0010, because the adapter would otherwise have failed at RUNTIME against a missing table. Tests drive the real handler (`auth.api.*` bypasses the limiter); mutation-proven — the table assertion goes red under `storage: "memory"`. The rebuild-survives case was HONESTLY DOWNGRADED in its own comment after mutation showed it passes under memory too (that store is process-wide), so the comment now says which half each case proves. **#18** the scan gates CI and **immediately found 4 high + 3 moderate pre-existing advisories** — baselined with per-advisory reasons and an owner in `SECURITY-EXCEPTIONS.md`, a deferral and not an acceptance; `drizzle-orm` is a direct dependency on the money path and needs its own change. **#19** proprietary notice per D-AUDIT-4. **#21** the no-new-reader constraint is ENFORCED by a source scan, proven red against a planted reader; receiver recorded in build-plan M6 with its own accept-when. **#9/#10/#11** backup + restore scripts written and REHEARSED end-to-end on the real dev dataset (encrypted dump, checksum, isolated restore, representative rows, the ledger's no-negative invariant re-derived on restored data, `db:check` parity clean, and the drill's own guard proven to fire); RUNBOOK rewritten for honest rollback with the blast radius stated.
- 2026-08-17 — **R4 COMPLETE.** `docs/progress/audit/closure-2026-08-17.md` maps all 30 findings to ENG / CI / OWNER proof, and names the five things that are NOT closed rather than rounding them up. RUNBOOK #12/#13/#14 fixed.
- 2026-08-17 — **CI-SHAPE GATE, and it caught a real regression the earlier runs could not.** `TEST_DATABASE_URL` set, both Docker suites live: the first full run was **496/497 with one FAILURE** — `concurrency.docker.test.ts`'s invoice fixture set the LINE's parent but not the INVOICE's `parent.subscription_details`, so under audit #4's identity check every delivery in that race threw and the case measured ZERO grants against an expected one. It is the SAME fixture defect R1 had already corrected in `isolation.test.ts`, and it survived because **the Docker suites were never run after R1 landed** — exactly the gap the previous ledger entry flagged as "NOT PROVEN". Fixture corrected (the code guard is right; real Stripe always sends that field). Final entry gate: typecheck clean, `eslint .` clean, **497 passed / 497, 0 skipped, 26 files** with both Docker suites live, `db:check` clean, keyless `pnpm build` exit 0.

- 2026-08-18 — **CRITICAL-PATH GATES RUN AT LAST** (the previous entry's own blocker). Billing: **BLOCK, grade D.** Tenancy: **NEEDS CHANGES, grade B.** Both were right, and the BLOCK is the one that mattered: **D-AUDIT-1 shipped with ZERO tests.** The refusal branch — the one that denies a paying creator a month's allowance and records `ignored` permanently — had no case in any suite, and `stripe.test.ts:630`, the test R-25 itself names as having "actively pinned the violation", was still there UNEDITED and still asserting the grant lands. It was green for a reason nobody had written down: `mkEvent` stamps `created: nowSec()` and the pause opens milliseconds earlier, so `lagMs ≈ 0` and it was exercising the TOLERANCE EXCEPTION under a title claiming a general property. Deleting the whole pause check left the suite green. Retitled to say what it proves, plus 5 new cases (during-pause refusal with zero ledger rows and the `ignored` event row selected BY ID; idempotent redelivery; the 59s/600s boundary in both directions; unpaused; resumed). **Mutation-proven: 3 of 6 go red with the refusal deleted**, the 3 that stay green being the grant directions, which is correct.
- 2026-08-18 — **#7 was HALF-LANDED and the code claimed otherwise** (billing gate CHANGE). `maybeAutoTopup` still charged `Math.round(content.pack.priceUsd * 100)` as a raw off-session amount — no Stripe `Price`, no divergence check — while `pack-price.ts`, `actions.ts` and `isolation.test.ts` all asserted in prose that "both paths now come through `resolvePackPrice`". `grep` found ONE caller. Worse, the hazard had CHANGED SHAPE rather than closed: after the manual path was fixed, an `/admin/config` edit to `pack.priceUsd` made manual Checkout REFUSE while auto-top-up silently charged the new un-validated number. Now genuinely one authority. Ordering matters and is documented: the spend query and an EXACT no-headroom short-circuit (`spent >= cap` — true for every possible price, so no read could change it) run BEFORE the resolver, which keeps every cheap refusal keyless, and the authoritative cap test runs after. Four new cases incl. the config/Stripe divergence the plan named and never had. Fallout, fixed honestly rather than papered over: auto-top-up now REQUIRES a mapped Stripe pack price, so `seedDb`'s empty `stripePriceMap` made five pre-existing cases fail — the baseline now maps one, which is the state a real install is in after `stripe:setup`.
- 2026-08-18 — remaining billing CHANGEs closed: **#1's package half** (the AUTHORITATIVE guard, per the code's own argument, was the untested one) with a paused-refusal case asserting `sessionCreate` is never called plus its unpaused twin; **`state.ts`'s `incomplete` branch** (4 cases — changing `tier: "free"` to the paid tier, the exact thing its docblock forbids, would have failed nothing; plus unmapped-price, the `incomplete_expired` non-vacuity twin, and branch-order preservation under a pause); **`createInvoiceRecoveryUrl`'s Stripe half** (the mock exposed no `subscriptions.retrieve`/`invoices.retrieve`, so the happy path, both refusals and the `string | Invoice` dual-shape branch were all unreachable — 5 cases now).
- 2026-08-18 — tenancy CHANGE closed: **the retention tripwire did not catch the class its own header claims.** The reviewer ran the regex against five planted shapes: `select({p: stripeEvents.payload})` CAUGHT, `select().from(stripeEvents)` MISSED, `db.query.stripeEvents` MISSED, `row.payload` MISSED — in the file written to discharge the "a comment claiming a property is not the property" lesson. Widened to four read shapes (a bare full-row select IS a payload read), and SCOPED to product source: excluding `*.test.ts` is the constraint's own wording ("no new product **surface**"), not a loophole — the suites that assert what the dispatcher wrote are not surfaces. Re-proven red against a planted whole-row select in `packages/credits/src`.

- 2026-08-18 — tenancy CHANGE closed: **`internalOnly` meant less than every reason attached to it claimed.** The assertion checked absence from `index.ts` only, but app/** imports the FACADE, not the index — so a name could be "package-private" and still fully reachable, which `hasLiveStripeSubscription` already demonstrated. Now checked against both, and writing the check IMMEDIATELY found two more legitimate divergences the old assertion could never have shown (`StripeNotConfiguredError`, `getWebhookSecret`). Error classes are exempted as a CLASS, not one by one — `facade-errors.test.ts` actively REQUIRES every constructible error to be re-exported, so an individual list here would be a second drifting copy of that rule; everything else needs a named reason in `FACADE_REEXPORTED`. First attempt at the exemption used an inverted `isPrototypeOf` and exempted nothing; corrected to `prototype instanceof Error`.
- 2026-08-18 — **NOT fixed, and recorded rather than quietly dropped** (billing gate NOTEs + one tenancy CHANGE):
  - **`trustedProxies` is unset.** Verified in the installed types (`advanced.ipAddress.trustedProxies?: string[]`, and the docs state that unset "trusts only single-value IP headers"): behind a two-hop proxy chain — or against any client that sends its own `X-Forwarded-For`, since the proxy then appends a second entry — `getIPFromHeader` returns null and every request falls back to the shared key `no-trusted-ip|<path>`, so ALL tenants share ONE 5-per-minute sign-in bucket and one client can lock everybody out. NOT configured because the correct value is the deployment's real proxy addresses and Lightsail is unprovisioned; a WRONG value is worse than none (it lets a client spoof its own IP and evade the limiter entirely). Recorded in **R-26 as a first-deploy blocker** with the follow-up test named: a two-hop `X-Forwarded-For` case, which the current suite does not have.
  - **`pausedBlock` and the package guard key on different things.** The UI blocks on `state.state === "paused"`, which is liveness-gated since audit #5; `createPackCheckoutUrl` blocks on the raw `pausedAt !== null`, which is not. In the #5 drift shape (`{status: canceled, pausedAt: set}`) the Buy-pack button therefore renders LIVE and fails on click — the exact "button that fails on click" the `pausedBlock` docblock exists to prevent, in the one state it cannot see. No money moves (the server refusal is correct and authoritative); the defect is a worse error experience in a drift state. Deferred deliberately: the fix threads a new prop page → view and belongs in a change with room to test it, not appended to a gate round.
  - `metrics.ts`'s default sink `console.info`s on EVERY fold — correct for D-AUDIT-3's sampling, but a per-workspace activity stream in production logs, with its own unrecorded log-retention question. Worth a level or a sampling decision before Lightsail.

- 2026-08-18 — **final entry gate after the gate round.** typecheck exit 0, `eslint .` exit 0, `db:check` clean, keyless `pnpm build` exit 0. CI-shape suite (`TEST_DATABASE_URL` set, both Docker concurrency suites live, no loud-skips): **515 passed / 517, 2 files failing on `Test timed out in 30000ms` / `Hook timed out in 30000ms`** — `tests/action-gate.test.ts` and `packages/auth/tests/auth.test.ts`. **Both pass 20/20 when run in isolation**, and the failures are timeouts rather than assertions, so they are this machine's load after hours of back-to-back PGlite suites, not logic. Reported as 515/517 rather than rounded up to green: the honest claim is "no assertion failed and two suites timed out under local load", and CI (fresh runner, Postgres service container) is where that gets settled. Test count is up from the 497 of the pre-gate run (+20 from the gate round's new cases).
