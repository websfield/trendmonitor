# Progress ledger — cutdown

Append-only. One line per lifecycle event.

- 2026-07-21 — phase 1: started (checkpoint snapshot `claude-jig checkpoint: cutdown phase 1`; dependency gate: `Depends on: none`)
- 2026-07-21 — phase 1: decisions D-39 (toolchain actuals), D-40 (asset classification), D-41 (rotation via Display Matrix) appended to `docs/video-editing/decisions.md`
- 2026-07-21 — phase 1: tasks 1-7, 9, 10 complete. Contracts entry gate green: `validate:contracts` 15 cases PASS (Ajv+Pydantic agree), `build:contracts --check` PASS. Tests: contracts 24/24, ingest classify+rights 16/16
- 2026-07-21 — phase 1: task 8 (ingest) partial — classify.ts + rights.ts done and tested; main.ts pending renderer-core probe/proxy API
- 2026-07-21 — phase 1: task 8 (ingest) complete — atomic staging+promote, D-40 classification with real alpha probe, D-42 (video-only proxy) appended
- 2026-07-21 — phase 1: entry-gate PASS (tsc clean; validate:contracts 15 cases 0 disagreements; build:contracts --check current; tests 24+65+16)
- 2026-07-21 — phase 1: reviewer code-reviewer (round 1): BLOCK — 3 must-fix (floating assertLibass; rights fail-open on non-bare dates; proxy storedPath into deleted staging), 6 should-fix, 4 optional
- 2026-07-21 — phase 1: all 3 BLOCKs + CHANGEs 4-9 fixed; 10 new integration/regression tests added. Entry gate re-run PASS; tests now 24+65+26 = 115, 0 fail
- 2026-07-21 — phase 1: reviewer code-reviewer (round 2) requested
- 2026-07-21 — phase 1: reviewer code-reviewer (round 2): NEEDS CHANGES — all 3 BLOCKs verified FIXED; 1 new defect (promote order → dangling inventory), 3 residuals. All 4 fixed; tests 24+65+28 = 117, 0 fail
- 2026-07-21 — phase 1: reviewer code-reviewer (round 3) requested. NOTE: this exceeds the /implement two-round limit — round 3 is accepted as FINAL regardless of verdict; any remaining findings are surfaced as residuals, not fixed in a further loop
- 2026-07-21 — phase 1: reviewer code-reviewer (round 3): PASS. Final optional finding (comment overstated what is persisted) fixed — rejected date values now committed into the artefact's `notes`, with 3 tests
- 2026-07-21 — phase 1: complete. Entry gate green; 120 tests 0 fail; review at docs/progress/cutdown-phase-1-review.md; master-plan Progress Tracking updated
- 2026-07-21 — phase 2: NOT started. Recommended: fresh session (/clear then /go) — plan guidance is one phase per session
- 2026-07-21 — phase 2: started (checkpoint snapshot `claude-jig checkpoint: cutdown phase 2`; dependency gate PASS — phase-1 review reads Ready on disk)
- 2026-07-21 — phase 2: indexer worker scaffold created (`workers/indexer-python`, uv workspace member); ML toolchain installed and each engine verified by import: faster-whisper 1.2.1, scenedetect 0.7, cv2 4.10.0, paddleocr 3.7.0, paddle 3.3.1, silero-vad 6.2.1, torch 2.13.0+cpu, sentence-transformers 5.6.0, librosa 0.11.0
- 2026-07-21 — phase 2: toolchain defect found + fixed — three OpenCV distributions (headless 5.0, contrib 4.10, opencv-python 5.0) share one `cv2/` dir and clobbered each other, breaking scenedetect/paddleocr/paddle at import. Pinned to a single upstream 4.10.0.84 via `override-dependencies` in the workspace root. Candidate decisions.md append (toolchain pin, not an engine swap)
- 2026-07-21 — phase 2: KNOWN ISSUE (blocks T6) — `panns_inference` (D-20) fails at *import*: it shells out to `wget` for `~/panns_data/class_labels_indices.csv`, and wget does not exist on Windows. Fix planned in T6: vendor the AudioSet label CSV and import the engine lazily behind a structured error, per the phase plan's offline/degraded contract
- 2026-07-21 — phase 2: task 10 (range-check) complete — `packages/contracts/src/range-check.ts` is the single implementation (exact BigInt cross-timebase comparison, never throws, fails closed on unknown duration); 21/21 tests pass in `packages/contracts/tests/range-check.test.ts`. CLI post-step wiring + fixture corpus + the Python-drives-CLI test still outstanding
- 2026-07-21 — phase 2: task 1 (sub-stage harness) complete — `workers/indexer-python/src/harness.py`: REQ-005 cache keying (content hash + indexer version + model config), resume-on-checkpoint, artefact-before-checkpoint write order, atomic temp+rename, tech-spec §6.2 structured errors (exit 2/3), run-log append matching the TS CLI's shape, explicit TRACEPARENT threading. 26/26 tests pass in `tests/test_harness.py`
- 2026-07-21 — phase 2: PHASE-1 RESIDUAL FOUND + FIXED — the `pnpm cutdown` script pointed at `apps/cli/dist/main.js`, but tsc emits `dist/src/main.js`, so the documented entry-gate command `cutdown validate:contracts` failed with MODULE_NOT_FOUND. Phase 1's review evidently invoked the CLI by direct path. Fixed in `cutdown/package.json`
- 2026-07-21 — phase 2: PHASE-1 RESIDUAL FOUND, NOT FIXED (needs a decision) — `pnpm test` FAILS in aggregate: `packages/skill-runtime`, `skills/brief`, and `apps/cli` each declare `"test": "node --test dist/tests/"` but ship no tests directory. `skills/brief` therefore has ZERO tests despite Phase 1 task 7 delivering the brief skill. Phase 1's "120 tests 0 fail" was counted by running suites individually. Individually: contracts 45, renderer-core 65, ingest 31 = 141 pass, 0 fail
- 2026-07-21 — phase 2: entry gate PARTIAL — `validate:contracts` PASS (15 cases, 0 disagreements), `build:contracts --check` PASS (generated trees current), TS suites 141 pass / 0 fail individually, python 26 pass. Aggregate `pnpm test` RED on the three empty-test-dir packages above (pre-existing, not introduced by phase 2)
- 2026-07-21 — phase 2: PAUSED mid-phase at a clean boundary. Complete: tasks 1 and 10 (core). Outstanding: tasks 2-9, 11, 12 plus range-check fixture corpus/CLI post-step. Next session resumes at task 2 (transcript sub-stage)
- 2026-07-21 — phase 2: decisions.md D-43 appended (single pinned OpenCV distribution). CLAUDE.md Lesson added (verify installs by import, not exit code; workspace roots need --all-packages)
- 2026-07-21 — phase 2: task 8 (index assembly) complete — `src/assemble_index.py` + `src/ids.py`: REQ-019 timebase map (CFR empty / VFR explicit PTS entries), exact Fraction tick conversion, sub-stage ledger that REFUSES a skip/failure without a reason, deterministic content-derived ULIDs (byte-identical re-runs, tech-spec §12). 26/26 tests, validated against the generated Pydantic `SourceIndex`
- 2026-07-21 — phase 2: task 9 (Moment extraction) complete — `src/moments.py` + `src/embed.py`: D-31 speaker-turn x shot-boundary segmentation with 3-30 s merge/split and deterministic time-sliced fallback for static takes, full REQ-018 field population, null-with-reason visual summaries, verbatim/display separation, D-22 bge-small embeddings (384 dims, cap-checked). 40/40 tests, validated against the generated Pydantic `Moment`
- 2026-07-21 — phase 2: task 10 corpus complete — `packages/contracts/fixtures/range-check/cases.json` (13 cases + fail-closed case, every violation code exercised) driven by `tests/range-check-corpus.test.ts` 16/16
- 2026-07-21 — phase 2: DEFECT FOUND + FIXED via entry gate — the Ajv and Pydantic halves of `validate:contracts` DISAGREED on fixture discovery: TS required a `valid/`/`invalid/` bucket, Python treated every subdirectory of `fixtures/` as a contract and failed on the new corpus. Aligned `python/validate_fixtures.py` to the TS rule while preserving its missing-schema drift guard for real fixture dirs. `validate:contracts` PASS again (15 cases, 0 disagreements)
- 2026-07-21 — phase 2: task 10 wiring complete — `cutdown range-check --input <file>` CLI command (exit 0 clean / 1 violations / 2 unusable input, so "nothing to check" can never read as "nothing wrong"); `workers/indexer-python/tests/test_bounds.py` drives that ONE implementation from Python over the committed corpus, 22/22 pass, including the end-to-end property test that ranges produced by the real segmenter pass the real bounds checker
- 2026-07-21 — phase 2: `index` skill surface created — `skills/index/SKILL.md` (uv argv entrypoint per tech-spec §6.2) + input/output schemas; output carries a `boundsCheck` summary reporting `checked` alongside `ok`
- 2026-07-21 — phase 2: task 5 (visual descriptions + model gateway) complete via delegated agent — 68 tests. `--no-vlm` defaults ON (fail-closed while D-21 ceiling unset); every skip path yields empty descriptions + a reason and never fabricated text; injected transport so no test touches a socket; API key redacted from anything leaving the module. NOTE for owner: gateway uses stdlib urllib against the documented Messages API rather than the `anthropic` SDK (not installed); model default `claude-sonnet-5` per D-21 "current Sonnet-class". `cutdown/.env.example` still needs creating before the ceiling is set
- 2026-07-21 — phase 2: PHASE-1 TEST GAP CLOSED via delegated agent — 8 new test files, 145 new tests (apps/cli 80, skill-runtime 38, skills/brief 27). `pnpm test` now passes in AGGREGATE (exit 0); previously 3 of 6 packages errored on a missing dist/tests dir. Agent verified the tests can fail via mutation checks (removed the `..` guard in assertSafeJobId -> the dot-dot test failed; sources restored). Totals: contracts 61, renderer-core 65, skill-runtime 38, cli 80, ingest 31, brief 27
- 2026-07-21 — phase 2: two source findings recorded, NOT changed (need a decision): (1) `apps/cli/src/skills.ts` `parseStructuredError` doc says it scans for the LAST JSON object on stderr but the code takes the FIRST brace, so leading noise containing a brace defeats structured-error recovery — degrades to raw stderr, not a safety hole, covered by a `KNOWN LIMIT:` test; (2) `errors.ts` emits `err.stack` in details for non-CutdownError throws while the skill runtime never emits a stack — defensible (different streams) but the two modules state the "never a stack trace" rule differently
- 2026-07-21 — phase 2: task 11 (orchestrator) written — `workers/indexer-python/src/main.py` sequences all seven sub-stages with per-sub-stage degradation (a failed sub-stage is RECORDED with a reason and the run continues; the job stays resumable), validates both artefacts against the generated Pydantic models BEFORE writing, adopts TRACEPARENT explicitly, and delegates bounds checking to the `cutdown range-check` CLI. An out-of-bounds Moment FAILS the run. `cutdown index <job> --asset <id>` wired; VLM opt-in via `--vlm` (fail-closed default)
- 2026-07-21 — phase 2: task 2 (transcript + speaker turns) complete via delegated agent — 117 tests. Real-model determinism verified (two runs over clean.mp4 byte-identical). Engine findings: `broll-silent.mp4` has NO AUDIO STREAM at all (not silent audio) and faster-whisper 1.2.1 raises a bare IndexError from PyAV — fixed at the cause with a pre-load `has_audio_stream()` probe, and the artefact now carries `audioPresent` so "no audio" stays distinguishable from "transcribed silence"; `temperature` pinned to 0.0 (the default is a re-decoding fallback ladder, non-deterministic); `vad_filter=True` kept or whisper hallucinates speech from silence. Speaker-turn confidence is deliberately capped at 0.5 — always below the D-28 0.6 threshold — because a silence-gap heuristic is not diarisation (D-17) and a turn that presented itself as confident would make the lowConfidence mark decorative
- 2026-07-21 — phase 2: two agent-flagged gaps closed in files the agent correctly would not touch — `pyyaml` now DECLARED in the worker pyproject (it resolved only transitively), and the `slow` pytest marker registered in `cutdown/pyproject.toml` (an unknown-mark warning trains people to ignore warnings)
- 2026-07-21 — phase 2: task 4 (OCR) files landed (`src/ocr.py`, `tests/test_ocr.py`) but the delegated agent ended WITHOUT a completion report — its work is unverified by its own account and must be checked at the gate, not assumed
- 2026-07-21 — phase 2: task 3 (shots + scenes) complete via delegated agent — 83 tests, 3 generated transition fixtures (~70 KB). Engine findings on scenedetect 0.7: `FrameTimecode` now carries `.pts`/`.time_base` (the whole VFR story — frame_num is never used); the **pyav** backend reports the real container timebase 1/15360 while **opencv** reports 1/1000000 with an AVERAGED frame rate, so pyav is used; pyav's `video.duration` is WRONG for VFR (2.667 s for a 3.8 s asset) so duration comes from ffprobe `duration_ts` as an exact integer; `get_cut_list()` deprecated. Scene barriers: fade and camera_change break scenes unconditionally, hard_cut is absorbed (otherwise the scene layer just duplicates the shot layer) — proved three ways including a counter-case where hard-cut.mp4 correctly stays ONE scene
- 2026-07-21 — phase 2: INTEGRATION DEFECT FOUND BY THE END-TO-END RUN, FIXED — the visual sub-stage correctly reported `status: "skipped"` with a full reason nested under its `subStage` record, but the orchestrator read only the top level and recorded `completed` with a NULL reason beside an empty `visualDescriptions` array — reintroducing the exact "did we look, or was there nothing there?" ambiguity the sub-stage ledger exists to remove. An honest sub-stage was made dishonest by the layer above it. `_stage` now honours a sub-stage's own account (nested record, then top-level status) and manufactures a reason if a non-completed status arrives without one. Regression suite `tests/test_orchestrator.py` 15/15
- 2026-07-21 — phase 2: END-TO-END PROVING RUN GREEN on `clean.mp4` (job idx-1). Second run: **7/7 sub-stages served from checkpoint** (resume proven), indexId `PYHMR5ZH94PA6537PTQFGQ124X` IDENTICAL across runs (deterministic identity proven), `boundsCheck {checked: 1, ok: true}` (the 'zero invalid source ranges' mechanism proven end to end), visual_descriptions correctly `skipped` with its reason surfaced as a warning. Artefacts on disk: source-index + moments + 7 sub-stage artefacts + checkpoints
- 2026-07-21 — phase 2: task 6 (audio events) complete via delegated agent — 108 tests (99 fast + 9 slow). PANNs blocker fixed BOTH ways as directed: AudioSet 527-class CSV vendored to `workers/indexer-python/data/` and copied into place before import, AND `panns_inference` imported lazily so a missing model degrades to ModelUnavailableError. Agent found a SECOND instance of the same bug I had not flagged: `AudioTagging.__init__` also shells out to `wget` for the ~320 MB CNN14 checkpoint — fixed with urllib + temp-file/rename so a truncated download cannot be mistaken for complete. Contract finding: `source-index-v1` is STRICTER than my brief — an `energy_change` whose engine is the RMS track alone is a declared Phase 2 test failure, so RMS only PROPOSES and PANNs must corroborate. Verified on real audio: impact-bursts.wav has four genuine 90 dB transitions and emits ZERO energy_change events because the classifier will not corroborate a synthetic tone — REQ-015 refusing to treat loudness as importance. Stale-doc finding: `enums/audio-event-kind.json` describes a `detector` field that does not exist on AudioEvent
- 2026-07-21 — phase 2: task 7 (quality flags) complete via delegated agent — 68 tests, 12 of 12 REQ-014 kinds implemented with real detectors, positive + negative fixtures and recorded thresholds; both negative controls report zero flags of any kind. ONE HONEST GAP: `poor_crop` covers framing (letterbox/pillarbox) only, NOT subject-clipping — the agent implemented a saliency rule, measured it (1.18 textured / 0.16 flat / 0.77 edge-clipped), found no content-independent fire point, and REMOVED it rather than ship a rule tuned to one footage type. Closing it needs a subject/face model. Two further findings: `cv2.saliency` is an EMPTY namespace in the pinned opencv-contrib 4.10.0.84 wheel; and the textbook 8x8 dHash fired duplicate_frames on every frame of the clean control (consecutive testsrc2 frames hash identically) — grid raised to 32x32, caught precisely by the negative control
- 2026-07-21 — phase 2: task 4 (OCR) VERIFIED BY ME after its agent ended without a report — `pytest test_ocr.py -m "not slow"` 45 passed. The earlier apparent hang was slow model tests plus CPU contention from six concurrent agents, not a wedge
- 2026-07-21 — phase 2: `pytest-timeout` added (dev) with a project-wide `timeout = 300`, and the `slow` marker registered — a hanging test reports nothing at all, which reads identically to "still running" until someone gives up on the gate
- 2026-07-21 — phase 2: ENTRY GATE GREEN — python 607 passed / 17 slow deselected / 0 fail (193s); TypeScript aggregate `pnpm test` exit 0, 302 passed / 0 fail (contracts 61, renderer-core 65, skill-runtime 38, cli 80, brief 27, ingest 31); `validate:contracts` PASS (15 cases, 0 lint violations, 0 cross-validator disagreements); `build:contracts --check` PASS; UGC schemas still parse (untouched by cutdown work)
- 2026-07-21 — phase 2: reviewer gate requested — three parallel read-only reviewers (code-reviewer on the Python worker, code-reviewer on the TypeScript diff, security-reviewer on key handling / subprocess / untrusted input / model downloads). Per CLAUDE.md's Critical-Path table and tech-spec §14, cutdown touches NONE of the four UGC Critical Paths, so the generic reviewer applies — never zero reviewers
- 2026-07-21 — phase 2: reviewer round 1 — ALL THREE REVIEWERS RETURNED **BLOCK**. security-reviewer: 1 HIGH + 2 MEDIUM + 2 LOW. code-reviewer (TypeScript): 1 BLOCK + 7 CHANGE + 4 OPTIONAL. code-reviewer (Python): 3 BLOCK + 5 CHANGE + 4 NOTE. The gate did its job — two of the three BLOCKs were in code I wrote myself, and one of them was a test of mine that could not fail
- 2026-07-21 — phase 2: SECURITY HIGH FIXED — `jobId`/`assetId` reached filesystem paths unvalidated in the Python worker, giving arbitrary file write/read outside `project-data/` (verified by the reviewer: a jobId of `../../../../Users/Public/evil` resolved out of tree). The TypeScript CLI guarded `jobId` via `assertSafeJobId`, but `main.py`'s own docstring documents direct invocation, so the guard had a documented bypass — and `assetId` was validated NOWHERE. Added `assert_safe_id` + `assert_contained` + `assert_safe_media_path` to `harness.py` and enforced them in the worker, so the guarantee holds regardless of caller
- 2026-07-21 — phase 2: SECURITY MEDIUM FIXED — `storedPath` flowed from a stored artefact straight into ffmpeg argv; an absolute value silently replaces the join root and a UNC path makes FFmpeg fetch over SMB, leaking NTLM credentials. Now mirrors `ffmpeg.ts::assertSafeInputPath` plus containment. Also: PANNs CNN14 checkpoint now verified against a pinned sha256 on BOTH the download and the CACHED path (a cache trusted forever was the actual threat) with a 60 s timeout — the file is unpickled by `torch.load`, i.e. code execution. Digest is honestly documented as trust-on-first-use, computed locally, NOT publisher-signed
- 2026-07-21 — phase 2: TYPESCRIPT BLOCK FIXED — `range-check.ts` used `Number.isInteger` where it needed `Number.isSafeInteger`. Above 2^53 the tick was already destroyed by `JSON.parse` before any comparison, so an out-of-bounds range was reported CLEAN — the exact defect the module exists to catch. Worse, the test claiming to cover it was a TAUTOLOGY: both literals rounded to the same value, so it demonstrated float rounding while asserting the opposite. Rewritten to drive through `JSON.parse` and assert rejection. Also fixed: three never-throws holes (null/undefined bounds, null candidates), empty batch now exits 2 (`nothing to check` must never read as `nothing wrong`), corpus coverage now compiler-enforced via an exhaustive Record (it had silently omitted MALFORMED_RANGE), `--no-vlm` was accepted and IGNORED so `--vlm --no-vlm` ran the paid stage, and `test_bounds.py` no longer skips itself silently when dist/ is missing
- 2026-07-21 — phase 2: PYTHON BLOCK 1 FIXED (the most serious) — sub-stages emit ticks in THREE different timebases (16 kHz transcript/audio, container video 1/15360 for shots/OCR, mixed frame/sample for quality) and the orchestrator unioned them as RAW INTEGERS. Every Moment boundary was silently misplaced, and because a 16 kHz tick is numerically larger than the video tick for the same instant, out-of-range detections were DROPPED rather than reported — observed live in job idx-1, where a speech end at 4.98 s was discarded against a 5.0 s asset. Two modules in the same diff (`scenes.py`, `quality.py`) already stated and obeyed this rule; mine did not. Added exact `Fraction`-based `rescale_item`/`rescale_items` and normalise every collection into one timebase before anything compares them. VERIFIED on the real artefact: exactly one timebase (1/15360) now appears across every collection, and the Moment spans the full 5.0 s asset
- 2026-07-21 — phase 2: PYTHON BLOCK 2 + 3 FIXED — a failed `shots` turned `ocr`/`visual` into dishonest "completed, found nothing" AND their cache keys omitted the shots digest, making that empty result permanent; they are now skipped-with-reason when shots did not complete, and BOTH carry a `shotsDigest` in the cache key. The index and Moments were written BEFORE the bounds gate whose error claims to "refuse to commit" them; the gate now runs first and both artefacts land together only on pass. Also fixed: `moment_extraction` missing from the index ledger (verified present now), the VFR timebase map that could never be populated yet asserted a mapping it could not back (now refuses), `_stage` catching only SubStageError so any engine exception killed the run, and `embed.py` caching one encoder while ignoring `model_id` (a vector-space mixing bug in waiting)
- 2026-07-21 — phase 2: entry gate re-run after fixes — python **621 passed** / 17 slow deselected / 0 fail; slow audio suite 9 passed against the real PANNs model and the new digest path; TypeScript **311 passed** / 0 fail (contracts rose 61 -> 70 on the new range-check cases); `validate:contracts` PASS; `build:contracts --check` PASS. End-to-end re-run after the orchestrator restructure: same deterministic indexId, boundsCheck ok, all 8 sub-stages in the ledger
- 2026-07-22 — phase 2: reviewer round 2 (verification) — verdict **BLOCK**. 17 of 19 round-1 fixes verified CLOSED adversarially (including the timebase fix, confirmed thorough down to nested word timebases and the non-mutating copy). But TWO problems: (a) my fix for the VFR timebase map was a **REGRESSION** — it raised for every `vfr`, `unknown` AND audio-only asset, gated on a `presentationTicks` field that NO code in the repo produces, as a bare `ValueError` (not a structured error) fired AFTER every expensive stage including the paid VLM had run. I traded a silent lie for a hard outage on the exact asset class REQ-019 exists to serve; (b) the `https` check I added was PARTIAL — `startswith("http://localhost")` also matches `http://localhost.evil.com`, so a key-bearing POST could still go plaintext to an attacker host
- 2026-07-22 — phase 2: ROOT CAUSE of the regression, named by the reviewer — **`index_asset` had no test at all.** The orchestrator suite only covered three pure helpers, so a change that made every VFR asset unindexable passed a full green suite. Helper-level tests cannot catch a defect that lives in the wiring
- 2026-07-22 — phase 2: round-2 fixes — VFR map now allows empty `entries` when `normalizedTimebase == sourceTimebase` (identity resolution is honest for VFR too, since presentation timestamps are integers in that same exact container timebase) and refuses only a genuine re-basing without a mapping; base-URL check now parses with `urlsplit` and compares hostname exactly against the loopback set; `index_asset` integration tests added (missing asset, no-duration fail-closed, every frame-rate mode reaches the timebase map) plus path-traversal fuzz over `load_request` for BOTH ids; zero-Moment assets decided explicitly rather than routed into a false bounds failure; protocol-shaped media paths now rejected explicitly and the harness docstring corrected to say containment is what carries the protocol guarantee
- 2026-07-22 — phase 2: entry gate after round 2 — python **641 passed** / 17 slow deselected / 0 fail (+20 tests over round 1); TypeScript **311 passed** / 0 fail; `validate:contracts` PASS; `build:contracts --check` PASS
- 2026-07-22 — phase 2: VFR REGRESSION VERIFIED FIXED on real footage — `ugly.mp4` (vfr + rotation + HDR metadata) ingested as job idx-vfr and indexed end to end. Index committed with `timebaseMap: vfr, entries: 0` (honest identity resolution), all 8 sub-stages recorded, Moment range 0->58368 exactly equalling the asset duration, and EXACTLY ONE timebase (1/15360) across every collection — proving the mixed-timebase fix holds on a VFR source too. Under the round-1 regression this asset raised before producing anything
- 2026-07-22 — phase 2: complete (verdict **Almost**). Review at `docs/progress/cutdown-phase-2-review.md`; master-plan Progress Tracking updated. 952 tests, 0 fail. FOUR residuals carried forward, none of them silent: (1) `poor_crop` covers framing only, not subject-clipping (REQ-014 partial); (2) `quality.py` omits a whole modality without recording it; (3) `quality.py::iter_luma_frames` ignores ffmpeg's exit status and never drains stderr; (4) `silence` semantics disagree between `audio_events.py` and `quality.py` — needs a decision record before Phase 3 consumes audioEvents
- 2026-07-22 — phase 3: NOT started. Recommended: fresh session (/clear then /go) — plan guidance is one phase per session. Phase 3 depends on 2; its dependency proof is the review above

- 2026-07-28 — phase 3: **started** (single-driver /implement). Dependency gate: Phase 2 is verdict "Almost" (4 accepted quality residuals, none touching editorial) but implementation-complete + gated with entry gate green (641 py tests) and the consumed artefacts present on disk (idx-1/idx-vfr Moments, moment-v1 schema, embed.py, range-check.ts) — satisfies the entry-gate+DoD clause; proceeding. Checkpoint stash `claude-jig checkpoint: cutdown phase 3` saved.
- 2026-07-28 — phase 3 Task 1 **complete**: 4 editorial contract schemas (creative-brief-v1, master-story-plan-v1, platform-edl-v1, style-profile-v1) + shared common/model-provenance-v1 def + 2 enums (aspect-treatment, audio-mode). Fixtures: valid+invalid per schema. `build:contracts --check` PASS (both TS+Python trees regenerated & current), `validate:contracts` PASS (23 cases, 0 lint, 0 cross-validator disagreements). NOTE: dev machine now runs Node 24.18.0 (D-39 pinned engines <23) — flag for Task 9 better-sqlite3 ABI.
- 2026-07-28 — phase 3 Tasks 2/3/4 + editorial halves of 5/6 **complete** (delegated agent, reviewed+integrated): packages/editorial (gateway.ts, brief.ts, retrieval.ts, angles.ts, story-plan.ts, platform-adapt.ts, edl-resolve.ts, schema.ts, util.ts) + workers/indexer-python/src/embed_query.py + pytest. Whole-graph `pnpm build` EXIT 0; editorial 63 node:test pass (glob form), contracts pass; embed_query 3 pytest pass. Reviewed by lead: gateway key-hygiene/one-retry/spend-ceiling (D-21/D-32) OK, retrieval fail-closed+null-with-reason (D-22) OK, edl-resolve reuses single checkSourceRange + fail-closed bounds (D-37) OK, embed_query no-fabrication OK. Root tsconfig now references editorial. **D-44 appended** (Node 24 drift + test-script glob form).
- 2026-07-28 — phase 3 Task 7 + skill halves of 5/6/7/10 **complete** (delegated agent, reviewed+integrated): packages/qa (editorial-checks.ts, editorial-gates.ts) + skills/{propose,plan,validate} + data/platform-capabilities/tiktok-organic-au-fixture.yaml + recorded-model fixtures + CLI propose/plan/validate/test:skills/test:models. Root tsconfig now references qa + 3 skills; whole-graph `pnpm build` EXIT 0. Tests (Node-24 glob): qa 21, propose 4, plan 3, validate 5 — all pass; no regressions (contracts 70, editorial 63, skill-runtime 38, cli 80, brief 27, ingest 31). validate:contracts 23/PASS. Lead review: D-37 separation structural (assembleGateResult copies gateStatus from deterministic blockers, critic findings can only enter advisories); quote-fidelity two-step subsequence (display⊆verbatim⊆moment) correct; rights/disclosures/prohibited-claims fail-closed. REQ-036 refusal is a tagged-union output arm; validate persists 2 outputs (gate + critic).
- 2026-07-28 — phase 3 Task 9 **complete** (delegated agent, reviewed+integrated): workflows/local durable runner + CLI run/rebuild-index. better-sqlite3 (D-11) genuinely fails on Node 24 (no prebuilt ABI, node-gyp MSBuild failure) -> fell back to built-in node:sqlite, zero runtime deps; **D-45 appended** (toolchain fallback, all D-11 properties hold, run-log authoritative + index.db projection). Root tsconfig + pnpm-workspace (workflows/*) wired; whole-graph build EXIT 0; runner 4/4 tests (advance/resume/rebuild-index/blocked, can-fail shown); cli 80 no regression; validate:contracts PASS. Lead review: analyze() pure state-derivation correct (satisfied-by-first-completed cursor, failed->blocked recoverable, never-attempted->pending); index.db pure projection of run-log => rebuild lossless; unimplemented stage -> awaiting (clean pause), structured-error -> blocked.
- 2026-07-28 — phase 3 Task 8 **complete** (lead-built): packages/style resolver (loadStyleProfile/findStyleProfileForAccount/buildStyleContext, D-26 invariants-only projection for prompt injection, cross-field logo rule enforced) + 2 committed placeholder profiles (data/style-profiles/acct-social-soup-00{1,2}.yaml, approval:null draft). Root tsconfig references style; build EXIT 0; style 5/5 tests (both profiles load+validate, account lookup, cross-field reject, schema-invalid reject). **All 10 phase-3 tasks now built & green.**
- 2026-07-28 — phase 3 **ENTRY GATE PASS**: build:contracts --check PASS; validate:contracts PASS (23 cases, 0 lint, 0 cross-validator disagreements); whole-graph pnpm build EXIT 0; TS tests 416 pass/0 fail (all packages+skills+runner, Node-24 glob); Python 644 pass/0 fail (17 slow deselected, +3 embed_query vs Phase 2 641). Next: code-reviewer gate.
- 2026-07-28 — phase 3 **COMPLETE** (Ready, impl-complete). code-reviewer PASS · Grade A (0 must-fix, 3 optional notes: quote-gate not negation-aware [D-37 backlog], run exits 0 on blocked, firstText throws outside retry loop — all safe). DoD satisfied: entry gate green (416 TS + 644 Py, 0 fail), code-reviewer PASS, PHASE_3_IMPLEMENTATION_COMPLETE earned, PHASE_3_ACCEPTED_LIVE BLOCKED-ON-D-21/D-27 (honest), decisions D-44/D-45 appended. Review: docs/progress/cutdown-phase-3-review.md. Master-plan row 3 updated. Next: phase 4 (render path) depends on this.
- 2026-07-28 — **Node-24 toolchain cleanup (D-46)**: .tool-versions Node 20.9.0->24.18.0; root engines >=20.9.0<23 -> >=22.5.0<25 (engine-strict passes on Node 24, no override); 13 test scripts migrated to native glob `node --test "dist/tests/**/*.test.js"`. Validated: pnpm install clean (no override), build EXIT 0, standard `pnpm -r test` 416 pass/0 fail/exit 0 — D-44 interim glob workaround retired.
- 2026-07-28 — **Live-gate scaffold**: created cutdown/.env.example (ANTHROPIC_API_KEY + CUTDOWN_SPEND_CEILING_AUD + provider/editorial/VLM vars; .env gitignored, .env.example commits). `test:models --live` verified fail-closed clean skip (exit 0, D-21/D-27 message). PHASE_3_ACCEPTED_LIVE remains OWNER-gated: ceiling amount (money), API key (secret), real footage+rights+accounts (D-27) — cannot be completed autonomously.
- 2026-07-28 — **Phase 4 STARTED** (render path). Dependency gate: PHASE_3_IMPLEMENTATION_COMPLETE earned ✓. Task 1 **complete**: render-manifest-v1 schema (immutable render plan — renderer+ffmpeg versions, fonts-by-sha256, media tier draft/final D-34, output geometry, bit-exact determinism knobs D-33, audio-mix plan w/ loudness+true-peak targets, caption triplet, overlay version) + valid/invalid fixtures; regen both languages; build:contracts --check PASS; validate:contracts 25 cases/0 disagreements PASS. Remaining Phase-4 tasks (2-10, render-heavy: RendererAdapter, harden ffmpeg.ts, renderer-ffmpeg + captions, fonts, render skill both tiers, technical-QA hard gate, runner gates + qa-waiver schema, tier-1 byte-identical determinism proof) = next build wave (needs FFmpeg 8.0.1).
- 2026-07-29 — phase 4: Tasks 2-10 built (lead-driven, no delegation). New: `packages/renderer-ffmpeg` (timeline/captions/filtergraph/loudness/adapter), `packages/qa/src/technical` (model/measure/checks/gate), `skills/render`, `workflows/local/src/gates.ts`, `renderer-core/{adapter,manifest}.ts` + determinism pins in `ffmpeg.ts`, CLI `render`. Contracts: render-v1, technical-qa-report-v1, qa-waiver-v1, enums/qa-check-id.json. Data: technical-qa-v1.yaml ruleset, hand-measured TikTok safe-zone overlay, Inter v4.1 fonts by sha256 + OFL
- 2026-07-29 — phase 4: SMOKE RENDER found three engine facts before tests were written — FFmpeg cannot infer a muxer from `output.mp4.partial` (temp name now `output.partial.mp4`, `-f <container>` explicit); libass loads EVERY file in its `fontsdir` and errors on non-fonts (fonts moved to `data/fonts/ttf/`); libass falls back to the system font provider for glyphs Inter lacks and FFmpeg's `subtitles` filter exposes no way to disable it (D-49)
- 2026-07-29 — phase 4: TIER-1 DETERMINISM PROVEN on real FFmpeg 8.0.1 — three renders of one manifest, sha256 `de1420980ea9dff6…` identical; duration exactly 4.000 s; loudness normalised to -14.0 LUFS / -2.9 dBTP. `assertDeterministicArgv` asserts the pins separately, so the byte match cannot pass by luck on a fast machine
- 2026-07-29 — phase 4: THE QA GATE CAUGHT THE RENDERER'S OWN DEFAULTS — `caption_safe_zone` fired on the clean control, exposing three real defects in my caption geometry: horizontal margin derived from HEIGHT, vertical margin placing captions under TikTok's bottom chrome, and a 42-char line that cannot physically fit a 720-wide 9:16 canvas at a 3.8%-height font. D-48 appended (wrap = min(ruleset ceiling, geometric fit) = 18 chars here)
- 2026-07-29 — phase 4: reviewer round 1 — **BLOCK**. 4 BLOCK + 12 CHANGE + 10 NOTE. The headline: I built the QA transition gate, unit-tested it, and NEVER WIRED IT into the production Runner — `cutdown run` advanced past both gates with no QA consulted. Also: waiver render-scope unenforced; waiver files parsed-and-cast rather than schema-validated; the D-35 non-waivable set fully demotable from the ruleset file — with a test of MINE asserting that demotion as desired behaviour
- 2026-07-29 — phase 4: round-1 fixes — gate parameter now REQUIRED (omitting it is a compile error) + `openGate` so "no gate" must be stated; `makeQaGateEnv` implemented and wired into `cutdown run`; `D35_NON_WAIVABLE` code-level floor; waivers Ajv-validated against qa-waiver-v1; `waivedFindingIds` added to technical-qa-report-v1 (the old `waiverIds.includes(findingId)` predicate was always false); three hard-coded thresholds moved into the ruleset; mixed-audio timeline REFUSED rather than half-performed; four false comments corrected
- 2026-07-29 — phase 4: reviewer round 2 (verification) — **BLOCK**. 2 of 4 round-1 BLOCKs CLOSED, 2 PARTIAL: BOTH gate fixes re-opened the same hole from a different angle. (a) `makeQaGateEnv` walked BACKWARDS through render dirs until it found any report, so a new unjudged render was authorised by an older render's verdict — the gate failing open in the exact situation it exists for. (b) The waiver scope was keyed on `renderId`, which `execute()` mints fresh every run — and applying a waiver MEANS re-rendering, so every waiver rejected itself on the run that used it. `--waiver` was dead end to end, and the fixture hid it by hard-coding both ids to match
- 2026-07-29 — phase 4: round-2 fixes — gate lookup now takes the LATEST render directory only and refuses when it has no report; waiver scope moved onto `planHash` (stable across re-renders of one plan, changes the instant the plan does), with `planHash` added as required to BOTH qa-waiver-v1 and technical-qa-report-v1 + a test proving a waiver SURVIVES a re-render of the same plan; the final-tier byte-identity test was hashing one file against itself (tautology) — hashes now taken between executes
- 2026-07-29 — phase 4: ENTRY GATE GREEN — `build:contracts --check` PASS; `validate:contracts` PASS (32 cases, 0 lint, 0 cross-validator disagreements); `test:skills` 3/3; `pnpm -r test` **596 pass / 0 fail / exit 0** (Phase 3 baseline 416); Python 661 pass / 0 fail (untouched). `cutdown run idx-1` and `rebuild-index` verified working with the gate wired
- 2026-07-29 — phase 4: **COMPLETE (verdict Almost)** — `PHASE_4_IMPLEMENTATION_COMPLETE`. Review at `docs/progress/cutdown-phase-4-review.md`. Decisions D-47 (refuse subject_reframe/split_screen), D-48 (caption wrap = min(ruleset, geometry)), D-49 (libass system-font fallback) appended. SEVEN residuals carried forward, none silent — most important: (1) five throw paths still orphan a render with no report beside it; (2) NO test covers the runner's gate-refusal branch or `makeQaGateEnv` — the same failure class that let the fail-open lookup survive round 1; (3) `audio.targetLoudnessLufs` is a dead ruleset setting; (4) `MIXED_AUDIO_TIMELINE_UNSUPPORTED` undocumented + untested; (5) `cutdown run` cannot originate the render step. Full list in the review §Residuals
- 2026-07-29 — phase 5: NOT started. Recommended: fresh session (/clear then /go) — plan guidance is one phase per session. Phase 5 depends on 4; clear residuals 1, 2 and 4 at its start, since Phase 5 writes the approval records Phase 4's `requireApproval` already reads
- 2026-07-30 — phase 5: **started** (single-driver /implement). Dependency gate PASS: Phase 4's review is on disk (`docs/progress/cutdown-phase-4-review.md`, verdict Almost) with `PHASE_4_IMPLEMENTATION_COMPLETE` earned, entry gate green (596 TS + 661 Py, 0 fail) and the consumed artefacts present (render-manifest-v1, render-v1, technical-qa-report-v1, qa-waiver-v1, `requireApproval` in skills/render) — the entry-gate+DoD clause, same as Phase 3→4. Checkpoint stash `claude-jig checkpoint: cutdown phase 5` saved. First work: clear Phase-4 residuals 1, 2 and 4 per the review's closing note.
- 2026-07-30 — phase 5: PHASE-4 RESIDUALS 1, 2 and 4 CLEARED. (1) The four hoistable QA inputs — ruleset, safe-zone overlay, `--audio-events`, and `--waiver` files (now schema-validated in `loadWaivers`) — load BEFORE `plan()`, so bad input can no longer orphan a render; the one input that cannot be hoisted (`measureRender` measures the render) is guarded by `commitUnmeasuredReport`, which commits manifest+render+a fail-closed `container_corruption` blocker report before re-throwing. Proof is timing as much as assertion: the refusal tests return in ~400 ms where a real render takes ~4.4 s. (2) `stopReason === 'gate-blocked'` now has two runner tests (a refusal mid-pipeline proving the refused skill is never invoked and never logged, and a first-step refusal proving `advanced: 0`), and `makeQaGateEnv` has 7 — ULID-latest selection with the newest directory written FIRST so ordering can't pass by accident, the no-report THROW, tier separation, and stray non-directory files. (3) D-50 appended (refuse a mixed audio/silent timeline, never synthesise). Render skill 9/9, workflows/local 18/18, new cli suite 7/7.
- 2026-07-30 — phase 5 Task 1 **complete**: `review-decision-v1.json` (+4 fixtures, both languages regenerated, validate:contracts 36 cases PASS) — subject is the reviewed draft render PLUS its EDL, manifest and planHash; `decision` is a nested tagged union so a rejection CANNOT exist without a reason; no package field and `additionalProperties:false` makes that structural. `packages/contracts/src/reviews.ts` is the SINGLE latest-decision resolver (total order on `(decidedAt, reviewDecisionId)`; the latest decision wins whatever it says, so a later rejection overrides an earlier approval) shared by approve/render/package/status — the same one-implementation discipline as `range-check.ts`. `skills/approve` (13 tests): refuses a final-tier subject, a QA-blocked draft (D-35 blockers are non-waivable BY ANYONE, which is what stops the direct CLI call bypassing the runner gate), a nameless approver and a reasonless rejection; a rejection of a QA-failed draft is ALLOWED. History preserved as one file per decision. Phase 4's `requireApproval` rewritten onto the resolver: `FINAL_RENDER_REJECTED` (quoting the reason) is now distinct from `FINAL_RENDER_NOT_APPROVED`, and an approval given for a different EDL is refused explicitly (render 11 tests). NEW GATE: entry to the final render is approval-gated (`REVIEW_REJECTED`/`REVIEW_DECISION_MISSING`/`REVIEW_DECISION_UNREADABLE`) because a REJECTION is a successful `approve` invocation — the run log advances the job to `final-rendering` either way, so something must read the decision's CONTENT before the most expensive irreversible step (workflows/local 25 tests). `reviewDecisionInForce` is a REQUIRED env member, per the Phase 4 lesson about optional gates. Runner request builders added for render(draft)/render(final)/package — Phase 4 residual 5 closed; `approve` stays `awaiting` BY DESIGN (a runner that could originate an approval would be an auto-approval path).
- 2026-07-30 — phase 5 Task 6 **complete**: `packages/editorial/src/review-payload.ts` (12 tests) — REQ-110's data with no UI, assembled from committed objects only. Deliberately schema-less: it is a DERIVED view, and a schema would be a second source of truth for fields that already have one. Never fabricates: every absent input is `{value: null, reason}`, and the rights summary reports the WEAKEST state with `unknown` ranked BELOW `expired`/`restricted` (an absent record is the worst case, REQ-003) — an asset with no record counts as unknown so a cut whose rights are unrecorded cannot summarise `cleared` off the one asset that has a record; an unrecognised state refuses to rank rather than defaulting. Written by the DRAFT render into `reviews/pending/<renderId>.json` and surfaced as `reviewPayloadPath` (required-but-nullable) — best-effort by design: a missing lineage costs a stderr warning, never a successful render. Render tests 13/13, including the degraded path. Test-helper defect found and fixed while proving it: `execFileSync` returns stdout ONLY, so a warning written to stderr by a SUCCESSFUL run was invisible to the suite — switched to `spawnSync`.
- 2026-07-30 — phase 5 Task 2 **complete**: `content-package-v1.json` + `enums/package-release-state.json` (+1 valid, 3 invalid fixtures; validate:contracts 40 cases PASS). Two states made UNREPRESENTABLE rather than merely rejected: `qa.gateStatus` has no `fail` member and `qa.blockerCount` is `const: 0`, and `rangeValidation.status` is `const: "ran"` — a package carrying a blocker or an unrun range check is not a thing that can exist. REQ-088's Phase-1 items (derivatives, stems, post copy, hashtags, alt text, OTIO) are ABSENT rather than present-and-null, so a Phase 1 addition is a compatible change and an empty field can't read as "none needed". `packages/contracts/src/contract-set.ts`: exit criterion 3 is computed from the packages' own immutable `contractSet` (a `majorVersion` that moved IS a breaking change, per tech-spec §3) rather than joining a separate mutable contract-change log — one place for the criterion to live, so it cannot disagree with itself. `packages/renderer-core/src/stills.ts` keeps FFmpeg spawning inside the one sanctioned module (`-ss` AFTER `-i` for exact-frame seek; `.partial.png` not `.png.partial`, the same muxer-inference trap Phase 4 hit). `skills/package` (22 tests): 15 fail-closed refusals, each leaving NOTHING behind (asserted — no half-package, no staging dir), including EDITORIAL_DIVERGENCE (the check that makes "the delivered cut is the cut that was approved" a fact), a blocker re-derived from `findings` past a hand-edited `gateStatus: "pass"`, an attempted blocker waiver, a skipped range check, and `unknown` rights refused on the same footing as `expired`. Bundle is staged + renamed (atomic); packages are immutable (a re-package mints a new id, proven). CLI `approve`/`package` wired; `findJobForRender` added. Defect found while proving it: bare backticks inside the USAGE template literal terminated the string.
- 2026-07-30 — phase 5 Task 3 **complete**: `packages/editorial/src/revise.ts` (9 unit tests) + `skills/revise` (7 recorded-fixture tests, no socket). Narrowness is DETERMINISTIC: `selectTarget` maps a closed constraint vocabulary to the widest object any constraint forces, and the model is explicitly told not to choose — "which object do we rewrite" decides how much approved work is discarded, and a sampler would occasionally answer `creative-brief` for a typo fix while producing a pipeline that still looked valid. The narrowness proof is taken from DISK (a second EDL landed; the creative-briefs/story-plans/index/moments snapshot is byte- AND mtime-identical), not from the result's own claim. REQ-112's checkability is enforced by requiring each constraint's `sourceText` to be a VERBATIM substring of the note — an exact check with no threshold to argue about, since a paraphrase would let a constraint the reviewer never asked for look like one they did. `reindexed` is `const: false` in the output schema, so a re-indexing revision is unrepresentable. Ambiguity → `needs_confirmation` before anything is applied (a half-understood note must not be half-applied to an approved cut); a wide target → refused with the owning command named; a paraphrased interpretation → exhausts the single D-32 repair retry and writes nothing. Contract validation caught FOUR schema-invalid enum values in my own Phase-5 test fixtures (`objective: 'awareness'`, `hookFamily: 'curiosity_gap'`) — fixed across all four suites.
- 2026-07-30 — phase 5 Task 4 **complete**: `skills/meta-schema.json` (strict, `additionalProperties:false`, D-15) + `cutdown skills sync [--check]` + generated `skills/registry.json` + the 10-skill `.claude/skills/cutdown-*` mirror (23 tests). The decision layer (`checkFrontmatter`, `registryEntry`, `mirrorBody`) is a PURE function so every rejection is testable without writing into the repo's own `.claude/`. Fails closed BEFORE writing anything — a half-written registry would describe a skill set that does not exist. Idempotent by content comparison (a second run: "no changes"); `--check` is entry-gate-able. Mirror names prefixed `cutdown-` because this repo already installs pack skills called `review`/`plan`/`validate` and a collision would be resolved by whichever loaded last. **The strict schema caught a real defect on its first run**: `skills/index` declared `sideEffects: [..., calls-model]`, which is not one of the three values tech-spec §6.1 fixes — fixed the frontmatter to `network` (which already covers the VLM call) rather than widening the schema. ROUND-TRIP PROVEN LIVE: the generated `cutdown-*` skills now appear in the agent's own available-skills list, which is tech-spec §15 step 9's *Done when*.
- 2026-07-30 — phase 5 Task 5 **complete**: `cutdown status --phase0` (18 tests) computes all four PRD §15 criteria from committed ContentPackages and NOTHING else (D-36) — a claim not in a package is not counted. Criterion 3's "last ten" needs a decidable order, so packages sort on `(envelope.createdAt, contentPackageId)` — total and tie-free, the same rule as the decision resolver. An empty or single-package window reports **UNPROVEN, not proven by absence**. Exit criterion 3 distinguishes a MAJOR bump (breaking) from a content-hash move under an unchanged major (compatible) — conflating them would let a schema comment typo reset the ten-output clock. D-38's two milestones are separate fields with separate reasons, and `PIPELINE_IMPLEMENTATION_COMPLETE` states plainly that it reports the PIPELINE half only (the six implementation gates live in docs/progress/, not in package evidence). Tested against the hand-computed 20-output scenario (7/7/6 across three accounts, 3 fixtures excluded, 2 warning-waived counted separately) plus every red path: 20 FIXTURE packages leave criterion 1 red while still earning the pipeline milestone; a schema major bump inside the window goes red and NAMES the schema; an older bump falls out of the window; skipped/violating/zero-count range evidence, uncleared rights, a hand-edited `gateStatus:"fail"` and a blocker all go red, and a package carrying a blocker is EXCLUDED from the real count. Test defect found while proving it: the fixture derived `createdAt` from an id counter that advances several times per package, so on-disk order did not match write order and the window came out scrambled — criterion 3 is entirely about ordering, so a fixture whose order is incidental cannot test it.
- 2026-07-30 — phase 5 SELF-REVIEW found and fixed one real weakness before the gate: `loadAppliedWaivers` in the package skill parsed-and-CAST waiver files (`as never`), so a file carrying only `waiverId` was indexed and its missing `approvedBy`/`reason`/`waivedAt` reached the package as `undefined` — the package's own contract validation would have caught it, but with an error pointing at the package rather than the file. Waivers are now Ajv-validated against `qa-waiver-v1` and a schema-invalid one is treated as ABSENT so the refusal names it by id (+1 test, 23 in the package suite).
- 2026-07-30 — phase 5 **ENTRY GATE GREEN**: `build:contracts --check` PASS (both generated trees current); `validate:contracts` PASS (**40** fixture cases, 0 lint violations, 0 cross-validator disagreements; Phase 4 baseline 32); `skills sync --check` PASS (10 skills, registry + mirror current); `test:skills` 6/6 suites; `pnpm -r test` exit 0, **721 TS tests / 0 fail** (Phase 4 baseline 596 → +125) before the self-review fix, re-run after it; Python **661 passed / 0 fail** in 548s — unchanged from Phase 4, as expected since Phase 5 touched no Python. Boundary check: `git status` shows changes ONLY under `cutdown/`, `docs/`, and `.claude/skills/cutdown-*` — the last being exactly what Phase 1's Out of Scope deferred to Phase 5.
- 2026-07-30 — phase 5 reviewer gate requested: `code-reviewer` (the authorisation chain, fail-closed package refusals, ids reaching paths, status honesty, tests that cannot fail, schema subset) + `security-reviewer` (path traversal from ids AND from paths stored in artefacts, the new FFmpeg still-frame call, the two destructive `rmSync` paths, notes-into-prompt, secrets). Per CLAUDE.md's Critical-Path table and tech-spec §14, cutdown touches NONE of the four UGC Critical Paths, so the generic reviewers apply — never zero reviewers.
- 2026-07-30 — phase 5: ENTRY GATE re-run after the self-review fix and two added tests — `pnpm -r test` exit 0, **724 TS tests / 0 fail** (Phase 4 baseline 596 → +128). New coverage closing acceptance criteria directly: `drives §15 steps 8-9 in order` asserts the exact 9-step call sequence through two REAL gates and then replays with draft QA withheld, proving the job parks at `review` with `approve` never invoked and packaging unreachable — the gates enforce the order, not the script. Also added: `approve` refuses even a REJECTION when no QA report exists (an honest limit, locked in — `subjectPlanHash` is required and the plan hash lives on the report, so recording a decision would mean inventing the identity it attaches to).
- 2026-07-30 — phase 5 reviewer round 1: **BOTH REVIEWERS BLOCK**. security-reviewer: 1 HIGH + 2 MEDIUM + 3 LOW/INFO. code-reviewer: 1 BLOCK + 10 CHANGE + 8 NOTE. The gate earned its keep twice over.
- 2026-07-30 — phase 5 SECURITY HIGH FIXED — **the Phase 2 path-traversal HIGH had recurred on the TypeScript side.** `jobDir()` in `@cutdown/skill-runtime` built a path from a caller-supplied `jobId` with NO guard, and the only check (`assertSafeJobId`) lived in the CLI where two documented callers bypass it: `cutdown skills run --job <safe>` passes the request file through UNMODIFIED (so the request's own `jobId` never met the assertion), and every `SKILL.md` declares a directly invocable `entrypoint`. Verified empirically that both separators escape (win32 join of the jobs root with a `..\..\..\evil` id resolves to `C:\evil`). Fixed at the point the path is BUILT — `assertSafeId` + `assertContained` inside `jobDir()`, the same pair the 2026-07-21 ledger records for `harness.py` — plus a `pattern` on `jobId` in all ten skill input schemas as defence in depth. The fix also closes the same hole in `ingest`, which had an UNCONDITIONAL out-of-tree write. 22 new containment tests.
- 2026-07-30 — phase 5 SECURITY MEDIUM FIXED — paths and ids read out of STORED artefacts reached `copyFileSync`/`readJson` unvalidated in `package`: a stored `outputPath` of `../../../<x>` would have copied a file from outside the job into a bundle whose whole purpose is to be handed to a client. Two fixes: `readContract` now Ajv-validates render/manifest/qa-report on read (which closes every ULID-derived case at once, since those ids cannot satisfy the `Ulid` pattern), and `resolveJobRelative` guards the three path fields that carry no pattern. The reviewer also CORRECTED my brief's hypothesis: joining with a split value does NOT reset on an absolute component and a UNC value collapses to a subdirectory — `..` traversal was the live vector, not the Phase 2 UNC/NTLM variant. Verified both empirically before fixing.
- 2026-07-30 — phase 5 CODE-REVIEWER BLOCK FIXED — `compareDecisions` ordered `decidedAt` with a **string** comparison while `review-decision-v1` types it `format: date-time`, which admits a UTC offset. So `2026-07-31T04:00:00+10:00` (18:00Z) sorted AFTER `2026-07-30T20:00:00Z` while being genuinely EARLIER — an approval could outrank a later rejection, and the render skill, the package skill, the runner gate and `status --phase0` would then all have authorised a cut a named human rejected. I wrote a "total order" docstring and then implemented a lexical compare on a field whose format is not lexically sortable. Now compares INSTANTS, and a decision whose `decidedAt` is unparseable is refused as a candidate rather than sorted arbitrarily (a value like "yesterday" would otherwise have won every comparison). Encoded as a test with the exact offset pair.
- 2026-07-30 — phase 5 round-1 CHANGEs fixed: `loadReviewDecisions` now Ajv-validates every candidate against `review-decision-v1` (the key-presence check admitted `decidedBy: null` — an anonymous approval, the one thing D-9 forbids — and a non-ULID subject id usable as a path fragment); `approvedDraftManifestId` constrained to a `Ulid` in render's input schema AND asserted before the join; `resolveCoverInstant` now bounded ABOVE as well as below (a tick past the first clip produced a positive offset landing in a LATER clip, and the package recorded a `declared` cover naming the FIRST clip's moment — fabricated provenance for the one field whose tagged union exists to stop exactly that); the revise prompt now states that a `caption_text` instruction IS the replacement text (the applier writes it verbatim, so "shorten the opening" would have been burned into the master) and `instruction` is capped at 200 chars; a revised EDL now mints a FRESH envelope and the revise call's own `modelProvenance` instead of inheriting `plan`'s (which `package` copies straight into the delivered package); `status --phase0` validates packages against `content-package-v1` on read (a package with an empty `approval` object crashed the command with a TypeError instead of being reported), its overstated "re-checked anyway" comment now says what the code actually checks, and criterion 3 now counts a REMOVED contract as breaking (`diffContractSets` hands that judgement to the caller and the caller was not making it); `subjectPlanHash` is now actually COMPARED — against the DRAFT's report, since a final's plan hash necessarily differs — so the schema no longer documents a check that does not exist.
- 2026-07-30 — phase 5 round-1 destructive-path fix: `skills sync` orphan removal required only a `cutdown-` name prefix, on a recursive force-delete in a root OUTSIDE the self-rooted workspace that also holds the pack's own hand-written skills — CLAUDE.md golden rule 3 enforced by naming convention alone, and the one branch of the command with no test. Removal now needs POSITIVE evidence this command wrote the directory (the generated banner in its `SKILL.md`), an explicit `--prune`, and a direct-child assertion; `withFileTypes` replaces `statSync` (which threw on a broken symlink and killed the whole sync, and followed symlinks). The mirror root is injected so the destructive branch is testable: 11 new tests, including "REFUSES to remove a hand-written cutdown-* mirror even with --prune".
- 2026-07-30 — phase 5 round-1 NOTEs closed: the stale `reviews.ts` comment corrected (it claimed name-pattern filtering; the code filters `isFile()`); the final QA report's `renderId`/`renderManifestId` now cross-checked against the render it sits beside (a report's LOCATION is not evidence of its subject); the staged master is now hashed and compared against `render.contentHash` (the package RECORDS that hash, so a truncated master would have shipped with a hash that does not describe it); the `contractSet` description corrected to say packaging-time; `recordedModelPath` contained to the skill directory (an unconstrained caller-supplied path reaching `readFileSync` is an arbitrary-file read).
- 2026-07-30 — phase 5: contract validation on read exposed FOUR partial test fixtures that were testing shapes the pipeline never produces — a render `captions` block missing `cueCount`, a manifest with an invented `media.tier` plus `libx264` and an incomplete `encoderSettings`, QA findings missing all four REQ-106 fields, and a one-field rights stub against a 16-field `RightsRecord`. All corrected to the real contracts. Two `status` tests were RECONCILED rather than patched: a skipped range check and an empty rights manifest are now UNREPRESENTABLE (`const:"ran"`, `minItems:1`), so such a file is reported as unreadable rather than counted as "a package missing evidence" — the stronger outcome, and the assertions now say so.
- 2026-07-30 — phase 5: **/cutdown-propose CONVERSATIONAL ROUND-TRIP RUN** (plan verification step 5, which the code-reviewer correctly noted had never been executed). Followed the generated mirror's four steps literally: read `schema/input.json`, authored a request, wrote it to the corrected repo-relative `cutdown/project-data/jobs/<job>/requests/<ulid>.json`, ran `cutdown skills run`. Both arms proven: `propose` surfaced a structured `BRIEF_NOT_FOUND` (exit 3) for an empty job, and a full POSITIVE round-trip through `brief` committed a JobBrief and returned its path, content hash and an honest no-CTA warning (exit 0). The rejection arm was exercised too — my first request invented a `cta.rationale` field and got exit 2 naming `cta.text`, which is step 1's "ask, don't invent" rule working.
- 2026-07-30 — phase 5 ENTRY GATE GREEN after all round-1 fixes: `build:contracts --check` PASS; `validate:contracts` PASS (40 cases, 0 lint, 0 disagreements); `skills sync --check` PASS; `pnpm -r test` exit 0, **761 TS tests / 0 fail** (Phase 4 baseline 596 → +165; 724 before the fixes → +37 new tests). Python untouched at 661.
- 2026-07-30 — phase 5 reviewer round 2 (verification): **BOTH REVIEWERS BLOCK AGAIN.** security-reviewer: round-1 HIGH CLOSED as scoped (the chokepoint holds; win32 edge cases — trailing dots/spaces, reserved device names, ADS, long-path prefixes — verified empirically against the real filesystem) but PARTIAL as a class, plus 1 NEW HIGH + 2 NEW MEDIUM + 4 LOW. code-reviewer: the BLOCK's stated defect CLOSED, but 1 NEW BLOCK (the inversion round 1 warned about), 1 REGRESSED fix, 4 tests that could no longer fail, and 5 untested round-1 fixes. Per /implement the gate caps at two rounds; both round-2 BLOCKs and every CHANGE were fixed anyway — a BLOCK on the authorisation chain is not a residual.
- 2026-07-30 — phase 5 round-2 SECURITY HIGH FIXED — **the round-1 fix built the right chokepoint and then routed three id fields around it.** `skills/validate` was the worst: a caller-supplied `edlId` reached `readFileSync` AND two `writeJsonAtomic` calls (which `mkdir -p`), so `../<other-job>/edl/<x>` read another job's EDL and wrote a gate result INSIDE that job's directory, creating directories on the way — cross-job (cross-client) write from a documented surface, and `validate`'s schema had gained the `jobId` pattern in that very fix while leaving `edlId` at `minLength: 1`. Fixed with `assertSafeId` + a `Ulid` pattern + both write paths through `resolveJobRelative`; 5 tests including "writes NOTHING into the victim job" and "still accepts a legitimate ULID" (the Phase-4 lesson: a guard must not break the ordinary path).
- 2026-07-30 — phase 5 round-2 SECURITY MEDIUMs FIXED — (a) `asset.storedPath` reached an FFmpeg input path in `render` with no containment. `assertJobRelative`'s OWN docstring names that exact field, and the guard had been applied only in `package` — so the invariant held in the Python worker (`assert_safe_media_path`) and not in TypeScript. A traversing value would have burned content from outside the job into a master about to be handed to a client. (b) `revise` joined `render.edlId` read from an artefact, `plan` joined `creativeBriefId`, `render` joined `edlId` — all now `assertSafeId`'d and ULID-patterned. Also closed the LOWs: the unguarded `apps/cli/src/paths.ts` `jobDir` twin (no live exploit, but "every caller happens to call the guard first" is not an invariant), `assertContained` being lexical-only where the Python twin resolves symlinks (added `assertContainedPhysical`, now used by `resolveJobRelative` since every caller reads or copies), the ADS colon, and the banner ownership test being a bare `includes` (a hand-written skill QUOTING the banner would have been pruned).
- 2026-07-30 — phase 5 round-2 CODE BLOCK FIXED — **the inversion round 1 explicitly asked about, and my round-1 fix widened it.** `ApprovalResolution` carried `rejectedFiles` on the `none` arm ONLY, so when an approval resolved, an unreadable REJECTION was invisible and render/package/the runner gate/`status --phase0` all authorised a cut a human may have rejected. Ajv validation made the drop window WIDER than the key-presence check it replaced: a rejection carrying an RFC-3339 leap second (`23:59:60Z`) is contract-valid and `Date.parse`-NaN, so it vanished. Fixed by adding an `indeterminate` arm — ANY unreadable file under `reviews/` makes the answer indeterminate before any decision is examined, because the SET is what determines "latest", so an incomplete set cannot determine it. All three callers handle it. Also made `compareDecisions` total (it was non-antisymmetric for NaN: `cmp(bad,good)` and `cmp(good,bad)` both returned 1) and wired up `decidedAtInstant`, which was dead code the docstring claimed was in use.
- 2026-07-30 — phase 5 round-2 REGRESSION FIXED — my round-1 mirror-path fix moved the breakage from step 2 to step 3: `pnpm -C cutdown` runs with cwd=`cutdown/`, so a `cutdown/`-prefixed `--input` double-prefixed to `cutdown/cutdown/...`. My earlier "round-trip proven" was run from inside `cutdown/` with a `project-data/...` path — i.e. I proved the wrong thing. The mirror body now names BOTH roots explicitly (Read/Write are repo-relative, CLI args are `cutdown/`-relative) and warns about the exact ENOENT. Re-verified properly this time: from the REPO ROOT, following the mirror literally, `brief` committed a JobBrief and returned its path, hash and no-CTA warning.
- 2026-07-30 — phase 5 round-2 honesty fixes: `skills sync` printed "NOTHING was written" after writing (an unowned orphan is discovered AFTER the write phase) — now reports what actually happened; `comparePackages` still string-compared `envelope.createdAt` while its comment claimed parity with the fixed `compareDecisions` — now compares instants; the `subjectPlanHash` check was `existsSync`-guarded with no else, so deleting one file silently disabled the strongest post-approval check — now refuses; `render-manifest-v1.media.source` was derived from the tier alone, so a draft that fell back to the original recorded `proxy` — a false provenance claim in the artefact, now the media actually used.
- 2026-07-30 — phase 5 round-2: FOUR `status` tests could no longer fail (validate-on-read made every subject schema-invalid, so each passed because the file was unreadable, not because the criterion caught it). Reconciled the same way as the two from round 1, and added live coverage for the only two `evidenceGaps`/`rangeGaps` checks a contract-VALID package can still trip (`weakestState: unknown`, and range evidence naming a different QA report). Five untested round-1 fixes now have tests: the two cover-default arms, the 200-char instruction cap, the child EDL's fresh envelope + provenance, the removed-contract drift arm, and the PRODUCTION `reviewDecisionInForce` (previously only its stub was tested — the gate's own docstring records that production wiring is where the last gate went missing).
- 2026-07-30 — phase 5: the ULID pattern caught FIVE more invalid id literals across four suites (`MAL`, `MO`, `MIX`, `SIL` — Crockford base32 excludes I, L, O and U). Every 26-char literal in every test file is now audited against the alphabet programmatically.
- 2026-07-30 — phase 5 ENTRY GATE GREEN after all round-2 fixes: `build:contracts --check` PASS; `validate:contracts` PASS (40 cases, 0 lint, 0 cross-validator disagreements); `skills sync --check` PASS (10 skills); `pnpm -r test` exit 0, **793 TS tests / 0 fail** across all 18 packages (Phase 4 baseline 596 -> +197; 724 after round 1 -> +69 more, all of them tests the reviewers asked for). Python untouched at 661. One fix-introduced test failure was caught by this run and fixed: the anchored banner check was stricter than the mirror-prune fixture's body, so the fixture now reproduces the real generator's anchor exactly and a new case proves a hand-written skill QUOTING the banner is not classified as generated.
- 2026-07-30 — phase 5 **COMPLETE (verdict Almost)** — `PHASE_5_IMPLEMENTATION_COMPLETE`. Review at `docs/progress/cutdown-phase-5-review.md`; master-plan row 5 updated. All six tasks delivered plus Phase 4 residuals 1, 2, 4 and 5. D-50 appended. SIX residuals carried forward, and **residual 1 is that no THIRD verification round ran** — the round-2 BLOCKs and CHANGEs were fixed after the two-round cap, so those fixes have my own tests behind them but not an independent adversarial pass. Given that round 2 found round 1's fixes had created a new BLOCK *and* a regression, that is the residual most likely to be hiding something: Phase 6 should open with a round-3 pass over this diff. Other residuals: the mirror writes above the self-rooted workspace (spec-sanctioned by tech-spec §2 but a Stage C extraction loses it); `render-v1`'s path fields still carry no pattern (tightening is a BREAKING change to a Phase 4 contract, so it belongs in a deliberate bump, not a security fix); `propose`/`plan`/`validate` still take unbounded fixture paths where `revise` is now contained; `packagesMissingEvidence` is nearly unreachable after validate-on-read and should be pruned or annotated; and four known Phase-0 limits including two Windows-symlink test skips that say so.
- 2026-07-30 — phase 6: NOT started. Recommended: fresh session (/clear then /go) — plan guidance is one phase per session. Phase 6 depends on 5; its dependency proof is the review above. **Open Phase 6 with a round-3 verification pass over the Phase 5 diff** (residual 1) before any new build work.
- 2026-07-30 — phase 5 reviewer round 3 (verification, cap lifted at the user's request): **BOTH REVIEWERS BLOCK.** The residual was right to exist — round 2's fixes had created a **CRITICAL outage** and left a second fix connected in name only. security-reviewer: 1 CRITICAL + 2 MEDIUM + 2 LOW; code-reviewer: 2 BLOCK + 5 CHANGE + 5 NOTE. Round 3 also confirmed every round-2 finding closed except one, and that the entry gate was **green through both BLOCKs** — because neither path had a test.
- 2026-07-30 — phase 5 round-3 CRITICAL FIXED — **my round-2 `indeterminate` arm made every real job permanently unpackageable.** `loadReviewDecisions` treated every `*.json` directly under `reviews/` as a candidate decision, and `validate` (pipeline step 5 of 9 — so EVERY job) wrote `<edlId>-gate.json` and `<edlId>-critic.json` there. So with a valid human approval on disk: `render --tier final` threw, `package` threw, the runner gate blocked, and steps 7–9 were unreachable — making two of the four Phase-0 exit criteria permanently unmeasurable. On the happy path, no attacker involved. The printed remedy told the operator to delete required review evidence. Fixed both ways: `reviews/` is now a NAMESPACE scoped to `^[0-9A-HJKMNP-TV-Z]{26}\.json$` (the name `approve` guarantees), and validate's outputs moved to `reviews/gates/` per tech-spec §9.1, which was already ReviewDecision-only. Lesson: **a control with no recovery path is an outage wearing a control's clothes** — fail-closed still has to leave a way forward.
- 2026-07-30 — phase 5 round-3 BLOCK-2 FIXED — `media.source` provenance: round 2 declared `usedSourceOriginal`, wrote the comment describing it, detected the proxy-less fallback, warned on stderr — and **never assigned the variable**. It occurred exactly twice in the file: the declaration and the read. So behaviour was byte-for-byte the pre-round-2 version and the comment was false. Third instance this phase of a comment claiming a property the code lacked; the difference is this one was written *as the fix for* the property it did not implement.
- 2026-07-30 — phase 5 round-3: the sibling-id gap in its **third** recurrence, fixed STRUCTURALLY rather than per field. `readContractJson` now lives in `skill-runtime`: `validate` and `render` load the EDL, story plan and CreativeBrief through their contracts, so every `$ref: Ulid` on those artefacts is enforced once at the boundary instead of re-asserted per field. It immediately caught three fixture families that had been invalid all along (`modelProvenance.promptVersion`/`recordedFixture` — invented fields the real emitters never wrote), and the render lineage fixtures are now DERIVED from `packages/contracts/fixtures/*/valid/complete.json` so they stay contract-true by construction rather than by my memory of the schema.
- 2026-07-30 — phase 5 round-3: the four unbounded fixture-path fields (`recordedModelPath`/`boundsPath`/`styleProfilePath`/`queryVectorPath`) contained to the skill directory in `propose`/`plan`/`validate`, matching `revise`. My round-2 residual reasoning — that this was a contract change — was **wrong**; it is code-side and was one `assertContained` call each. The reviewer demonstrated a file-read oracle against `cutdown/.env`: an unbounded path plus an echoed `JSON.parse` message quotes the first bytes of any file. Both halves closed, and the test writes a real canary file so the "does not quote the content" assertion can actually fail.
- 2026-07-30 — phase 5 round-3: `assertContainedPhysical` permitted a **write** through an in-job symlink — `realpathSync` throws on the missing leaf, and the old code returned on that. Now walks up to the nearest existing ancestor, which is what carries the link. Asserted through a Windows **directory junction** (unprivileged) rather than a file symlink (EPERM here), because round 3 found the security-relevant escape case was asserted NOWHERE while the suite reported PASS with 2 quiet skips.
- 2026-07-30 — phase 5 round-3: `.gitattributes` added pinning `eol=lf`, and the banner anchor + `writeIfChanged` comparison made newline-insensitive. With `core.autocrlf=true` and no attributes file, a fresh clone would classify **every** generated mirror as hand-written (never pruned, all ten reported as golden-rule-3 problems) and leave `skills sync --check` permanently stale on a tree nobody had touched.
- 2026-07-30 — phase 5 round-3: `rejectedFiles` narrowed to the `indeterminate` arm alone. It had been on all four, which read as live policy — but the indeterminate return is unconditional, so on the other three the array was provably empty and every `rejectedFiles.length > 0` branch in render/package/run was **dead code that looked like a control**. The compiler enumerated all six sites. Its test was also a tautology (`Array.isArray` on a field TypeScript already guaranteed) and is now an exhaustive `switch` that fails to COMPILE if an arm changes.
- 2026-07-30 — phase 5 round-3: four comments claimed `status --phase0` reads the review resolver. It does not — it imports no review symbol and documents the opposite; the real fourth caller is `run.ts`. A reader trusting those comments would believe `status --phase0` fails closed on `indeterminate`. Fourth honesty defect of this phase, all mine.
- 2026-07-30 — phase 5 round-3: **the new `artefact-path-discipline` lint found a FOURTH recurrence within minutes of being written** — six unguarded sites (`manifest.captions.{ass,srt,vtt}Path` reaching a bare `join` in `render` and in the FFmpeg adapter, where the ASS file is then a subtitles-filter input). Root cause was structural: the guards lived in `skill-runtime`, which `renderer-ffmpeg` does not depend on. The pure guards now live in `@cutdown/contracts` (the right home — "this field is job-relative" is a *contract* statement), with `skill-runtime` re-wrapping them as `SkillError`s so exit-code semantics stay put. Three review rounds missed these; a 90-line lint did not.
- 2026-07-30 — phase 5 round-3 caller coverage: the `indeterminate` arm had **zero** tests in any of the three callers that block on it — only in the resolver's own suite, which is precisely why the CRITICAL shipped. Added caller-level tests in `render`, `package` and the runner gate, each asserting BOTH directions: a ULID-named unreadable file blocks, and `validate`'s gate outputs under `reviews/gates/` do not. One pre-existing runner test used a `broken.json` that is now out of namespace — it had been passing for the wrong reason and asserting the opposite of what the pipeline needs.
- 2026-07-30 — phase 5 round-3 entry gate re-run: `npx tsc --build` clean, **805 TS tests / 0 fail** (793 → +12), `build:contracts --check` PASS, `validate:contracts` PASS (40 cases), `skills sync --check` PASS, `ruff` clean, Python suite clean, and `pnpm audit` — never run for the 103 added lockfile lines — reports **no known vulnerabilities**.
- 2026-07-30 — phase 5 reviewer round 4 (verification, cap lifted): security **NEEDS CHANGES** (up from BLOCK — 4 of 5 round-3 fixes verified correct, incl. the ULID namespace regex checked character-for-character against `timecode-v1`'s `Ulid`, and the ancestor walk verified live against a real Windows junction); code **BLOCK**. Both round-3 BLOCKs confirmed genuinely closed. The failure pattern did not repeat as a new CRITICAL, but it repeated in its older form: **my round-3 fixes enumerated FIELDS rather than classes**, so three sites that needed them didn't get them and one site got a guard that broke a documented production option.
- 2026-07-30 — phase 5 round-4 BLOCK FIXED — **the post-approval integrity check was silently reducible to nothing.** `render` read the approved draft manifest with a bare cast, and `assertFinalMatchesApprovedDraft` iterated `Object.keys(draft)` ONLY — so a draft manifest reduced to `editorialPlanHash` + `captions.captionPlanHash` skipped every remaining comparison and returned `ok: true`, leaving `output` geometry, `fonts`, `platformOverlayVersion`, renderer/ffmpeg versions and `audioMix` unchecked. A final master nobody signed off would pass tech-spec §11. **`package` read the same file for the same purpose through a validating helper 90 lines away** — the fifth recurrence, and the clearest one yet that the fix has to be structural. Two defences now: `readContractJson` on the read, and the comparison scans the UNION of both key sets so an absent key is itself a change. Proved the test can fail (old logic returns `ok: true`, new returns `ok: false` with `fonts`/`audioMix` named).
- 2026-07-30 — phase 5 round-4: **a security "fix" of mine broke a documented production option.** I contained `styleProfilePath` to the skill directory along with the two recorded-fixture overrides, but it is not one of them: `cutdown validate --style-profile` is documented, the real profiles ship at `data/style-profiles/*.yaml`, and their `prohibitedClaims` feed the BLOCKING prohibited-claim gate — so every legitimate profile was refused with `PATH_ESCAPES_ROOT` and the gate ran with fewer prohibitions than the brand declares. Worse, `validate`'s loader was JSON-only, so the shipped YAML had never parsed at all. Reclassified: containment kept for `recordedModelPath`/`boundsPath`/`queryVectorPath`, and the oracle closed the OTHER way — both `validate` and `render` now load through `@cutdown/style`, which parses YAML and validates against `style-profile-v1`, reporting `instancePath`/`params` and never instance values. **Second time this project has learned "a guard must not break the ordinary path".**
- 2026-07-30 — phase 5 round-4: the sibling-id defect at recurrence **five and six** — `plan`'s `loadMoments` bare-cast Moment records whose `assetId` (a `$ref: Ulid`) becomes `assets/<assetId>.json`, in a file the round-3 fix had already edited; and `package` bare-cast a `MasterStoryPlan` one line below a contract-validated EDL read. Both now go through the boundary. Added `validateContract` for the array case, because a `moments/*.json` file is a LIST and the array is not itself a contract object — validating the document would have proven nothing about the ids.
- 2026-07-30 — phase 5 round-4: `render`'s `styleProfilePath` and `audioEventsPath` were unbounded AND echoed `JSON.parse`'s message — a demonstrated ~10-byte read of any file plus a clean existence oracle, from a request the `.claude` mirror authors from free text. Round 3 fixed this shape in three sibling skills and skipped `render` entirely. Also deduplicated: `package` carried its own copy of `readContract` (same name, same signature, copy-pasted message text, private Ajv) which still echoed the parser message long after that shape was suppressed elsewhere — now the shared helper, with a local Ajv kept only for the two checks that genuinely need raw non-throwing access.
- 2026-07-30 — phase 5 round-4: `reviews/`'s `entry.isFile()` used lstat semantics, so a SYMLINKED decision was neither read nor reported — a third state the module's absolute rule did not cover, and if the link pointed at a rejection an older approval won. That is the round-2 inversion again, through a different door. Symlinks are candidates now; a dangling one becomes a rejected candidate, which is `indeterminate`, which blocks.
- 2026-07-30 — phase 5 round-4: `renderer-ffmpeg` called the moved guards RAW, so a traversing caption path surfaced as `UNEXPECTED_ERROR` exit 3 where the identical guard in `render` gives `UNSAFE_ARTEFACT_PATH` exit 2 — and `asSkillError`'s own docstring claimed exit-code semantics "stay HERE". Wrapped via the adapter's existing `inputError`. The guard move was behaviour-preserving for every consumer except the one it was made for.
- 2026-07-30 — phase 5 round-4: **`.gitattributes` did not cover the files its own header said it covered.** `cutdown/.gitattributes` governs `cutdown/**`, but the generated mirror lives at `.claude/skills/` ABOVE it — so on a fresh clone with `core.autocrlf=true` every mirror would still be misclassified, and `normaliseNewlines` was not a legacy fallback there but the only protection, permanently. Added a repo-root `.gitattributes` (verified with `git check-attr`) and corrected both comments. Fifth honesty defect of the phase, and this one was inside the honesty fix.
- 2026-07-30 — phase 5 round-4 test gaps closed — every one of these was a claim with no assertion behind it, the shape that made both round-3 BLOCKs invisible to a green gate: `media.source` on a proxy-less draft (BLOCK-2's entire content had NO test — deleting the assignment would have left the gate green exactly as last round); `readContractJson`'s "best-effort in ABSENCE only" split in `validate` (absent → gate report still produced; present-but-invalid → named refusal); `render` degrading to a warning on a present-but-invalid brief (the existing test covered only the ABSENT branch); and `approve`'s success-path stderr warning — which its own harness **structurally could not see**, because it used `execFileSync` (stdout only) while `render.test.ts` had already documented that exact trap in a comment. Harness switched to `spawnSync`.
- 2026-07-30 — phase 5 round-4: the new lint's own defects, found by the reviewer reading it adversarially — its multi-line opt-out form was *encouraged by one check and rejected by the other*, its "states a reason" test measured the longest TOKEN (a URL passed, a plain-English sentence failed), and `statSync` threw on a broken symlink, which would take the whole lint down silently. Fixed, and its docstring now enumerates its seven verified blind spots instead of implying it proves the invariant. It is a tripwire, not a proof.
- 2026-07-30 — phase 5 round-4 entry gate: `npx tsc --build` clean, **811 TS tests / 0 fail** (793 → 805 → 811), 2 documented skips, `build:contracts --check` PASS, `validate:contracts` PASS (40 cases), `skills sync --check` PASS, `pnpm audit` clean, `ruff` clean. Boundary re-verified: changes only under `cutdown/`, `docs/`, `.claude/skills/cutdown-*`, `CLAUDE.md`, and the new repo-root `.gitattributes`.

- 2026-08-02 — phase 6: **started** (single-driver /implement, /go full automation). Dependency gate PASS: phase-5 review on disk (docs/progress/cutdown-phase-5-review.md, PHASE_5_IMPLEMENTATION_COMPLETE, entry gate 811 TS + 661 Py green). Checkpoint stash `claude-jig checkpoint: cutdown phase 6` saved. Opening with the proving-run tasks per the phase plan.
- 2026-08-02 — phase 6 Task 1+2 (proving run + drills) **complete on job e2e-mixed-1**. Chain: brief → atomic 6-class mixed ingest (`mixed-job-valid`, hero-still honestly `rights: unknown`) → +promo-take.mp4 (new 27 s flite e2e fixture; the 5 s golden clips yield ONE moment each, below the REQ-036 floor of max(3, variants×2)) → index ×2 → propose (2 briefs, recorded replay) → plan → validate (D-37 gate + critic separated) → draft render+QA → named approve → final render+QA → runner-driven package. `PIPELINE_IMPLEMENTATION_COMPLETE` **EARNED**; `PHASE_0_EXIT_EARNED` correctly red (fixture excluded from real counts).
- 2026-08-02 — phase 6 KILL DRILL 1 (index): killed uv/python tree mid-OCR on the promo asset after transcript/shots/scenes checkpointed; resume served those three from checkpoint (cacheHit=true, ≤32 ms) and ran only OCR-onward. KILL DRILL 2 (final render): killed ffmpeg mid-encode; the skill died with a structured error and NO result file; `cutdown run` resumed with **advanced 0** and the QA transition gate refused the orphaned render dir fail-closed (`QA_REPORT_UNREADABLE` — "a render that exists without a report is never advanced past"); recovery was re-running the render, then `cutdown run` advanced exactly one step (package). Run-log shows zero LLM-stage re-execution after either kill.
- 2026-08-02 — phase 6 DEFECT FOUND BY THE PROVING RUN, FIXED AT THE CAUSE: `render --audio-events` demanded an output-relative `{events:[{kind,startMs,endMs}]}` file while the only producer in the pipeline (`index`) writes `{audioEvents:[...]}` in SOURCE ticks — the documented option refused the only artefact that exists (REQ-104 dead end-to-end; no test drove render from a real index artefact). Fix: `skills/render/src/audio-events.ts` (pure module) accepts both shapes, filters non-meaningful kinds (speech, silence), and projects source ticks through the EDL clips onto the output timeline; 9 unit tests including the cross-timebase (1/16000 event vs 1/15360 clip) and multi-clip-split cases.
- 2026-08-02 — phase 6 gates earned their keep on MY authoring, live: D-37 blocked the first EDL twice (brief durationRange.min 3 s below the D-3 platform floor of 5 s; a clip whose Moment carries `requires_setup` on the overlapping bridge moment the ASR's anaphoric "Then…" created — fixed by re-briefing at 5–15 s and swapping the dependent clip); draft QA failed on 2 caption_overflow warnings (D-48 geometric wrap) — fixed by shortening displayText (in-order subsequence preserved); the residual `unexpected_silence` warning (the fixture's own 1.6 s flite gaps) was waived TWICE by named D-35 records — once per tier, because a waiver is plan-scoped and the final tier is a different planHash. QA semantics confirmed: an unwaived warning FAILS the gate (fix-or-waive-by-name, never silent).
- 2026-08-02 — phase 6 engine facts: whisper transcribed the flite line "cut the whole take down" as "tape" and "are burned in" as "were burned" — quote-fidelity means captions quote the ASR verbatim, not the script; silero VAD's silence-gap turns assigned each utterance its own speaker (speaker_1…speaker_5, all capped ≤0.5 per D-17); `skills run <name>` reports gate failures in the result JSON with exit 0 while the `cutdown render` CLI verb exits 4 — scripts must read gateStatus, not the exit code, on the skills-run surface.
- 2026-08-02 — phase 6 **ENTRY GATE GREEN** (final whole-workspace run): `build:contracts --check` PASS; `validate:contracts` PASS (40 cases, 0 lint, 0 disagreements); `skills sync --check` PASS (10 skills); `pnpm -r test` exit 0, **822 TS tests / 0 fail** (Phase 5 baseline 811 → +11, the new audio-events suite; 2 documented Windows skips); Python **644 passed / 0 fail**; UGC schemas parse; boundary confined to cutdown/, docs/, .claude/skills/cutdown-*. RUFF CONFIG LEAK FOUND + FIXED: cutdown had NO ruff config, so ruff's upward config discovery silently applied the REPO ROOT's UGC selection (E,F,I,UP,B,SIM,RUF) to cutdown Python — 185 findings under a foreign style guide, 0 of them defects. `cutdown/ruff.toml` now declares cutdown's own standard explicitly (correctness core E4/E7/E9/F; widening is a decision, not an accident of directory nesting); the ONE real finding (unused `import sys` in test_bounds.py) fixed, suite re-run 25/25. Prior phases' "ruff clean" claims were vacuous for cutdown Python.
- 2026-08-02 — phase 6 reviewer code-reviewer (round 1): **NEEDS CHANGES** — 3 must-fix + 5 optional. The headline was the project's own lesson repeating in miniature: the new module's docstring claimed "exactly one of the two shapes must be present" while the code silently preferred `events` when both keys appeared (a gate input half-read), with no test on either case; plus two doc citations to evidence not on disk (the master-plan row cited a not-yet-written phase-6 review file; proving-run-real cited before/after status outputs not yet pasted). The projection math, fail-closed paths, no-content-echo rule, golden-set safety and ruff reasoning all verified clean, several live.
- 2026-08-02 — phase 6 round-1 fixes: exclusivity ENFORCED (both-keys refused; a non-array under either key refused even alone) + 4 new tests incl. the empty-array semantics ([] → check runs clean; null → skipped) — suite 12/12, full render suite 30/30, whole-graph build clean; the verbatim before/after `cutdown status --phase0` outputs pasted into proving-run-placeholder §6; `docs/progress/cutdown-phase-6-review.md` written (acceptance walk, gates, deviations incl. the delegated fixture-approval honesty note and the two optional findings deliberately not taken). CLAUDE.md Lesson appended (config-by-upward-discovery + dead-option-without-producer-test), per the session instruction to mark down learnings.
- 2026-08-02 — phase 6 reviewer code-reviewer (round 2, verification): **PASS · Grade A**. All three round-1 findings verified genuinely closed — the reviewer recompiled from source before trusting the tests (30/30 render suite, 12/12 audio-events), hunted the inversion the fix could have caused (the both-keys refusal breaking the producer's happy path) and ruled it out by driving the REAL golden-set index artefact + REAL EDL through the compiled interpreter (0 spans, correctly `ran`-clean since the fixture's events are all speech/silence), and re-ran `cutdown status --phase0` live to confirm the pasted "after" block matches exactly. One note for the pattern book: the review doc's "Ready" was written while round 2 was in flight — backed now, but strictly claimed before earned.
- 2026-08-02 — phase 6 **COMPLETE (Ready)** — **`PIPELINE_IMPLEMENTATION_COMPLETE` EARNED.** DoD: entry gate green (822 TS + 644 Py, 0 fail; contracts/skills-sync/ruff clean), code-reviewer PASS after 1 fix round, proving-run docs written (placeholder run complete; real run BLOCKED-ON-D-27/D-36 — no third state), e2e golden set seeded, cost table with D-21 comparison, D-51 appended, developer-guide §7 addendum, CLAUDE.md Lesson added, master-plan row 6 + this ledger updated. Review at docs/progress/cutdown-phase-6-review.md. Cutdown Phase 0 build is done: all six phases complete; what remains is OPERATIONS (the 20-output real-footage accumulation, owner-gated on D-21/D-27/D-36) — `PHASE_0_EXIT_EARNED` stays honestly red until that evidence exists.
- 2026-08-05 — **FIRST REAL-FOOTAGE JOB COMPLETE**: `schwarzkopf-w1-showcase` — a 27.8 s campaign-showcase cut of the six best-performing Wave 1 posts (selected from ai-results.json by reach + ER), ContentPackage `01KZ8B40TENCWQ72F061FXK79S`, `releaseState: rights_approved`, `sourceClassification: real`, QA pass_with_waivers (0 blockers). `status --phase0` now counts **1/20 approved real outputs across 1/3 accounts** (acct-schwarzkopf-extracare-au-w1); PHASE_0_EXIT_EARNED honestly red. Full record: `cutdown/docs/proving-run-real.md` (replaces the D-27 blocker note per its own instruction). Approval + 4 waivers are DELEGATED under the owner's in-session instruction and say so on their face; the decision notes instruct the owner to watch the draft and supersede if needed.
- 2026-08-05 — real-footage engine facts: PaddleOCR dominates indexing (~3 h for 183 s of fast-cut creator footage; ~1 shot/second keyframe rate, 30–120 s per frame scaling with text density and resolution — recorded for D-17/Phase-1); music-led reels correctly flagged speechless moments unusable and became caption-stat beauty shots; QA caught a glyph-width caption overflow char-count checks would miss; loudness/true-peak drift (0.2 LU / 0.2 dB) is intrinsic to concatenating differently-mastered native-audio sources at Phase 0.
- 2026-08-05 — mid-run feature (user-requested): **index progress heartbeat** — `harness.append_progress` + OCR loop instrumentation writes `index/progress.jsonl` one line per keyframe (best-effort, never fails the work; warns once). Born from a 71-minute silent OCR pass being indistinguishable from a hang. 4 new tests (2 harness, 2 OCR callback threading), ocr+harness suites 75 pass, ruff clean. KNOWN GAP recorded: D-51 audio-events projection doesn't filter by asset for multi-asset EDLs — `--audio-events` omitted for this job (check recorded skipped-with-reason); follow-up is threading assetId through ClipSourceSpan. This diff + the showcase fixtures still need a code-reviewer pass — queued as the session's wrap-up gate.
- 2026-08-05 — showcase-session reviewer code-reviewer: **PASS · Grade A** (0 must-fix, 2 optional). The reviewer re-verified the fixtures against the REAL gate semantics (all five quote captions tokenised-subsequence-checked against the real Moment transcripts on disk), all 10 moment ULIDs/ranges/timebases against the real artefacts, every proving-run-real.md claim against the package/waivers/decision on disk, ledger append-only (85 insertions, 0 deletions), and the four new tests live. Optional note applied: the `progress` callback's must-not-raise contract is now documented at the call boundary. First real-footage job closed reviewed.
- 2026-08-05 — OCR retrospective (user-prompted, and right to be): the 3 h showcase wait was NOT inherent to OCR. Measured on a real frame: shipped config (cpu_threads=1 for byte-identical re-runs, enable_mkldnn=False) = 31 s/frame warm; 8 threads = 17 s/frame; mkldnn=True genuinely crashes (paddle 3.3 oneDNN/PIR `ConvertPirAttribute2RuntimeAttribute` — Phase 2's disable comment reproduced and confirmed true). The 3 h = slow-by-design config × sequential orchestration (my call — assets are independent) × machine contention (a dozen leftover dev-server processes) × ~140 keyframes. My earlier "text density and resolution" explanation was a guess dressed as a finding.
- 2026-08-05 — operator flags shipped: `cutdown index --skip ocr` (only OCR is skippable — the one stage nothing downstream requires; ledger records the skip with reason; skip WINS over force per the --no-vlm precedent; unskippable names refused exit 2 before any asset read) and `--ocr-threads N` (enters the engine config and therefore the REQ-005 cache key; schema documents the byte-identical-re-runs trade-off). 4 new orchestrator tests (37/37), CLI 146/146, tsc clean, sync --check PASS, ruff clean. LIVE PROOF on job `skip-proof`: the 27 s promo asset indexed in **2 m 30 s with --skip ocr vs 75 min with OCR** — same 7 moments, bounds ok. Standing practice adopted: independent assets index in PARALLEL from now on. Reviewer pass running.
- 2026-08-05 — skip/threads flags reviewer round 1 **NEEDS CHANGES** (3 must-fix, all test/validation gaps — headline: the ocrThreads wiring was exactly the 2026-08-02 lesson repeating, an option no test drove from its real producer) → all fixed: `TestOperatorSkipWiring` runs `index_asset` end-to-end with stubbed runners proving cpu_threads=8 arrives IN the real engine config beside shotsDigest (and the real bounds-gate CLI runs un-stubbed); the NameError near-miss path pinned (skip records reason, visual still invoked); Python-side `ocrThreads` guard (OCR_THREADS_INVALID, exit 2, bool excluded, before any asset read — proven by the missing-asset ordering technique) + non-list `skip` refusal. Round 2 **PASS · Grade A** — vacuity of the wiring tests explicitly checked and ruled out. Suite 41/41, ruff clean. Two optional nitpicks recorded, deliberately not taken (SKIP_NOT_SKIPPABLE reused for the type refusal; skip+threads combination silently discards threads).
- 2026-08-06 — **D-52 shipped: editorial transitions (fades)** — the owner asked for "fade in fade out multi window and other common visuals". Scope decided and recorded in D-52: duration-preserving video+audio fades ONLY (`clips[].transition {fadeInMs, fadeOutMs}`, 40–2000 ms), living in the EDL because the edit decision list must carry every edit decision; crossfades (overlap shrinks the timeline → every caption cue and QA duration shifts) and multi-window (parallel-track timeline model; D-47's split_screen refusal stands) deferred BY NAME to their own design pass / the D-16 Remotion tier. Mechanics: platform-edl-v1 1.0.0→1.1.0 compatible bump + changelog + both trees regenerated (validate:contracts 42 cases PASS incl. 2 new fixtures); `skillEnvelope` gained a contract-version param so the EDL envelope states 1.1.0 truthfully; fades append AFTER the aspect-treatment composite and REPLACE (never layer under) the 20 ms boundary click-killer; all arithmetic exact scaled-integer decimal strings; `FADE_LONGER_THAN_CLIP` refuses a pair that cannot fit. 9 new filtergraph/timeline tests (renderer-ffmpeg 63/63), whole workspace 834 TS tests / 0 fail.
- 2026-08-06 — faded showcase LIVE: EDL `01KZ9YBNH1SNSYCF5B3D4C56WA` (400 ms open, 250 ms dip-to-black joins, 600 ms close) passed the editorial gate; draft+final QA profiles IDENTICAL to the hard-cut version (same 4 known warnings, no new findings, loudness unchanged — the duration-preservation claim held on real footage: packaged master exactly 27.800000 s). Approval `01KZ9YFGRDHC32MEBAKSVDEGN8` (delegated, flagged on its face), 4 plan-scoped waivers re-scoped per tier, package `01KZ9YK48KBRAX85DJ1P76NYMN` (`rights_approved`, real). Deliverable copy: `work/.../schwarzkopf-w1-showcase-faded.mp4`. Reviewer pass running.
- 2026-08-06 — D-52 reviewer rounds: round 1 **NEEDS CHANGES** (4 must-fix — the class lesson replaying: `revise` is the SECOND PlatformEDL producer and still stamped 1.0.0 on revisions that preserve transitions; the live model's prompt never mentioned the new vocabulary; a false "plan-hash chain unaffected" claim in four places asserted nowhere; the one-sided audio fade path untested behind a fragile null-check). All fixed at the class: `PLATFORM_EDL_SCHEMA_VERSION` constant in @cutdown/contracts with a drift test pinning it to the schema file, both producers importing it; prompt teaches the vocabulary; claim reworded everywhere to the true property (a fade moves the EDL hash like any edit; the draft→final chain is the invariant) and asserted with the real hashContent; strict `editorialOut` condition + both one-sided audio tests. Round 2: 3 of 4 verified closed; the 4th's DECISION-ROW sentence was stale (my fix summary claimed an edit I had not made — caught) + 2 notes (a reference-equality hash tautology; no producer-level envelope pin in revise's tests). All three applied post-cap with tests behind them: D-52 sentence now describes the shipped mechanism, the hash test compares an independent clone, and revise's test pins `envelope.schemaVersion === '1.1.0'` (7/7). Final suites: contracts 71, renderer-core 94, renderer-ffmpeg 65, revise 7, plan 3 — all green; workspace 838 TS / 0 fail.
- 2026-08-06 — **residual-closure pass started** (post-`PIPELINE_IMPLEMENTATION_COMPLETE`, driven by `/go` "100% completion, best quality"). Triaged the carried-forward residual backlog from the Phase 2/4/5/6 reviews into closeable-now vs owner-blocked. Owner-blocked and untouched: `PHASE_0_EXIT_EARNED` (D-27/D-36 real footage, rights, account ids), `PHASE_3_ACCEPTED_LIVE` (D-21 spend ceiling). Deliberately deferred with reasons: `poor_crop` subject-clipping (needs a subject/face model — Phase 2 residual 1), `render-v1` path patterns (a BREAKING Phase-4 contract change; belongs in a deliberate version bump, Phase 5 residual 1), the grep-shaped `artefact-path-discipline` lint (a tripwire by construction, Phase 5 residual 4)
- 2026-08-06 — Phase 2 residual 3 CLOSED (a real defect, not a tidy-up): `quality.py::iter_luma_frames` broke on a short read and discarded ffmpeg's exit status, so a mid-stream decode failure yielded a TRUNCATED frame series that every run-length detector then measured as clean footage. Now checks `returncode` — but ONLY when the stream ended on its own, so an early-stopping consumer's deliberate `kill()` is not misread as a fault — and drains stderr on a thread (a per-frame complaint on a damaged source can exceed the pipe buffer and deadlock the decode, which reads exactly like a hang). Positive + negative tests
- 2026-08-06 — Phase 2 residual 2 CLOSED: `quality.py` named an unexamined modality instead of omitting it silently. "No video flags" and "no decodable video stream" were indistinguishable, and the ledger read `completed, reason: null` for both. Emits the nested `subStage` record `main.py` already honours (the shape `visual` has used since Phase 2); `source-index-v1.json` already legislated the answer ("the reason a collection is empty lives in the matching `subStages` entry"). 4 tests incl. the control that a fully-examined asset records NO reason
- 2026-08-06 — **D-53 appended** — Phase 2 residual 4 CLOSED. `audio_events.py` synthesised a whole-asset `silence` at `confidence: 1.0` under `ENGINE_RMS` for a stream-less asset while `quality.py` refused to report `silence` for the same asset; Phase 3 now consumes `audioEvents`, so the disagreement was about to become load-bearing. Decided per developer-guide §5 (not an owner-reserved class): an AudioEvent is a DETECTION, so a stream-less asset yields an EMPTY collection plus the ledger reason — the synthesised event named an engine that processed zero samples, which is a false provenance claim. No consumer loses information (D-51's projection already discarded `silence` as non-meaningful)
- 2026-08-06 — Phase 2 minor CLOSED across all THREE mirrors of the guard (the "fix the class, not the field" lesson): `assert_safe_id` / `assertSafeId` / `assertSafeJobId` now refuse Windows reserved DEVICE names. Not a traversal — worse: on Windows `jobs/nul/` opens successfully and DISCARDS every write, so a job would report success and leave no artefact, and D-33 pins Phase 0 to Windows. Found en route: the Python guard — the one the Phase 2 security review demanded because the worker is directly invocable — had **no tests at all**; it has 4 now, including the acceptance half (`falcon`, `nul-check`, `com10` still pass)
- 2026-08-06 — **D-54 appended** — Phase 4 residual 3 CLOSED, structurally. `audio.targetLoudnessLufs` was required by the ruleset loader and read by nothing (the check measures against the MANIFEST's declared target). It survived all of Phase 4 because the "every ruleset field is READ" test drove a HAND-WRITTEN list of 18 of 26 settings — it asserted every field it had been told about, and the field nobody told it about was the dead one. Key deleted (ruleset -> 1.1.0; a ceiling is policy and stays, a target is a per-render declaration and lives in the manifest), and the test now DERIVES its leaves from the shipped ruleset: every one must be perturbed, proven live in the measurement filtergraph (`buildDetectionFilters` extracted and exported so the four FFmpeg-side thresholds are provable at all), or declared not-a-threshold with a reason. Three previously-unperturbed settings gained perturbations
- 2026-08-06 — Phase 4 residual 6 CLOSED both ways, per the project's own "assert it in a test or delete the claim" lesson: `assertDeterministicArgv`'s docblock claimed to check "every tier-1 pin" and omitted `-flags:a +bitexact` — the AAC encoder stamps its own build identifier and `-flags` does not reach it, so an argv carrying every other pin still differs across an FFmpeg upgrade. Fixed the CODE (assertion + its own negative test), not the comment. `checks.ts`'s "every threshold comes from the ruleset, never a literal" was false of three literals; the claim is now narrowed to "every number a verdict TURNS ON" with all three named and justified
- 2026-08-06 — Phase 5 residual 3 CLOSED: `counts.packagesMissingEvidence` annotated per-line with WHY seven of its nine gap checks cannot fire after validate-on-read (`$ref Ulid`, `enum`, `const: 0`, `minItems: 1`, `const: "ran"`, `minimum: 1`) and which two remain REACHABLE. Annotated rather than pruned — they are defence-in-depth for packages that travelled away from the job that minted them — and a new test PINS the eight schema constraints the annotations depend on, plus a control asserting the two live checks stay unconstrained. A schema loosening now fails loudly instead of leaving the comment quietly wrong
- 2026-08-06 — Phase 5 residual 5 CLOSED (two of them): `revise`'s refusal for a non-`text` caption said the clip "carries no display text to rewrite" — true of `none`, FALSE of `quote` (`QuoteCaption` requires `displayText`). The real reason is D-37's quote-fidelity subsequence binding, so an operator was being sent to look for a missing field; the two kinds now refuse separately, each naming the command that CAN make the change. And `skillsSync` gained an injectable `registryPath`: every mirror test was rewriting the REPO's real `skills/registry.json`, a no-op only because the content regenerated identically — luck, not isolation
- 2026-08-06 — **D-55 appended** — Phase 5 residual 2 CLOSED as a recorded note: `skills sync` writing to `<repo>/.claude/skills/` is spec-sanctioned by tech-spec §2 itself (a generated projection INTO the host, not a source dependency ON it — nothing under `cutdown/` reads the mirror), with the Stage C migration stated: the mirror does not travel, the host regenerates it
- 2026-08-06 — **near-miss worth the lesson:** the first "green" full TS run was a false green twice over. (a) `pnpm -r` reported every package `fail 0` while `apps/cli` ALSO printed a failing test — a throw during suite CONSTRUCTION (a top-level `readFileSync` in a `describe`) is not counted in that package's fail tally, so an aggregate built from `ℹ fail` lines alone reads clean; only `ERR_PNPM_RECURSIVE_FAIL` and the `✖` lines showed it. (b) Every TS package runs `node --test dist/tests/**`, so the new `revise` tests added after the last `tsc --build` **did not run at all** and their absence looked exactly like a pass. Both fixed and re-run from a fresh build
- 2026-08-06 — reviewer gate round 1, TWO reviewers, both **NEEDS CHANGES**, and both earned it. `code-reviewer` (Grade B−, 6 should-fix): the headline was a defect I had just introduced — `iter_luma_frames`'s `finally` killed ffmpeg on EVERY successful decode and then judged decode integrity by that exit code. The reviewer MEASURED it (180/180 runs across three fixtures had `poll() is None` at the short read), so only `TerminateProcess` losing a race to an already-exiting process kept the status at 0, and the docstring asserted the exact distinction the code did not draw. Second: the `-flags:a +bitexact` fix left its identical twin live in `packages/renderer-core/src/proxy.ts` — a content-ADDRESSED artefact that encodes AAC, with a comment already claiming tier-1 determinism. `security-reviewer` (Grade B, 3 medium): the three id-guard mirrors are NOT equivalent — Python's `$` also matches before a trailing newline, so `"abc
"` was accepted by the worker and rejected by both TypeScript mirrors, in the one mirror reachable without the CLI; the CLI mirror threw a bare `Error`, which `reportError` classifies as `UNEXPECTED_ERROR` exit 1 **with a stack trace** onto the stream four callers parse; and the new stderr drain was unbounded on untrusted media
- 2026-08-06 — reviewer fixes applied. Decode integrity: the natural-EOF path now WAITS for ffmpeg to exit and reports its own status, killing only if it overstays, while the early-stop path records `killed_here` — a process we killed can never testify about decode integrity. `proxy.ts` now takes `...determinismArgs()` and runs `assertDeterministicArgv` over its own argv (no test pinned a proxy hash, so the byte change is a cache miss, not a break). `_SAFE_ID` anchors with `\Z`; `assertSafeJobId` and `jobDir` refuse with structured `inputInvalid` (`UNSAFE_ID`/`PATH_ESCAPES_ROOT`, exit 2, no stack); the drain keeps a bounded 16 KiB rolling tail. Also: `-protocol_whitelist file` mirrored onto all three of `quality.py`'s ffmpeg/ffprobe invocations (the TypeScript renderer already refuses to rely on ffmpeg's defaults, and golden rule 9 makes those a version-pinned fact); the device check added to `assertJobRelativePath`, which is the CLASS boundary for artefact paths and was the sibling that did not get it; `-threads` presence tightened to a positive integer (`-threads 0` is ffmpeg "pick a number from the machine"); the seven-vs-eight miscount, a D-53/D-54 citation, and the orphaned `runDetectionPass` docblock corrected
- 2026-08-06 — **the guard comment was measurably FALSE and is now measured.** My own justification for the reserved-device rule claimed every such name silently discards writes. Measured on the D-33 machine (CPython, Win11 26200): only `nul`/`nul.` are the device (0 bytes, unlisted); `nul.json`, `con`, `aux`, `prn`, `com1`, `lpt1` are ORDINARY files — and a `nul` DIRECTORY accepts `mkdir` then fails every child write LOUDLY with `FileNotFoundError`, not silently. All three comments and both user-facing messages now state that, the rest are refused for portability across Windows builds and APIs, and `TestWindowsDeviceNamesReallyMisbehave` measures the claim (Windows-gated, with a control) instead of restating it — the project's "assert it in a test or delete the claim" rule applied to my own prose
- 2026-08-06 — **the structural fix the drift asked for:** `packages/skill-runtime/tests/safe-id-cases.json` — ONE case list (14 accept, 42 reject), driven through all three mirrors by `path-safety.test.ts`, `paths.test.ts` and `test_harness.py`. Three deliberate duplicates with three independent suites is how they diverge; nothing pinned them to each other until now, which is why a security review found the trailing-newline gap instead of a test
- 2026-08-06 — **one reviewer finding REJECTED with evidence.** security-reviewer LOW: "`MEDIA_DECODE_FAILED` fails closed with no way forward — an asset with a damaged tail can never complete `index`." Not so: `main.py::_stage` catches `SubStageError` *and* every `Exception`, records the sub-stage `failed` with its reason, returns `None`, and the run continues — its docstring is literally "never abort the run", and it was widened to bare `Exception` for exactly this. The index still assembles with an honest ledger entry, which IS the way forward. `code-reviewer` independently reached the same conclusion. Adding `quality` to `OPERATOR_SKIPPABLE` would have been a change with no problem to solve
- 2026-08-06 — residual-closure pass **COMPLETE**. Verification gate green from a clean rebuild: TypeScript **820 tests / 818 pass / 2 documented Windows skips / 0 fail** (no `ERR_PNPM`, no `x` markers), Python **671 passed / 0 fail** (baseline 644), ruff clean under cutdown's own config, `build:contracts --check` PASS, `validate:contracts` PASS (42 cases, 0 cross-validator disagreements), `skills sync --check` PASS (10 skills), UGC schemas still parse. Net +27 Python and ~+9 TypeScript tests. **No milestone changed**: `PIPELINE_IMPLEMENTATION_COMPLETE` stands from 2026-08-02, `PHASE_0_EXIT_EARNED` still red (D-27/D-36), `PHASE_3_ACCEPTED_LIVE` still blocked (D-21). Record: `docs/progress/cutdown-residual-closure-review.md`

- 2026-08-09/10 — **product-program Stage 0A started and COMPLETE** (single-driver `/implement` via `/go` "full automation, grade A, 100% completion"). Route recorded honestly: the user named `cutdown-master-plan.md`, which is **already 100% complete** (all six phases; `PIPELINE_IMPLEMENTATION_COMPLETE` earned 2026-08-02; residual closure 2026-08-06) — what remains in it is operations, owner-gated on D-21/D-36 and the 20-output accumulation. Its own §Post-completion pointer names the successor, so the buildable continuation is `cutdown-product-program-master-plan.md` Stage **0A** (plan §7a: tasks 1, 2, 15, 16, 17, 18, 19, 20, 21; the contract migration is 0B and untouched). Checkpoint stored non-destructively with `git stash create` + `git stash store`, leaving the working tree intact. Entry-gate baseline green first: 857 TS + 689 Py, contracts/sync PASS, ruff clean.
- 2026-08-09 — Stage 0A delivered: the two missing Cutdown reviewers and their rule canons (`cutdown-measurement-reviewer` + `cd-measurement-honesty`, `cutdown-boundary-reviewer` + `cd-tenancy-boundaries` — the `cd-` prefix is deliberate, because `orphanMirrors` fails `skills sync --check` on any hand-written `cutdown-*` mirror directory); `CLAUDE.md` and tech-spec §14 reconciled so both documents state the SAME gate set (§14 previously said the Critical-Path gates simply "do not apply to Cutdown"; it now says the *UGC* gates do not, and that the exemption is from those gates rather than from gating); the multi-asset audio-event projection (D-51's remaining half) reviewed for the first time; CI (**D-57**, superseding D-33); tech-spec §12's determinism environment; ruff widened (**D-58**); `cutdown doctor`; the master-plan correction.
- 2026-08-09 — **`cutdown doctor` found a real defect in itself on its first live run**, before any reviewer saw it: two probes died with `spawn EINVAL` on the D-33 machine. The cause is a present-day toolchain fact rather than a typo — since the Node 20 fix for CVE-2024-27980, spawning a `.cmd`/`.bat` with `shell: false` throws EINVAL **synchronously**, and the first cut tried `pnpm.cmd` before `pnpm.exe`. Fixed at the cause: resolve the executable on PATH ourselves (PATH × PATHEXT), never attempt a shim, and catch the synchronous throw. A diagnostic that crashes is worse than no diagnostic.
- 2026-08-09 — **the §11 single-spawner architecture guard fired on `doctor.ts`, and the response was structural rather than an exclusion.** tech-spec §11 permits exactly one module to spawn ffmpeg/ffprobe; its enforcing test detects a file that both imports `node:child_process` and names a media binary. `doctor` legitimately spawns pnpm/uv while reporting on FFmpeg. That test's own comment warns an exclusion list is where the next real violation hides, so the spawning machinery moved to `apps/cli/src/tool-probe.ts` and the guard passes because the invariant genuinely holds. **The boundary reviewer then found the split had opened a hole the detector cannot see**: a caller writing `toolVersion` with a media-binary name names it without importing `child_process`, while that module imports `child_process` without naming one — neither half trips the conjunction. Closed at the primitive: `toolVersion` refuses the media binaries outright, importing the list from `renderer-core` (its one home) rather than restating names that would trip the very detector the guard supports.
- 2026-08-09 — reviewer gate: `code-reviewer` round 1 **BLOCK** (1 must-fix, 10 changes, 2 notes), earned. The headline was DEMONSTRATED, not argued: `cutdown doctor` printed a green `OK` for a `uv`/`pnpm` that was present on PATH but unrunnable — with "could not be spawned" in the detail on that same line. `toolVersion` returned an empty version from four different situations and both checks treated all four as a pass. **Absence reported as success, in the one command whose whole job is honest environment reporting.** It survived because NO test called `checkPnpm` or `checkUv` at all. Fixed with a discriminated `ProbeOutcome` (absent / shim / unrunnable / exit-nonzero / timeout / read), and the untested branches now have tests. Also caught: `satisfiesRange` short-circuiting past clauses it documented itself as refusing to answer (returning a verdict it had not computed); a bare-major clause read as exactly `X.0.0`; a raising probe losing its identity; `audio-events` guarding the field rather than the class; the CI Linux leg predicted red (the distro ffmpeg is 6.x against a hard major-8 gate); an unfrozen `uv sync` beside `pnpm --frozen-lockfile`; no `permissions:` block; and D-58 citing counts the repo did not support.
- 2026-08-10 — reviewer gate: **both new Cutdown reviewers ran on the diff that created them**, via the `general-purpose` fallback the plan review log already uses (the agent registry is snapshotted at session start). `cutdown-boundary-reviewer` **NEEDS CHANGES · Grade B** — 0 BLOCK, and every hard rule held: no schema moved, no delivered package became unreadable, `decisions.md` shows insertions only, no unguarded path id, and every D-57 trigger-discharge claim verified against the actual D-39/D-43/D-44/D-45/D-46 row text. `cutdown-measurement-reviewer` **NEEDS CHANGES · Grade C** — 0 BLOCK; nothing turned a criterion green or collapsed an absence into a number.
- 2026-08-10 — the two findings that mattered most were both introduced BY the round-1 fixes — this project's signature failure, on schedule. (a) **The append broke the log it appended to**: D-57 and D-58 were separated from the decisions table by blank lines, so the two newest rows rendered as literal pipe-delimited text rather than table rows. (b) **The ruff config's clean run was carried by `.gitignore`, not by itself**: `exclude` REPLACES ruff's built-in default list rather than extending it, measured at **238,569** findings under `--no-respect-gitignore`. That is the 2026-08-02 config-discovery lesson exactly one turn on — knowing which config produced the green is not the same as that config controlling it. Now `extend-exclude`.
- 2026-08-10 — **D-58's numbers were wrong twice, and the second correction produced a new rule.** Round 1: "22 dead noqa directives" — 22 was the TOTAL directive count; only 17 (`BLE001`) were dead, and the other 5 (`E402`) had been live all along under the already-selected `E4`. Round 2: the counts justifying each unselected family were measured over a population INCLUDING the generated tree the config excludes from linting — `N` was 403 with generated code and **6** in hand-written code, so the honest figure did not support the argument being made. Both corrected, and the argument restated rather than propped up. Added **R9** to `cd-measurement-honesty` ("a number written in a doc must match the artefact it came from", with both traps named) and the matching check to its reviewer, because the check that found most of these findings had no rule behind it.
- 2026-08-10 — **three tests reported PASS having asserted nothing** (`if (process.platform === 'win32') return`), and two of them were the POSITIVE CONTROLS for the round-1 BLOCK fix — so the proof that the fix was not a blanket refusal had never executed on this machine. R1 ("an unrun check is not a pass") applied to our own suite; they are real `skip:` options now, and the count reads 5 skipped instead of silently claiming 5 extra passes. Related: `shim` was the ONE ok-producing arm of the new `ProbeOutcome` with no test, and it was being counted inside "All 7 checks passed. This machine can run the pipeline" — a third `CheckStatus`, `unverified`, now keeps present-but-not-executed out of a verification claim.
- 2026-08-10 — Stage 0A **COMPLETE (Ready — Almost, 4 residuals)**. Final gate from a clean rebuild: **901 TS pass / 0 fail / 5 skipped** (baseline 857 / 2), **689 Python passed / 0 fail** (unchanged — which is the evidence the ruff fixes were behaviour-preserving), `build:contracts --check` PASS, `validate:contracts` PASS (42 cases, 0 cross-validator disagreements), `skills sync --check` PASS (10 skills), `doctor` 7/7, ruff clean under the widened selection. **No milestone changed**, and no contract schema was touched — `git diff --exit-code 501f212` over the schemas pathspec is clean, and the pathspec was checked for vacuity (18 tracked files). **A7 is NOT met and is not claimed**: the branch is unpushed (T-13), so CI has never executed and "green on a clean clone" is unproven; the workflow fails loudly with a named remedy rather than degrading silently. Record: `docs/progress/cutdown-product-program-stage-0a-review.md`. Residuals: the `non_speech_cue_review` multi-asset population gap, the single-asset audio-events exemption (deliberate, pinned by a test), tag-pinned CI actions, and A7 itself.
