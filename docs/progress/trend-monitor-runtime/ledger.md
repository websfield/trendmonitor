# Progress ledger — trend-monitor-runtime

Append-only timeline. One line per lifecycle event. Proof of completion is the evidence on disk; this is the resumability layer.

- 2026-07-16 — plan-review gate READY (A-): boundary/measurement/security reviewers ×2 rounds + generalist plan-reviewer ×2 (simulation NOT READY → all N1–N15 fixed → verification pass READY). Report: `docs/progress/trend-monitor-runtime-plan-review.md`.
- 2026-07-16 — entry-gate PASS (pre-build baseline): schemas parse; dotnet build 0 warn/0 err + 454/454 architecture tests; ruff clean + 261/261 pytest; frontend typecheck + 86/86 vitest. All green, no baseline ratchet needed.
- 2026-07-16 — phase 1 started (deps: none). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 1`.
- 2026-07-16 — phase 1 implemented (docs-only): NEW `docs/initial/adr/0009-trend-monitor-runtime.md` (R1/R2 — decision + 8 invariants with rule/test citations); `docs/initial/integration-contract.md` runtime-note section (R3, additive); `RUNBOOK.md` known-seams + `docs/initial/tech-spec-trend-subsystem.md` scheduling-host note (R4). Grep confirms no "Hangfire runs the trend scan" text remains; submission-path Hangfire untouched.
- 2026-07-16 — phase 1 entry-gate PASS (docs-only; full pre-build run green same day — see baseline entry above; no JSON/code touched).
- 2026-07-16 — phase 1 reviewer boundary-reviewer: PASS (Ready · A; 0 blocking, 4 notes — 3 fixed, 1 intentional).
- 2026-07-16 — phase 1 complete — review READY at `docs/progress/trend-monitor-runtime-phase-1-review.md` (the proof Phase 2's dependency gate reads).
- 2026-07-16 — phase 2 started (deps: phase 1 — proof above). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 2`.
- 2026-07-16 — phase 2 implemented: NEW `detector/assemble.py` (`assemble_signal`, `signal_id` uuid5 business key + caller-resolved `first_seen`, `select_primary_series`), `registry/terms.py` gains `TrackedTerm.kind` (default `topic`), exports, NEW `tests/Architecture/test_trend_assemble.py` (15 tests).
- 2026-07-16 — phase 2 entry-gate PASS: pytest 276/276 (was 261 + 15 new) + ruff clean; five guard tests green inside the run.
- 2026-07-16 — phase 2 reviewer round 1: boundary-reviewer NEEDS CHANGES + measurement-reviewer NEEDS CHANGES, both on one identical CHANGE — `admit()` refresh dropped `kind` (terms.py). Fixed (`kind=existing.kind` + `test_tracked_term_kind_survives_refresh`); also took two measurement notes (empty-volumes ValueError, `_VALIDITY_DAYS` derived from lifecycle thresholds).
- 2026-07-16 — phase 2 reviewer round 2: boundary-reviewer PASS (Ready · A) + measurement-reviewer PASS (Ready · A); note fixes applied (keep-existing docstring, test header, empty-volumes regression test); final run 278/278 + ruff clean.
- 2026-07-16 — phase 2 complete — review READY at `docs/progress/trend-monitor-runtime-phase-2-review.md`.
- 2026-07-16 — phase 3 started (deps: phase 2 — proof above). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 3`.
- 2026-07-16 — phase 3 implemented: NEW `detector/run_scan.py` (spine: fetch→group→z→detect→assemble→store→coverage; pinned `SOURCE_PLATFORM` map + `DEFAULT_TRACKED_PLATFORMS` incl. blind platforms; `SCAN_WINDOW_DAYS=14` with ADR-0004 rationale), NEW `detector/identity.py` (`IdentityIndex`), exports, NEW `tests/Architecture/test_trend_run_scan.py` (12 tests incl. merge/corroboration, gaps-stay-gaps, dark-source coverage, blind-platform honesty, double-run + shifted-window idempotency, new-episode, no-network assertion).
- 2026-07-16 — phase 3 entry-gate PASS: pytest 290/290 + ruff clean; guard tests green inside the run.
- 2026-07-16 — phase 3 reviewer round 1: boundary NEEDS CHANGES (TikTok fetch hole — demonstrated; map test missing) + measurement NEEDS CHANGES (no-summation test missing; map test; mid-write KeyError). All fixed: refuse-before-fetch at entry, map pin test, dark-source whole-run drop, desync→new-episode, typed fetchers, plan-text pins.
- 2026-07-16 — phase 3 reviewer round 2: boundary closed 4/5 + 1 test-gap residual (partially-dark drop) → `test_source_dark_mid_run_drops_its_earlier_observations` added, green; measurement closed 5/6 + 1 residual (summation test empirically vacuous) → replaced with the prescribed structural lock, green. Final run 295/295 + ruff clean.
- 2026-07-16 — phase 3 complete — review READY at `docs/progress/trend-monitor-runtime-phase-3-review.md`.
- 2026-07-16 — phase 4 started (deps: phase 3 — proof above). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 4`.
- 2026-07-16 — phase 4 implemented: NEW `detector/store_durable.py` (`StateRoot` one-file durable root: signals/identity/samples/registry/ledger, atomic writes, `StateCorrupted` fail-closed; `ResolvedSampleBook` public-only origin-labelled deduped) — the N1 amnesia fix; `archive.py` repository-layer tenancy + archived-overwrite guard; `identity.py` `first_detected_at` anchor + `by_signal_id` reverse lookup; `run_scan.py` resolution capture + anchor-on-new-episode-only (a test caught the anchor being overwritten on re-detection — fixed) + resurrection skip; NEW `tests/Architecture/test_trend_store_durable.py` (14 tests).
- 2026-07-16 — phase 4 entry-gate PASS: pytest 309/309 + ruff clean.
- 2026-07-16 — phase 4 reviewer round 1: boundary NEEDS CHANGES (episode-unfaithful `by_signal_id` in-process + lost after restart; `refresh()` missing the archived guard) + measurement NEEDS CHANGES (lifetime-vs-remaining-days contract mismatch; samples persistence untested with data). All fixed: per-episode identity records serialized by replay; `refresh()` guard; lifecycle docstring + Phase 6 age-adjustment acceptance pinned; archive durations close at `valid_to`; persistence-with-data + dedupe-across-restart, episode-faithful, collision-skip, serializer-drift tests; past-date test anchor; naive-`as_of` refusal; dict-shape corruption check. Final 313/313 + ruff clean.
- 2026-07-16 — phase 4 reviewer round 2: boundary PASS (Ready · A — episode replay verified order-correct; 3 advisory notes taken: wider drift guard, naive-as_of + non-dict-JSON tests, ordering docstring); measurement NEEDS CHANGES narrowing (band also needs age-adjustment; valid_to duration untested; mixed-basis plan sentence) → all fixed same day (lifecycle + phase-6 R2 pins; exact-duration test `samples[0] == 8` — which itself caught my wrong 22 constant: the tail classifies peak, not rising). Final 315/315 + ruff clean.
- 2026-07-16 — phase 4 complete — review READY at `docs/progress/trend-monitor-runtime-phase-4-review.md`.
- 2026-07-16 — phase 5 started (deps: phases 3+4 — proofs above). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 5`.
- 2026-07-16 — phase 5 implemented: NEW `detector/run.py` (entrypoint + scheduling port: one invocation = one scan; hydration load→seed-only-new→scan→atomic persist; deterministic `synthetic_fetchers`; injectable `--as-of`), NEW `config/tracked-terms.yaml`, RUNBOOK nightly-scan section (cron documented, deployment honestly deferred), NEW `tests/Architecture/test_trend_run_entrypoint.py` (9 tests: CLI e2e, idempotency, persisted-admission survival, dark-source degrade, no-partial-commit, blind-platform gaps, injectable as_of).
- 2026-07-16 — phase 5 entry-gate PASS: pytest 324/324 + ruff clean + schemas parse. **MVP milestone demonstrated live**: `python -m c1_pattern_engine.detector.run` end-to-end → 6 signals, 0 dark, gaps stated (tiktok/instagram_reels/youtube). Reviewer gates in flight.
- 2026-07-16 — phase 5 reviewers: measurement PASS (Ready · A — own byte-identity check); boundary NEEDS CHANGES (1 line: ungitignored state root) → fixed + notes taken (clobber test, --fetchers guard, ENV constant, RUNBOOK honesty refresh, phase-8 carried notes). Final 324/324 + ruff clean.
- 2026-07-16 — phase 5 complete — **MVP milestone** — review READY at `docs/progress/trend-monitor-runtime-phase-5-review.md`.
- 2026-07-16 — phase 6 started (deps: phase 5 — proof above). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 6`.
- 2026-07-16 — phase 6 implemented: NEW `detector/tenants.py` (artefact-only-forever TenantBrief supplier); `verdict.py` IssuedVerdict + issue/resolve split (first-issuance-wins; public-only global accuracy); `lifecycle.py` `days_remaining_adjusted` (est AND band age-adjusted); `run_scan.py` scoped cross-product rendering (`feed(for_tenant=...)`) + resolution at decline/archive; `store_durable.py` ledger_issued persistence; `run.py` `--tenants`; NEW `tests/Architecture/test_trend_verdict_render.py` (13 tests — one caught my own wrong test scenario: an archived-then-resurged episode is genuinely young, `go` was correct).
- 2026-07-16 — phase 6 entry-gate PASS: pytest 337/337 + ruff clean.
- 2026-07-16 — phase 6 reviewer round 1: boundary NEEDS CHANGES (1: risk_flag unvalidated — typo softens blocked→caution) + measurement NEEDS CHANGES (2: execution-confirmed issue/resolve churn — duplicate skip outcome per scan; risk_flag, independently). Fixed: risk_flag validation + loud test (dataclass + loader); `VerdictLedger._resolved` closed-forever set + `ledger_resolved` persistence + exact-repro churn test incl. restart; notes taken (open_issues caveat, upward-bias docs, origin-pool Phase 9 MUST comment, first-issuance asymmetry docstring). Final 339/339 + ruff clean.
- 2026-07-16 — phase 6 reviewer round 2: measurement PASS (Ready · A — churn closure verified by own execution; notes taken: rebound-go docstring, compute_verdict guard); boundary PASS (Ready · A — `_resolved` persistence judged tenancy-safe; duplicate-tenant rejection taken). Final 339/339 + ruff clean.
- 2026-07-16 — phase 6 complete — review READY at `docs/progress/trend-monitor-runtime-phase-6-review.md`.
- 2026-07-16 — phase 7 started (deps: phase 5 — proof above; live HTTP + allowlist, Security gate joins). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 7`.
- 2026-07-16 — phase 7 implemented: `trend_sources:` section in `config/source-allowlist.yaml` (structurally disjoint from `sources:`; version bumped; tiktok absent by design); NEW `adapters/allowlist.py` (host+template pinned at load, deny-by-default, request-time `check_url`), `adapters/http.py` (stdlib urllib: https-only, redirects refused, 5MB bounded read, 3-attempt bounded retry, per-host pacing, all injectable), `adapters/fetchers.py` (6 fetchers, `quote(term, safe="")`, DTD refusal, one `client.get` per call, unit docs); `run.py` `--fetchers live` wiring; NEW `tests/Architecture/test_trend_fetchers.py` (22 mocked tests incl. the acquire-refusal disjointness proof, hostile-term matrix, redirect/size/retry/pacing, DTD bomb, no-follow-up-fetch).
- 2026-07-16 — phase 7 entry-gate PASS: pytest 361/361 + ruff clean. Reviewer gates (Security + Boundaries + Measurement) in flight.
- 2026-07-16 — phase 7 reviewer round 1: all three NEEDS CHANGES, 0 BLOCK, convergent. Security (B): MEDIUM DTD 64KB-window bypass + LOW path-slot test gap + LOW port-pin + INFO NaN/Infinity + INFO abort-not-exclude. Boundary (B): CHANGE-1 build_live_fetchers aborts whole scan (diverges from R4 exclude-don't-abort) + CHANGE-2 construction refusal untested through entrypoint + NOTEs (port-pin, disjointness breadth, duplicate-name shadow). Measurement (B): CHANGE hacker_news "stable" claim dishonest on a 1000-hit-truncated feed + NOTEs (google_trends inertness/geo, stale assemble.py rationale). Fixes: DTD whole-payload scan; build_live_fetchers exclude-and-surface (loud stderr, never abort); check_url port pin (None/443); load-time duplicate-name refusal; json parse_constant non-finite refusal + finite guard; hacker_news/google_trends/wikipedia docstrings corrected; assemble.py id-stability rationale de-staled. 11 new regression tests (path-slot ×5, construction-exclude, entrypoint-exclude, all-six-hosts-disjoint, port-pin, duplicate-name, non-finite-JSON). Final 372/372 + ruff clean. Accepted-with-rationale residuals (LOW/NOTE): slowloris total-time deadline (availability-only, chunked-read deferred), request-time-refusal→AdapterDark distinguishability (safe direction, near-unreachable), reddit drop-boundary-day hardening + youtube word-boundary match (conservative direction). Round-2 confirmation gates in flight.
- 2026-07-16 — phase 7 reviewer round 2: Security PASS (Ready · A — every round-1 finding closed + test-locked, fresh attacker-pass on the fixes found no new hole; confirmed the 1e400→inf path the finite-guard catches beyond parse_constant), Boundaries PASS (Ready · A — exclude-don't-abort triple-gated, disjointness holds for all six hosts, D5 closed; 1 non-blocking NOTE on excluded-source durability), Measurement PASS (Ready · A — hacker_news truncation honest, google_trends inertness verified against the detector not just docs; 2 non-blocking NOTEs). Final 372/372 + ruff clean.
- 2026-07-16 — phase 7 complete — review READY at `docs/progress/trend-monitor-runtime-phase-7-review.md` (the proof Phase 8's dependency gate reads).
- 2026-07-16 — phase 8 started (deps: phase 5 — proof above; Phase 7 READY too). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 8`.
- 2026-07-16 — phase 8 implemented: NEW `detector/coupling.py` (`apply_trend_direction` — public rising+go → `TermRegistry.admit(upgrade_origin=True)`; refuses internal-scope/unresolvable fail-closed; imports nothing from scoring/amplif/miner.arm); `registry/terms.py` NEW `AdmissionOrigin.TREND_DETECTED` (0.8, between mechanism 0.7 and client-brief 0.9) + monotonic opt-in origin upgrade on `admit`; `corpora/exemplar.py` NEW `occasion_exemplar` (production TREND_DIRECTED tagging via dataclasses.replace, Proxy invariant re-validated, D5 untouched); `run.py` wires the coupling into `run_once` (after scan, before persist; dedupe by trend_id) + carried Phase-5 note (a) seed-dedup guard (skip terms known in active OR cold) + note (b) evict_stale DECISION documented (deferred — needs observation-based last_activity refresh first); exports. NEW `tests/Architecture/test_trend_coupling.py` (12 tests: go-admit, non-go/internal/unresolvable no-admit, monotonic upgrade, occasion tags + Proxy survival, D5 unchanged, standing no-import AST test, arm-axis disjointness, seed cold-dup guard, run_once wiring).
- 2026-07-16 — phase 8 entry-gate PASS: pytest 384/384 + ruff clean. R3-cited guards green (mechanism arm-absent, synthesiser ingestion_arm_stratified_report, trend→score REQ-005e). Reviewer gates (Boundaries + Measurement) in flight.
- 2026-07-16 — phase 8 reviewer round 1: Boundaries PASS (Ready · A — one-way + fail-closed on internal/unresolvable, dedicated monotonic origin, AST no-import guard closes the reverse-direction gap, D5 intact; 1 forward NOTE: scope assertion at occasion_exemplar when production ingestion lands — taken as docstring caller-contract). Measurement NEEDS CHANGES (Ready-B: all 9 invariants hold; 1 CHANGE — stale "five origins" module docstring after adding the sixth). Fixed docstring; took 2 optional NOTEs (defensive stage=="rising" re-check; occasion_exemplar caveat). Final 384/384 + ruff clean.
- 2026-07-16 — phase 8 reviewer round 2: Measurement PASS (Ready · A — CHANGE fixed, actioned NOTEs sound/behaviour-neutral, unactioned NOTEs measurement-honest deferrals; 0.8 verified registry-eviction-only). Boundaries already PASS r1.
- 2026-07-16 — phase 8 complete — review READY at `docs/progress/trend-monitor-runtime-phase-8-review.md`.
- 2026-07-16 — phase 9 started (deps: phase 5; phase 8 soft — both READY). Checkpoint snapshot saved: `claude-jig checkpoint: trend-monitor-runtime phase 9`.
- 2026-07-16 — phase 9 implemented (final phase): NEW `submissions/merge.py` (`admit_submission_terms` registry-side; `merge_resolutions` signal-side — predate-gated confidence upgrade anchored on persisted `first_detected_at` + submission-born public `human_sourced` signals whose stage is the resolver's `observed_class`; `open_submissions_by_platform`); `submissions/submit.py` gains `label`(=term, per tech-spec API body) + `kind` and NDJSON `load_submission_book` (tz-aware `_parse_utc`, type-discriminated replay); `signals.py` NEW `detection_origin` label; `coverage.py` split now keys on ORIGIN not confidence rung (R3 axis de-conflation); `archive.py` NEW in-place `upgrade_confidence`; `run_scan.py` `book` param + merge before verdicts/coverage + NEW `_resolution_origin()` applied at 3 sites (closes the Phase 4 R3/Phase 6 sample-pool deferral); `run.py` book hydration from `<state-root>/submissions.ndjson` + `--submissions` + term admit before scan; `store_durable.py` persists `detection_origin` (`.get` default for older files); `test_trends.py` helper updated for the 2 new required fields. NEW `tests/Architecture/test_trend_submission_merge.py` (14 tests).
- 2026-07-16 — phase 9 entry-gate PASS: pytest 398/398 + ruff clean; serializer drift-guard caught the new field mid-build (fixed). Cited guards green: R2 submission-isolation (rationale-injection, self-resolution, max-open-positions, sandbagging, dead-evidence), R4 `test_creator_role_denied`, R3 `test_coverage_gap_stated`. Entrypoint smoke re-run end-to-end: 6 signals, 0 dark, gaps stated. Reviewer gates (Boundaries + Measurement) in flight.
- 2026-07-16 — phase 9 reviewer round 1: **BOTH gates BLOCK (grade D)** — 4 BLOCKs total, every one reproduced by the reviewers with runtime probes against the real spine. Measurement: (B1) the `human` resolution pool was definitionally CONSTANT — born signals are never re-detected so `valid_to` never moves and archive-close recorded the horizon constant every time (probe `[21.0]*5`), giving MAD=0 and a zero-width days-remaining band on a pure presumption; (B2) the spine silently relabelled `human_sourced` signals `automated` on re-detection (same uuid5 → `store.add` overwrote with the default), overstating automated reach — the R3 conflation in reverse. Boundary: (B3) R1's public-scope rule was asserted but never enforced — a `role: "client"` row (REQ-005a explicitly permits clients) minted a public signal every tenant can read and put a client label into the shared `TermRegistry`; (B4) resolver independence was bypassable — `load_submission_book` is a replay that bypasses `resolve()`'s self-resolution check, so a hand-written `resolver_id == submitter_id` row let a submitter's own claim set the stage driving verdicts. Root cause common to B3/B4: **the NDJSON book is untrusted input and I trusted invariants enforced elsewhere.** Fixes: born-archive is CENSORED and never sampled (human pool stays empty → band-only, honest); `detection_origin` immutable at the store layer (birth property); `INTERNAL_SUBMISSION_ROLES` staff-only gate in both merge functions; self-resolution re-checked at point of use; `observed_class` validated against new public `assemble.VALIDITY_STAGES`; stale born signals refused (`valid_to < as_of`); born never minted over any prior episode; born writes `first_detected_at=None` and the spine sets the anchor on first genuine automated detection; naive `submitted_at` refused at construction; orphan rows skip+log; `validity_horizon_days()` promoted so horizons can't desync; coverage notes name open submissions; docstring overclaim on the credit anchor narrowed to the upgrade gate + deferral recorded. 14 new regression tests (incl. the reviewer's exact 5-signal pool probe). The new stale-guard immediately caught two of my own unrealistic fixtures. Final 412/412 + ruff clean. Round-2 gates in flight.
- 2026-07-16 — phase 9 reviewer round 2: Measurement **BLOCK** (D+) — all 7 round-1 findings verified fixed by re-probe (human/automated pools now both `[]` where the probe once read `[21.0]*5`; birth origin survives re-detection), but my BLOCK-2 fix INTRODUCED a new one-line defect: the declining-close passed the locally *assembled* signal to `_resolution_origin`, and since `assemble_signal` never sets `detection_origin` that object always carries the `automated` default while the *stored* one is `human_sourced` — so a born signal's human-basis lifetime (measured from the human's first sighting) landed in the AUTOMATED pool, the one that reaches MIN_RESOLUTIONS and publishes a median/MAD, systematically inflated and loosening the `go` lead-time guard (reproduced: `automated=[9.0]`). My own comment `# assembled signals are automated` was true before the fix and false after it. Boundary **NEEDS CHANGES** (B) — both round-1 BLOCKs verified genuinely closed (the reviewer re-ran its attacks through the real NDJSON replay, incl. role-string variants it invented: `"Manager"`, `" manager"`, `"admin"` — all refuse); 2 CHANGEs left: (i) an import cycle making `submissions.merge` unimportable standalone (invisible to the suite, which always imports `detector` first), (ii) the role gate applied at 2 of 3 use sites — a client's open submission still reached the tenant-neutral coverage count. Fixes: declining-close resolves origin from the STORED record (which also gives the human pool its one genuine write path — an observed decline, as opposed to censored archive-aging); `run_scan` imports the merge at CALL time with `SubmissionBook` under `TYPE_CHECKING` so the module-scope dependency is one-way; `_is_staff()` gates `open_submissions_by_platform`; naive-timestamp policy documented (replay coerces, programmatic refuses). 3 new tests incl. a standing AST layering guard (exempting `run.py` as the composition root, and asserting `detector/__init__` never imports it so the exemption can't become a cycle). Final 415/415 + ruff clean; standalone import verified. Round-3 confirmation gates in flight.
- 2026-07-16 — phase 9 reviewer round 3: Boundaries **PASS (Ready · A)** — arc D→B→A; both CHANGEs closed (import cycle broken via call-time imports + `TYPE_CHECKING`, verified `import c1_pattern_engine.submissions.merge` standalone; role gate now at all 3 use sites), 2 NOTEs taken (my AST layering guard walked only top-level `body` so a `try:`-nested import would have slipped through — hardened to recurse into executing blocks and verified against 4 synthetic cases; unreachable-defensive archive line labelled). Measurement **NEEDS CHANGES, 0 BLOCK** — round-2 BLOCK verified fixed by its own probe (`HUMAN [9.0] / AUTOMATED []`); 3 findings on doc-accuracy + test-strength, all closed by evidence: the `ResolvedSampleBook` docstring named the wrong axis (both pools close on an observed volume decline — what differs is the START anchor, so a human-anchored lifetime includes the lead time) → rewritten; the stale "future source" comment invited a third-basis mix → rewritten; the new regression test asserted only a negative behind a hedge and could pass vacuously → now asserts non-vacuity plus the reviewer's own probe value `samples("reddit","human") == [9.0]`. Also corrected the censoring justification (empirically false for born signals on covered platforms — their `valid_to` does move) and added a test pinning verdict-side pool selection by origin, where the two-basis safety now rests. Documented the deliberate censoring ASYMMETRY (human = observed declines only → censored short → tightens the `go` guard; automated also takes archive upper bounds) with an explicit "do not symmetrise". Final 416/416 + ruff clean.
- 2026-07-16 — phase 9 complete — **final phase** — review READY at `docs/progress/trend-monitor-runtime-phase-9-review.md`. Boundaries PASS (A); Measurement 0 BLOCK with doc/test findings closed by evidence (stated as such in the card, not reported as a PASS).
- 2026-07-22 — `/sync-docs` run. Mechanical: `docs/initial/README.md` ADR index was missing **0008 and 0009** (both on disk — 0008 already missing pre-build) → rows added; `CLAUDE.md` "ADRs 0001–0007" → 0001–0009; `RUNBOOK.md` nightly-scan section had three stale facts (`--fetchers fake # live adapters land in Phase 7` after Phase 7 shipped; example missing `--tenants`/`--submissions`; env list missing `TREND_MONITOR_TENANTS_FILE`/`TREND_MONITOR_SUBMISSIONS_FILE`) → corrected, plus a pointer to `ops-todos.md` and the honest note that no live source has ever been contacted from this code. Subjective (asked, all approved): `CLAUDE.md` build-summary now names the trend-monitor runtime (it previously said only "Hangfire jobs still planned", so an agent reading the always-loaded file wouldn't know the runtime exists); **ADR-0009 gains invariants 9/10/11** — origin-vs-confidence de-conflation with origin as an immutable birth property; the deliberate two-pool censoring asymmetry with an explicit "do not symmetrise"; staff-only submission scope with the untrusted-replay point-of-use rule — each citing its guard tests (all 8 verified passing before the citations were written). Reverse drift resolved by decision: **REQ-005a amended** (`tech-spec-trend-subsystem.md`) from "manager, client, or resolver" to "manager or resolver", dated, with the tenancy rationale and pointers to ADR-0009 inv 11 + `ops-todos.md` 8b; the amendment also fixed a pre-existing internal inconsistency (REQ-005a's field list omitted `kind`/`label` that its own API block already required). Feed *read* gate unchanged (REQ-005g). Verified: 416/416, ruff clean, schemas parse.
- 2026-07-16 — `ops-todos.md` corrected after the boundary gate caught item 7 asserting something the code did not do (self-resolution "enforced in code" was false on the NDJSON path — now true in two places, and stated precisely); NEW item 8b records the REQ-005a client-submission divergence as an explicit product decision, and item 8 records the `max_open_positions` replay bypass as a known limitation of the interim file surface.
- 2026-07-16 — NEW `ops-todos.md` at repo root: 11 human-only tasks surfaced by the build (scheduler install, day-boundary decision, live-source verification, trend-host ToS review, tenant briefs, editorial seed terms, resolver staffing, submission ingestion decision, state-root backup+restore drill, monitoring/alerting, D5 ratification). Non-coding by construction — coding follow-ups stay in the phase plans/review cards.
