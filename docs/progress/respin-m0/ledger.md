# Progress ledger — respin-m0 (append-only)

- 2026-08-14 — plan gate: respin-tenancy-reviewer round 1 NEEDS CHANGES (5+5) → fixed; round 2 conditions met (2 residuals + 4 notes) → fixed; plan-reviewer READY (`docs/progress/respin-m0-plan-review.md`)
- 2026-08-14 — phase 1 started
- 2026-08-14 — phase 1 complete: scaffold + CI green locally (install/typecheck/lint/test/build all exit 0; lint provenance = respin/eslint.config.mjs; boundary-rule fixtures pass both directions; workspaces.json blocking check wired; R-16 appended). Evidence: command outputs this session; files on disk.
- 2026-08-14 — phase 2 started
- 2026-08-14 — phase 2 complete: @respin/db (users/workspaces/memberships, migration 0000_serious_kylun.sql committed, dev-guarded idempotent seed) — typecheck/lint green, 14/14 tests (migration-on-fresh-PGlite, uniques, cascades both ways, seed idempotency + AC-8 guard, createDb env error), db:check "Everything's fine", CI gained drift-check step, R-17 appended.
- 2026-08-14 — phase 3 started
- 2026-08-14 — phase 3 complete: Clerk v7 middleware (URL-prefix matchers from lib/routes.ts, admin allowlist fail-closed in middleware), (auth)/(product)/(admin) pages, ensureUserWorkspace (transactional, resolve-existing-on-conflict, repair path), withWorkspace (verify-membership-then-scope, enumerable accessor map), default-deny lint (allowImportNames proven by fixtures). 41/41 tests; typecheck+lint green; keyless `pnpm build` green with pinned URL topology (/ static; /studio,/admin,/sign-in,/sign-up dynamic). Evidence-pending (owner keys): live sign-up→shell→sign-out.
- 2026-08-14 — phase 4 started
- 2026-08-14 — phase 4 complete: vercel.json (installCommand + ignoreCommand, keys verified against current Vercel docs; Root Directory documented as dashboard-only), env template at respin/env.example (NOT .env.example — the pack's deny rule blocks .env.* writes; README/.gitignore updated to match), owner runbook README with per-step success checks, CLAUDE.md Commands block gained the Respin entry gate.
- 2026-08-14 — M0 engineering-vs-evidence table:
  | Criterion | Status |
  |---|---|
  | typecheck/lint/test/build + db:check green locally | ENGINEERING ✅ (this session's command outputs) |
  | Migration applies to fresh DB; drift check clean | ENGINEERING ✅ (PGlite suite + db:check) |
  | Cross-workspace read suite + default-deny lint | ENGINEERING ✅ (41/41 tests incl. breach fixtures) |
  | Path-scoped CI workflow exists | ENGINEERING ✅ (.github/workflows/respin.yml; proof-on-GitHub pending push) |
  | Live sign-up → shell → sign-out (Clerk) | EVIDENCE ⏳ pending owner (README §One-time service setup step 2) |
  | PR preview deploy, Root Directory respin/ | EVIDENCE ⏳ pending owner (README step 3) |
  | Fresh clone passes CI on GitHub | EVIDENCE ⏳ pending push (git policy: only on owner ask) |
- 2026-08-14 — least-confident: the Clerk v7 middleware wiring (auth.protect + admin 404 return) is proven only at the matcher/allowlist unit level — the full middleware request path has never executed against a real Clerk instance; if v7's auth.protect redirect semantics differ from what the code assumes, the sign-in flow could loop or the admin 404 could short-circuit Clerk's own handling.
- 2026-08-14 — entry gate PASS (full repo): respin typecheck/lint/test(41)/db:check/keyless-build green; schemas parse; dotnet build 0 errors + 454/454; pytest all pass; ruff clean; frontend typecheck + 86/86 — no new failures vs the green baseline.
- 2026-08-14 — reviewer respin-tenancy-reviewer (code round 1): NEEDS CHANGES — 3 CHANGE (tested matcher ≠ deployed matcher; createRouteMatcher deprecated in installed v7; flaky ESLint-test timeout) + 5 NOTEs. All fixed same session (middleware now deploys the tested functions; deprecated API removed; shared ESLint engine + 30s testTimeout; lint guard widened to everything-except-packages/tests; client.ts remedy names env.example; README requires the Clerk URL env vars). Suite re-run twice: 41/41 both.
- 2026-08-14 — reviewer respin-tenancy-reviewer (code round 2): PASS (Ready, A) — fixes verified by execution incl. live lint-scope probes; sole residual is the agreed M1 email-guard deferral, now recorded in the master-plan Deferral Ledger.
- 2026-08-14 — M0 complete (engineering). Evidence rows remain pending owner execution per the table above; next milestone M1 (billing + credit ledger).
- 2026-08-14 — stack pivot (owner-directed, R-18): Neon/Clerk/Vercel dropped for Lightsail + self-hosted Postgres. Local dev DB LIVE: respin/docker-compose.yml (postgres:17-alpine, port 5435), migration applied, seed verified by in-container psql (3 tables; dev user/workspace/owner-membership row). vercel.json removed; tech-spec §1 hosting/db rows updated; env.example + README rewritten. Auth replacement: OPEN DECISION — Clerk wiring stands until it lands; the swap is a tenancy-path gated change.
- 2026-08-14 — auth swap built (R-19): @respin/auth (createAuth factory, lazy instance, split /client entrypoint, requireUser/requireAdmin, lazy authHandlers), auth schema CLI-generated into packages/db (user/session/account/verification), migration 0000 regenerated (pre-first-commit allowance) + Docker DB rebuilt and verified (7 tables), domain identity renamed auth_user_id, own sign-in/up forms, middleware demoted to optimistic cookie redirect, gate-completeness test + default-deny lint extended. Evidence: 55/55 tests incl. REAL sign-up→session on PGlite (AC-1), fail-closed admin matrix (AC-3), studio server-refusal (AC-9), completeness fixture (AC-10), lint fixtures (AC-5), reset-URL guard; keyless build green (sign-in/up now STATIC, middleware 34.8 kB); CLERK-ZERO grep clean repo-wide (AC-6). Repo entry gate: unaffected trees untouched since the 15:05 green run.
- 2026-08-14 — T4 recording (code-gate CHANGE 1): the four Better Auth tables — `user`, `session` (incl. `ip_address`, `user_agent`), `account` (incl. password hashes and future OAuth `access_token`/`refresh_token`), `verification` — are part of the REQ-A04 export AND deletion surface. Because domain `users` ↔ auth `user` deliberately has no FK until M1, the M6 deletion flow must delete BOTH sides explicitly; an export or deletion that omits an auth table is a T4 finding. (This line was promised in the round-2 fix report before it was actually on disk — a && chain after a failed mv skipped the append while the status echo still printed; caught by the reviewer's full-file grep, appended and read back now.)
- 2026-08-14 — auth swap code gate FINAL: respin-tenancy-reviewer PASS (Ready, A) — all findings across the plan gate and three code rounds closed, each verified by execution or read-back. M0 (as amended by R-18/R-19) engineering-complete; evidence pending: local sign-up run (now zero-external-account) + first Lightsail deploy.
- 2026-08-14 — EVIDENCE: local sign-up → shell → sign-out executed live (auth-swap Exit Demonstration). On the Docker DB (`respin-postgres`, healthy): `pnpm dev` → `/sign-up` email/password ("M0 Evidence Runner" / m0-evidence@example.com) → landed on `/studio` with the workspace shell banner ("M0 Evidence Runner's workspace") → Sign out → landed on `/`. DB verified in-container: auth `user` row + domain `users` row + workspace + owner membership + 1 `session` row, and the session row gone (0) after sign-out. Post-sign-out `/studio` visit redirected to `/sign-in` (gate holds). Only console error: missing favicon (404, cosmetic). Screenshot: `.playwright-mcp/m0-evidence-studio.png`. Remaining evidence rows: first Lightsail deploy + fresh-clone CI on GitHub (both owner/push-bound).
