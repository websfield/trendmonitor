# Workspace packages resolve to their in-repo source, never the registry.
link-workspace-packages=true
# Engine pins in package.json are enforced, not advisory (decisions.md D-39).
engine-strict=true
