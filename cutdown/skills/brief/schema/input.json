{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cutdown.local/skills/brief/input.json",
  "title": "BriefRequest",
  "description": "Request for the `brief` skill. The brief document itself is passed as an opaque object and validated against job-brief-v1 by the skill \u2014 not declared inline here, so there is exactly one place the JobBrief shape is defined.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "jobId",
    "brief"
  ],
  "properties": {
    "jobId": {
      "type": "string",
      "minLength": 1,
      "description": "The job this brief opens. Becomes a directory name under project-data/jobs/. Becomes a directory name under `project-data/jobs/`. The pattern is defence in depth \u2014 `jobDir()` in @cutdown/skill-runtime guards and containment-checks it regardless of caller, because a skill's `entrypoint` is a documented direct invocation.",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$"
    },
    "sourcePath": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "null"
        }
      ],
      "description": "Where the brief was read from, recorded for provenance. Null when authored in-memory (e.g. by the .claude mirror from a conversational ask)."
    },
    "brief": {
      "type": "object",
      "description": "The candidate JobBrief. Validated against job-brief-v1; envelope and briefId are filled in if absent."
    }
  }
}
