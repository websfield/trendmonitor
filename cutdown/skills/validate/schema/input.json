{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cutdown.local/skills/validate/input.json",
  "title": "ValidateRequest",
  "type": "object",
  "additionalProperties": false,
  "required": ["jobId", "edlId"],
  "properties": {
    "jobId": { "type": "string", "minLength": 1 },
    "edlId": { "type": "string", "minLength": 1, "description": "The PlatformEDL to validate." },
    "recordedModelPath": {
      "oneOf": [{ "type": "string", "minLength": 1 }, { "type": "null" }],
      "description": "Test/fixture only: a recorded critic response replayed over an injected transport."
    },
    "boundsPath": {
      "oneOf": [{ "type": "string", "minLength": 1 }, { "type": "null" }],
      "description": "Optional file with an array of {assetId, duration} AssetBounds. When absent, bounds are read from the job's SourceAsset preflight."
    },
    "styleProfilePath": {
      "oneOf": [{ "type": "string", "minLength": 1 }, { "type": "null" }],
      "description": "Optional StyleProfile whose prohibitedClaims are added to the prohibited-claim gate."
    }
  }
}
