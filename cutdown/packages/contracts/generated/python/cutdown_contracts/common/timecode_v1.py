# generated by datamodel-codegen:
#   filename:  common/timecode-v1.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Timecode(BaseModel):
    pass


class Timebase(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    num: conint(ge=1)
    den: conint(ge=1)


class MediaTime(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    ticks: conint(ge=0)
    timebase: Timebase


class SourceRange(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    assetId: constr(pattern=r'^[0-9A-HJKMNP-TV-Z]{26}$') = Field(
        ..., description='ULID of the SourceAsset this range indexes into.'
    )
    startTicks: conint(ge=0)
    endTicks: conint(ge=1) = Field(
        ...,
        description='Exclusive end. `endTicks > startTicks` is enforced by range-check.ts, not by JSON Schema — draft 2020-12 cannot express a cross-property inequality, and tech-spec §3 forbids `if/then/else`.',
    )
    timebase: Timebase


class ContentHash(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    algorithm: Literal['sha256']
    value: constr(pattern=r'^[0-9a-f]{64}$')


class Ulid(RootModel[constr(pattern=r'^[0-9A-HJKMNP-TV-Z]{26}$')]):
    root: constr(pattern=r'^[0-9A-HJKMNP-TV-Z]{26}$') = Field(
        ...,
        description="Object identity (tech-spec §3: 'object IDs are ULIDs'). Crockford base32, 26 chars, lexicographically sortable by creation time.",
        title='Ulid',
    )
