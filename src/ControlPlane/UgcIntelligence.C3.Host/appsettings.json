{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "Non-secret config only (golden rule 2). The Python calibration-source endpoint URL and any store connection string arrive via environment variables / a secret manager in R4b, never here."
}
