{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "Non-secret config only (golden rule 2). Secrets (LLM key, DB/blob connection strings, C3 breaker endpoint URL) arrive via environment variables / a secret manager in R4b, never here.",
  "C2": {
    "BreakerTransport": "fail_closed_stub_until_r4b"
  }
}
