"""C1 Pattern Engine — the belief-producing component of the intelligence plane.

Phase 7 populates the *trend subsystem*: term registry, keyless source adapters, the robust-z
detector, lifecycle/verdict logic, and the human-submission book. It depends only on Phase 0
(``substrate.provenance``) and reuses Phase 2's ``extraction.untrusted``. It calls no scorer, no
breaker, and no outcome event — *"Trends are disposable. Mechanisms compound."*
"""

from __future__ import annotations
